diff --git a/node_modules/ngx-chips/esm2020/components/dropdown/tag-input-dropdown.component.mjs b/node_modules/ngx-chips/esm2020/components/dropdown/tag-input-dropdown.component.mjs
index 7314db7..9e99147 100644
--- a/node_modules/ngx-chips/esm2020/components/dropdown/tag-input-dropdown.component.mjs
+++ b/node_modules/ngx-chips/esm2020/components/dropdown/tag-input-dropdown.component.mjs
@@ -98,7 +98,7 @@ export class TagInputDropdown {
             if ((!this.showDropdownIfEmpty && !value) ||
                 maxItemsReached ||
                 isDisabled) {
-                return this.dropdown.hide();
+                return this.safeHideDropdown();
             }
             this.setItems(items);
             if (shouldShow) {
@@ -138,7 +138,7 @@ export class TagInputDropdown {
                     this.dropdown.show(this.calculatePosition());
                 }
                 else {
-                    this.dropdown.hide();
+                    this.safeHideDropdown();
                 }
             };
             this.autocompleteObservable(text)
@@ -229,12 +229,21 @@ export class TagInputDropdown {
     get state() {
         return this.dropdown.menu.dropdownState;
     }
+    /**
+     * @name safeHideDropdown
+     */
+    safeHideDropdown() {
+        if (!this.dropdown || typeof this.dropdown.hide !== 'function') {
+            return;
+        }
+        return this.dropdown.hide();
+    }
     /**
      * @name hide
      */
     hide() {
         this.resetItems();
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name scrollListener
@@ -249,7 +258,7 @@ export class TagInputDropdown {
      * @name onWindowBlur
      */
     onWindowBlur() {
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name getFormValue
diff --git a/node_modules/ngx-chips/fesm2015/ngx-chips.mjs b/node_modules/ngx-chips/fesm2015/ngx-chips.mjs
index 3cc02e0..5446de7 100644
--- a/node_modules/ngx-chips/fesm2015/ngx-chips.mjs
+++ b/node_modules/ngx-chips/fesm2015/ngx-chips.mjs
@@ -987,7 +987,7 @@ class TagInputDropdown {
             if ((!this.showDropdownIfEmpty && !value) ||
                 maxItemsReached ||
                 isDisabled) {
-                return this.dropdown.hide();
+                return this.safeHideDropdown();
             }
             this.setItems(items);
             if (shouldShow) {
@@ -1027,7 +1027,7 @@ class TagInputDropdown {
                     this.dropdown.show(this.calculatePosition());
                 }
                 else {
-                    this.dropdown.hide();
+                    this.safeHideDropdown();
                 }
             };
             this.autocompleteObservable(text)
@@ -1118,12 +1118,21 @@ class TagInputDropdown {
     get state() {
         return this.dropdown.menu.dropdownState;
     }
+    /**
+     * @name safeHideDropdown
+     */
+    safeHideDropdown() {
+        if (!this.dropdown || typeof this.dropdown.hide !== 'function') {
+            return;
+        }
+        return this.dropdown.hide();
+    }
     /**
      * @name hide
      */
     hide() {
         this.resetItems();
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name scrollListener
@@ -1138,7 +1147,7 @@ class TagInputDropdown {
      * @name onWindowBlur
      */
     onWindowBlur() {
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name getFormValue
diff --git a/node_modules/ngx-chips/fesm2020/ngx-chips.mjs b/node_modules/ngx-chips/fesm2020/ngx-chips.mjs
index 44f7850..0ff8aab 100644
--- a/node_modules/ngx-chips/fesm2020/ngx-chips.mjs
+++ b/node_modules/ngx-chips/fesm2020/ngx-chips.mjs
@@ -986,7 +986,7 @@ class TagInputDropdown {
             if ((!this.showDropdownIfEmpty && !value) ||
                 maxItemsReached ||
                 isDisabled) {
-                return this.dropdown.hide();
+                return this.safeHideDropdown();
             }
             this.setItems(items);
             if (shouldShow) {
@@ -1026,7 +1026,7 @@ class TagInputDropdown {
                     this.dropdown.show(this.calculatePosition());
                 }
                 else {
-                    this.dropdown.hide();
+                    this.safeHideDropdown();
                 }
             };
             this.autocompleteObservable(text)
@@ -1117,12 +1117,21 @@ class TagInputDropdown {
     get state() {
         return this.dropdown.menu.dropdownState;
     }
+    /**
+     * @name safeHideDropdown
+     */
+    safeHideDropdown() {
+        if (!this.dropdown || typeof this.dropdown.hide !== 'function') {
+            return;
+        }
+        return this.dropdown.hide();
+    }
     /**
      * @name hide
      */
     hide() {
         this.resetItems();
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name scrollListener
@@ -1137,7 +1146,7 @@ class TagInputDropdown {
      * @name onWindowBlur
      */
     onWindowBlur() {
-        this.dropdown.hide();
+        this.safeHideDropdown();
     }
     /**
      * @name getFormValue
