{
  "name": "c8oforms-tests",
  "private": true,
  "description": "E2E regression tests for C8oForms, driven by GitHub issues",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "verify": "./verify.sh",
    "runner": "node runner/server.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "dotenv": "^16.4.5"
  }
}
