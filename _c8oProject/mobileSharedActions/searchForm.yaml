↓doSearchForm [ngx.components.UICustomAction-1605199059317]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '//		console.log(page)
          //		if (page.local["ddoc"] == undefined || page.local["ddoc"] == null) {
          //			if (page[''pageOwner''] != undefined && page[''pageOwner'']?.local["ddoc"] != undefined) {				
          //				page.local["ddoc"] = page[''pageOwner''].local["ddoc"];
          //			}
          //		}
          		if (page["pageOwner"] != undefined) {
          			page = page["pageOwner"];
          		}
          		
          		if (
          			page.local["ddoc"] == undefined ||
          			page.local["ddoc"] == null ||
          			page.local["ddoc"] == ""
          		) {
          			page.local["ddoc"] =
          				page.local.published === true
          					? "published_formsV2"
          					: "formsV2";
          		}
          		function readEditionFilters(){
          			if(page["getEditionFilters"] != undefined){
          				return page["getEditionFilters"]();
          			}
          			let filters;
          			let local_str_data = window.localStorage.getItem("c8oforms_edition_filters");
          			try{
          				filters = JSON.parse(local_str_data);
          			}
          			catch(e){
          				
          			}
          			return filters == null ? {} : filters;
          		}
          		let filters = readEditionFilters();
          		const createPaginationState = () => {
          			return {
          				pageIndex: 0,
          				pageSize: 25,
          
                          currentPageToken: null,
                          previousPageToken: null,
                          nextPageToken: null,
                          lastPageToken: null,
                          pageTokenHistory: [],
          
                          hasPreviousPage: false,
                          hasNextPage: false,
                          totalCount: null,
                          pageCount: null,
                          mode: null,
                          paginated: false,
                          loading: false
          			};
          		};
          
          		const getPaginationTarget = ():
          			"edition" | "publication" => {
          			return page.local["ddoc"] ===
          				"published_formsV2"
          					? "publication"
          					: "edition";
          		};
          
          		const normalizePageSize = (
          			value
          		): number | "infinite" => {
          			if (
          				typeof value === "string" &&
          				value
          					.toLowerCase()
          					.trim() === "infinite"
          			) {
          				return "infinite";
          			}
          
          			const numericValue =
          				Number(value);
          
          			if (
          				Number.isInteger(numericValue) &&
          				numericValue > 0
          			) {
          				return Math.min(
          					numericValue,
          					100
          				);
          			}
          
          			return 25;
          		};
          
          		const ensurePaginationState = () => {
          			if (
          				page["pagination"] == null ||
          				typeof page["pagination"] !== "object"
          			) {
          				page["pagination"] = {};
          			}
          
          			const targets = [
          				"edition",
          				"publication"
          			];
          
          			for (const targetName of targets) {
          				if (
          					page["pagination"][targetName] == null ||
          					typeof page["pagination"][targetName]
          						!== "object"
          				) {
          					page["pagination"][targetName] =
          						createPaginationState();
          
          					continue;
          				}
          
          				const defaults =
          					createPaginationState();
          
          				for (const key of Object.keys(defaults)) {
          					if (
          						page["pagination"][targetName][key]
          							=== undefined
          					) {
          						page["pagination"][targetName][key] =
          							defaults[key];
          					}
          				}
          
          				if (
          					!Array.isArray(
          						page["pagination"][targetName]
          							.pageTokenHistory
          					)
          				) {
          					page["pagination"][targetName]
          						.pageTokenHistory = [];
          				}
          
          				page["pagination"][targetName]
          					.pageSize =
          						normalizePageSize(
          							page["pagination"][targetName]
          								.pageSize
          						);
          			}
          
          			const target =
          				getPaginationTarget();
          
          			return {
          				target,
          				pagination:
          					page["pagination"][target]
          			};
          		};
          
          		const resetPaginationState = (
          			pagination
          		): void => {
          			const pageSize =
          				normalizePageSize(
          					pagination.pageSize
          				);
          
          			pagination.pageIndex = 0;
          			pagination.pageSize = pageSize;
          
                      pagination.currentPageToken = null;
                      pagination.previousPageToken = null;
                      pagination.nextPageToken = null;
                      pagination.lastPageToken = null;
                      pagination.pageTokenHistory = [];
          
                      pagination.hasPreviousPage = false;
                      pagination.hasNextPage = false;
                      pagination.totalCount = null;
                      pagination.pageCount = null;
                      pagination.paginated = false;
          		};
              page.local.filters = filters;
          	
          //    var currentCall = vars.call != null && vars.call != undefined ? vars.call : new Date().getTime();
          //    page.local.totalCall = currentCall;
          //    page.local.spinner = true;
          //    page.local.loadingError = false;
          //    page.local.alreadyFinished = false;
          //    page.local.count = undefined;
          //    page["list"] = [];
          //    page.local.showAdvancedSearch = false;
          //	if (page.ref != undefined && page.ref.detectChanges != undefined) {
          //	  page.ref.detectChanges();
          //	}
          	const paginationContext =
          		ensurePaginationState();
          	
          	const paginationTarget =
          		paginationContext.target;
          	
          	const pagination =
          		paginationContext.pagination;
          	
          	/*
          	 * À utiliser lorsqu''une recherche, un tag, un sous-tag,
          	 * un utilisateur ou un filtre vient de changer.
          	 */
          	if (vars.resetPagination === true) {
          		resetPaginationState(
          			pagination
          		);
          	}
          	
          	const infiniteMode =
          		pagination.pageSize ===
          			"infinite";
          	
              if (
                  !Number.isFinite(
                      Number(page.local.requestCounter)
                  )
              ) {
                  page.local.requestCounter = 0;
              }
              const hasProvidedCall =
                  vars.call != null &&
                  vars.call != undefined;
              const currentCall =
                  hasProvidedCall
                      ? vars.call
                      : Number(page.local.requestCounter) + 1;
              if (!hasProvidedCall) {
                  /*
                   * Une recherche directe invalide aussi les requêtes de liste
                   * lancées avant elle par callViewLiveAndFillFormList.
                   */
                  page.local.requestCounter = currentCall;
              }
              else if (
                  Number.isFinite(Number(currentCall)) &&
                  Number(currentCall) >
                      Number(page.local.requestCounter)
              ) {
                  page.local.requestCounter = Number(currentCall);
              }
              page.local.totalCall =
          		currentCall;
          	
              const normalizeSearchQuery = (value) => {
                  return value == null
                      ? ""
                      : ("" + value).trim();
              };
              const normalizeSearchList = (value) => {
                  return Array.isArray(value)
                      ? value.filter((entry) => {
                          const rawValue =
                              entry != null &&
                              entry.value != undefined
                                  ? entry.value
                                  : entry;
                          return rawValue != null &&
                              ("" + rawValue).trim() != "";
                      })
                      : [];
              };
              const normalizeSearchUsers = (value) => {
                  const users = Array.isArray(value)
                      ? value
                          .map((entry) => {
                              return entry != null &&
                                  entry.value != undefined
                                      ? entry.value
                                      : entry;
                          })
                          .filter((entry) => {
                              return entry != null &&
                                  ("" + entry).trim() != "";
                          })
                      : [];
                  return users.filter((entry, index) => {
                      return users.indexOf(entry) === index;
                  });
              };
              const useActiveSearchRefresh =
                  vars.activeSearchRefresh === true &&
                  typeof page["getActiveSearchQuery"] === "function";
              const normalizedQuery =
                  normalizeSearchQuery(
                      useActiveSearchRefresh
                          ? page["getActiveSearchQuery"].call(page)
                          : page["query"]
                  );
              if (useActiveSearchRefresh !== true) {
                  page["query"] =
                      normalizedQuery;
                  if (page.local["_query"] !== undefined) {
                      page.local["_query"] =
                          normalizedQuery;
                  }
              }
              let activeSearchTags =
                  normalizeSearchList(
                      page.local.searchTags
                  );
              const activeSearchSubTags =
                  normalizeSearchList(
                      page.local.searchSubTags
                  );
              const activeSearchUsers =
                  normalizeSearchUsers(
                      page.local._searchUser
                  );
              const resetSelectedUserCollaborationFilter = () => {
                  if (
                      filters == null ||
                      filters.my_collaborations !== true ||
                      activeSearchUsers.length === 1
                  ) {
                      return;
                  }
                  filters.my_collaborations = false;
                  if (page.local.data != null) {
                      page.local.data.my_collaborations = false;
                  }
                  page.local.filters = filters;
                  try {
                      if (
                          typeof page["persistEditionFilters"] === "function" &&
                          page.local.data != null
                      ) {
                          page["persistEditionFilters"].call(
                              page,
                              page.local.data
                          );
                      }
                      else {
                          window.localStorage.setItem(
                              "c8oforms_edition_filters",
                              JSON.stringify(filters)
                          );
                      }
                  }
                  catch (e) {}
              };
              resetSelectedUserCollaborationFilter();
              if (
                  normalizedQuery == "" &&
                  activeSearchTags.length == 1 &&
                  activeSearchSubTags.length == 0 &&
                  activeSearchUsers.length == 0
              ) {
                  const folderTag =
                      activeSearchTags[0];
                  page.local.folder =
                      folderTag != null &&
                      folderTag.value != undefined
                          ? folderTag.value
                          : folderTag;
                  page.local.searchTags = [];
                  activeSearchTags = [];
              }
              if (
                  normalizedQuery == "" &&
                  activeSearchTags.length == 0 &&
                  activeSearchSubTags.length == 0 &&
                  activeSearchUsers.length == 0
              ) {
                  resetPaginationState(
                      pagination
                  );
                  page.local.searchTags = [];
                  page.local.searchSubTags = [];
                  page.local._searchUser = null;
                  page.local.showAdvancedSearch = false;
                  page.local.loadingError = false;
                  page.local.alreadyFinished = false;
                  page.local.count = undefined;
                  page.local.returnedCount = undefined;
                  page.local.spinner = true;
                  if (!Array.isArray(page["list"])) {
                      page["list"] = [];
                  }
                  if (
                      page.ref != undefined &&
                      page.ref.detectChanges != undefined
                  ) {
                      page.ref.detectChanges();
                  }
                  if (typeof this.STS1567094239807 == "function") {
                      const liveStack =
                          props && props.stack
                              ? props.stack
                              : {
                                  root: {
                                      scope: {},
                                      in: {},
                                      out: event
                                  }
                              };
                      const liveParent =
                          props
                              ? props.parent
                              : null;
                      this.STS1567094239807(
                          page,
                          {
                              stack: liveStack,
                              parent: liveParent,
                              out: event
                          },
                          Object.assign(
                              {},
                              liveStack["root"].in,
                              {
                                  keys: null,
                                  resetPagination: true
                              }
                          ),
                          event
                      )
                      .then(() => {
                          if (typeof page["clearActiveSearchQuery"] === "function") {
                              page["clearActiveSearchQuery"].call(page);
                          }
                          else {
                              page.local.activeSearchQuery = "";
                          }
                          resolve();
                          return null;
                      })
                      .catch((error) => {
                          page.c8o.log.error(
                              "[doSearchForm] empty search reset failed " +
                              JSON.stringify(error)
                          );
                          pagination.loading = false;
                          page.local.spinner = false;
                          page.local.loadingError = true;
                          if (
                              page.ref != undefined &&
                              page.ref.detectChanges != undefined
                          ) {
                              page.ref.detectChanges();
                          }
                          resolve();
                      });
                      return;
                  }
                  page.c8o.log.error(
                      "[doSearchForm] empty search reset unavailable"
                  );
                  pagination.loading = false;
                  page.local.spinner = false;
                  page.local.loadingError = true;
                  resolve();
                  return;
              }
              const searchSignature = JSON.stringify({
                  target: page.local["ddoc"],
                  query: normalizedQuery,
                  tags: activeSearchTags.map((entry) => {
                      return entry != null && entry.value != undefined
                          ? entry.value
                          : entry;
                  }),
                  subTags: activeSearchSubTags.map((entry) => {
                      return entry != null && entry.value != undefined
                          ? entry.value
                          : entry;
                  }),
                  users: activeSearchUsers,
                  filters: filters
              });
              if (page.local["_searchSignature"] !== searchSignature) {
                  resetPaginationState(
                      pagination
                  );
                  page.local["_searchSignature"] =
                      searchSignature;
              }
          	pagination.loading = true;
          	
          	page.local.spinner = true;
          	page.local.loadingError = false;
          	page.local.alreadyFinished = false;
          	page.local.count = undefined;
          	page.local.showAdvancedSearch = false;
          	
          	/*
          	 * On conserve les anciens résultats pendant le chargement
          	 * afin d''éviter un flash vide.
          	 */
          	if (!Array.isArray(page["list"])) {
          		page["list"] = [];
          	}
          	
          	if (
          		page.ref != undefined &&
          		page.ref.detectChanges != undefined
          	) {
          		page.ref.detectChanges();
          	}
              function escapeRegexChar(c) {
                return ("" + c).replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&");
              }
              function buildAccentInsensitiveRegex(value) {
                var accentGroups = [
                  "aAàÀáÁâÂãÃäÄåÅāĀăĂąĄ",
                  "bB",
                  "cCçÇćĆčČ",
                  "dD",
                  "eEèÈéÉêÊëËēĒėĖęĘěĚ",
                  "fF",
                  "gG",
                  "hH",
                  "iIìÌíÍîÎïÏīĪįĮ",
                  "jJ",
                  "kK",
                  "lL",
                  "mM",
                  "nNñÑńŃ",
                  "oOòÒóÓôÔõÕöÖøØōŌœŒ",
                  "pP",
                  "qQ",
                  "rR",
                  "sS",
                  "tT",
                  "uUùÙúÚûÛüÜūŪ",
                  "vV",
                  "wW",
                  "xX",
                  "yYýÝÿŸ",
                  "zZ",
                  "æÆ"
                ];
                var charToClass = {};
                for (var groupIndex = 0; groupIndex < accentGroups.length; groupIndex++) {
                  var group = accentGroups[groupIndex];
                  var escapedGroup = "";
                  for (var charIndex = 0; charIndex < group.length; charIndex++) {
                    escapedGroup += escapeRegexChar(group.charAt(charIndex));
                  }
                  for (var classIndex = 0; classIndex < group.length; classIndex++) {
                    charToClass[group.charAt(classIndex)] = "[" + escapedGroup + "]";
                  }
                }
                var regex = "";
                var text = "" + value;
                for (var valueIndex = 0; valueIndex < text.length; valueIndex++) {
                  var c = text.charAt(valueIndex);
                  regex += charToClass[c] != undefined ? charToClass[c] : escapeRegexChar(c);
                }
                return regex;
              }
              function escapeHtml(value) {
                return ("" + value).replace(/[&<>"\u0027]/g, function(c) {
                  if (c == "&") {
                    return "&amp;";
                  }
                  if (c == "<") {
                    return "&lt;";
                  }
                  if (c == ">") {
                    return "&gt;";
                  }
                  if (c == "\"") {
                    return "&quot;";
                  }
                  return "&#39;";
                });
              }
              function buildHighlightedName(name, regex) {
                var text = "" + name;
                if (regex == null) {
                  return escapeHtml(text);
                }
                regex.lastIndex = 0;
                var highlighted = "";
                var lastIndex = 0;
                var match;
                while ((match = regex.exec(text)) != null) {
                  var start = match.index;
                  var matched = match[0];
                  highlighted += escapeHtml(text.substring(lastIndex, start));
                  highlighted += "<strong>" + escapeHtml(matched) + "</strong>";
                  lastIndex = start + matched.length;
                  if (matched.length == 0) {
                    regex.lastIndex++;
                  }
                }
                highlighted += escapeHtml(text.substring(lastIndex));
                return highlighted;
              }
              var selectedUsers = activeSearchUsers;
              const selectedCollaborationUser =
                  filters != null &&
                  filters.my_collaborations === true &&
                  selectedUsers.length === 1
                      ? selectedUsers[0]
                      : null;
          	const dynamicParams = {
          		filters:
          			filters,
          
          		query:
          			normalizedQuery,
          
          		tag:
          			page.local.searchTags,
          
          		subTag:
          			page.local.searchSubTags,
          
          		shared:
          			page.local.shared,
          
          		user:
          			selectedUsers,
                  collaborationUser:
                      selectedCollaborationUser,
          		pagination: {
          			pageSize:
          				pagination.pageSize,
          
          			pageIndex:
          				infiniteMode
          					? 0
          					: pagination
          						.pageIndex,
          
          			/*
          			 * Aucun bookmark n''est envoyé en mode infinite.
          			 */
          			pageToken:
          				infiniteMode
          					? null
          					: pagination
          						.currentPageToken
          		}
          	};
          //	page.c8o.callJsonObject("C8Oforms.APIV2_ExecuteView",{target: page.local["ddoc"] + "/search", dynamicParams: JSON.stringify({filters: filters, query: page["query"], tag: page.local.searchTags, subTag: page.local.searchSubTags, shared: page.local.shared, user: selectedUsers})})
          	page.c8o.callJsonObject(
          		"C8Oforms.APIV2_ExecuteView",
          		{
          			target:
          				page.local["ddoc"] +
          				"/search",
          	
          			dynamicParams:
          				JSON.stringify(
          					dynamicParams
          				)
          		}
          	)
          //	        .then((response: any, parameters: any)=>{
          //				console.log(response);
          //				if(page.local.totalCall == currentCall){
          //					page.local.count = response.res.docs.length;
          //		            var regex = page["query"] != undefined && page["query"] != "" ? new RegExp(buildAccentInsensitiveRegex(page["query"]), ''g'') : null;
          //					page["list"] = [...response.res.docs.map((x)=>{if(x.name != null && regex != null) {x.boldName = buildHighlightedName(x.name, regex);} return x})];
          //					page.local.alreadyFinished = true;
          //					page.local.spinner = false;
          //                  if (page.ref != undefined && page.ref.detectChanges != undefined) {
          //                    page.ref.detectChanges();
          //                  }
          //				}
          //	            
          //	            resolve();
          //	            return null;
          //	        })
          		.then((
          			response: any,
          			parameters: any
          		) => {
          			console.log(response);
          		
          			if (
          				page.local.totalCall !==
          				currentCall
          			) {
          				/*
          				 * Réponse d''une ancienne recherche :
          				 * on l''ignore intégralement.
          				 */
          				resolve();
          				return null;
          			}
          		
          			if (typeof page["setActiveSearchQuery"] === "function") {
          				page["setActiveSearchQuery"].call(
          					page,
          					normalizedQuery
          				);
          			}
          			else {
          				page.local.activeSearchQuery =
          					normalizedQuery;
          			}
          
          			const result =
          				response != null &&
          				response.res != null
          					? response.res
          					: {};
          		
          			const docs =
          				Array.isArray(result.docs)
          					? result.docs
          					: [];
          		
          			const responsePagination =
          				result.pagination != null &&
          				typeof result.pagination ===
          					"object"
          					? result.pagination
          					: {};
          		
          			const isPaginated =
          				responsePagination.paginated ===
          					true;
          		
          			const responseMode =
          				responsePagination.mode != null
          					? String(
          						responsePagination.mode
          					)
          					: null;
          		
          			const rawResponsePageSize =
          				responsePagination.pageSize;
          		
          			const isInfiniteResponse =
          				responseMode === "infinite" ||
          				(
          					typeof rawResponsePageSize ===
          						"string" &&
          					rawResponsePageSize
          						.toLowerCase()
          						.trim() === "infinite"
          				);
          		
          			pagination.loading = false;
          			pagination.paginated =
          				isPaginated;
          		
          			if (isInfiniteResponse) {
          				pagination.pageSize =
          					"infinite";
          			}
          			else {
          				const numericPageSize =
          					Number(
          						rawResponsePageSize
          					);
          		
          				if (
          					Number.isInteger(
          						numericPageSize
          					) &&
          					numericPageSize > 0
          				) {
          					pagination.pageSize =
          						Math.min(
          							numericPageSize,
          							100
          						);
          				}
          			}
          		
          if (isPaginated) {
              pagination.mode =
                  responsePagination.mode ?? null;
              pagination.hasPreviousPage =
                  responsePagination
                      .hasPreviousPage === true;
              pagination.previousPageToken =
                  responsePagination
                      .previousPageToken ?? null;
              pagination.hasNextPage =
                  responsePagination
                      .hasNextPage === true;
              pagination.nextPageToken =
                  responsePagination
                      .nextPageToken ?? null;
              pagination.lastPageToken =
                  responsePagination
                      .lastPageToken ?? null;
              const totalCount =
                  Number(
                      responsePagination.totalCount
                  );
              pagination.totalCount =
                  Number.isInteger(totalCount) &&
                  totalCount >= 0
                      ? totalCount
                      : docs.length;
              const pageCount =
                  Number(
                      responsePagination.pageCount
                  );
              pagination.pageCount =
                  Number.isInteger(pageCount) &&
                  pageCount > 0
                      ? pageCount
                      : (
                          pagination.totalCount === 0
                              ? 1
                              : Math.ceil(
                                  pagination.totalCount /
                                  Number(pagination.pageSize)
                              )
                      );
              const currentPageIndex =
                  Number(
                      responsePagination.currentPageIndex
                  );
              if (
                  Number.isInteger(currentPageIndex) &&
                  currentPageIndex >= 0
              ) {
                  pagination.pageIndex =
                      currentPageIndex;
              }
              else if (pagination.currentPageToken == null) {
                  pagination.pageIndex = 0;
              }
          
              const boundedPageCount =
                  Number.isInteger(Number(pagination.pageCount)) &&
                  Number(pagination.pageCount) > 0
                      ? Number(pagination.pageCount)
                      : 1;
          
              if (pagination.pageIndex >= boundedPageCount) {
                  pagination.pageIndex =
                      boundedPageCount - 1;
              }
          }
          else {
              /*
               * Mode infinite ou réponse legacy :
               * aucun bookmark ne doit rester actif.
               */
              pagination.mode = null;
              pagination.pageIndex = 0;
              pagination.currentPageToken = null;
              pagination.previousPageToken = null;
              pagination.nextPageToken = null;
              pagination.lastPageToken = null;
              pagination.pageTokenHistory = [];
              pagination.hasPreviousPage = false;
              pagination.hasNextPage = false;
              pagination.totalCount = null;
              pagination.pageCount = null;
          }
          			page.local.count =
          				docs.length;
          		
          			page.local.returnedCount =
          				Number.isFinite(
          					Number(
          						responsePagination
          							.returnedCount
          					)
          				)
          					? Number(
          						responsePagination
          							.returnedCount
          					)
          					: docs.length;
          		
          			const regex =
          				normalizedQuery != ""
          					? new RegExp(
          						buildAccentInsensitiveRegex(
          							normalizedQuery
          						),
          						"g"
          					)
          					: null;
          		
          			page["list"] =
          				docs.map((doc) => {
          					if (
          						doc != null &&
          						doc.name != null &&
          						regex != null
          					) {
          						doc.boldName =
          							buildHighlightedName(
          								doc.name,
          								regex
          							);
          					}
          		
          					return doc;
          				});
          		
          			page.local.alreadyFinished = true;
          			page.local.loadingError = false;
          			page.local.spinner = false;
          		
          if (
              page.ref != undefined &&
              page.ref.detectChanges != undefined
          ) {
              page.ref.detectChanges();
          }
          requestAnimationFrame(() => {
              const currentPage = page as any;
              if (
                  typeof currentPage.refreshViewportMetrics ===
                      "function"
              ) {
                  currentPage.refreshViewportMetrics(6);
              }
              else if (
                  typeof currentPage.viewport?.checkViewportSize ===
                      "function"
              ) {
                  currentPage.viewport.checkViewportSize();
              }
          });
          resolve();
          return null;
          		})
          //	        .fail((error)=>{
          //				console.error("e", error);
          //				page.c8o.log.error("[doSearchForm] an error occured "+ JSON.stringify(error));
          //              if(page.local.totalCall == currentCall){
          //                  page.local.count = 0;
          //                  page.local.alreadyFinished = true;
          //                  page.local.loadingError = true;
          //                  page.local.spinner = false;
          //                  if (page.ref != undefined && page.ref.detectChanges != undefined) {
          //                    page.ref.detectChanges();
          //                  }
          //              }
          //				resolve();
          //	        });
          		.fail((error) => {
          			console.error(
          				"doSearchForm error",
          				error
          			);
          		
          			page.c8o.log.error(
          				"[doSearchForm] an error occured " +
          				JSON.stringify(error)
          			);
          		
          			if (
          				page.local.totalCall ===
          				currentCall
          			) {
          				pagination.loading = false;
          		
          				page.local.count = 0;
          				page.local.alreadyFinished = true;
          				page.local.loadingError = true;
          				page.local.spinner = false;
          		
          				if (
          					page.ref != undefined &&
          					page.ref.detectChanges != undefined
          				) {
          					page.ref.detectChanges();
          				}
          			}
          		
          			resolve();
          		});
          '
  app_ts_imports: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
      - com.twinsoft.convertigo.beans.common.XMLVector: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ HttpClient }'
            - java.lang.String: 
              - ↑value: '@angular/common/http'
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ C8o }'
            - java.lang.String: 
              - ↑value: c8osdkangular
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ C8oSettings }'
            - java.lang.String: 
              - ↑value: c8osdkangular
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ C8oLogLevel }'
            - java.lang.String: 
              - ↑value: c8osdkangular
  ↓call [ngx.components.UIControlVariable-1747402401432]: 
    varValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: script:params.call
↓call [ngx.components.UIStackVariable-1747402391980]: 