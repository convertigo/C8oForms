comment: Process a tick (Refresh define in caf)
↓DoCallViewLiveAndFillFormList [ngx.components.UICustomAction-1567094239810]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '//			page.local.requestCounter = 0;
          //			if(page["pageOwner"] != undefined){
          //				page = page["pageOwner"];
          //			}
          
          			if (page["pageOwner"] != undefined) {
          				page = page["pageOwner"];
          			}
          			
          			if (
          				!Number.isFinite(
          					Number(page.local.requestCounter)
          				)
          			) {
          				page.local.requestCounter = 0;
          			}
          			
          			const createPaginationState = () => {
          				return {
          					pageIndex: 0,
          					pageSize: 25,
          
          					mode: null,
          
          					currentPageToken: null,
          					previousPageToken: null,
          					nextPageToken: null,
          					lastPageToken: null,
          
          					pageTokenHistory: [],
          
          					hasPreviousPage: false,
          					hasNextPage: false,
          
          					totalCount: null,
          					pageCount: null,
          
          					paginated: false,
          					loading: false
          				};
          			};
          
          			const getCurrentDdoc = (): string => {
          				if (
          					page.local["ddoc"] != undefined &&
          					page.local["ddoc"] != ""
          				) {
          					return page.local["ddoc"];
          				}
          
          				return page.local.published === true
          					? "published_formsV2"
          					: "formsV2";
          			};
          
          			const getPaginationTarget = ():
          				"edition" | "publication" => {
          				return getCurrentDdoc() ==
          					"published_formsV2"
          						? "publication"
          						: "edition";
          			};
          
          			const ensurePaginationState = () => {
          				if (
          					page["pagination"] == null ||
          					typeof page["pagination"] != "object"
          				) {
          					page["pagination"] = {};
          				}
          
          				const targets = [
          					"edition",
          					"publication"
          				];
          
          				for (
          					let targetIndex = 0;
          					targetIndex < targets.length;
          					targetIndex++
          				) {
          					const targetName =
          						targets[targetIndex];
          
          					if (
          						page["pagination"][targetName] == null ||
          						typeof page["pagination"][targetName]
          							!= "object"
          					) {
          						page["pagination"][targetName] =
          							createPaginationState();
          
          						continue;
          					}
          
          					const defaults =
          						createPaginationState();
          
          					for (const key of Object.keys(defaults)) {
          						if (
          							page["pagination"][targetName][key]
          								=== undefined
          						) {
          							page["pagination"][targetName][key] =
          								defaults[key];
          						}
          					}
          
          					if (
          						!Array.isArray(
          							page["pagination"][targetName]
          								.pageTokenHistory
          						)
          					) {
          						page["pagination"][targetName]
          							.pageTokenHistory = [];
          					}
          				}
          
          				const target =
          					getPaginationTarget();
          				console.log(target, page.local.tabSelected)
          				const pagination =
          					page["pagination"][target];
          
          					const rawPageSize =
          						pagination.pageSize;
          
          					const isInfinitePageSize =
          						typeof rawPageSize === "string" &&
          						rawPageSize
          							.toLowerCase()
          							.trim() === "infinite";
          
          					if (isInfinitePageSize) {
          						pagination.pageSize =
          							"infinite";
          					}
          					else {
          						const parsedPageSize =
          							Number(rawPageSize);
          
          						pagination.pageSize =
          							Number.isInteger(parsedPageSize) &&
          							parsedPageSize > 0
          								? Math.min(
          									parsedPageSize,
          									500
          								)
          								: 25;
          					}
          
          				return {
          					target,
          					pagination
          				};
          			};
          
          			const resetPaginationState = (
          				pagination
          			): void => {
          				const rawPageSize =
          					pagination.pageSize;
          
          				const isInfinitePageSize =
          					typeof rawPageSize === "string" &&
          					rawPageSize
          						.toLowerCase()
          						.trim() === "infinite";
          
          				let normalizedPageSize:
          					number | "infinite";
          
          				if (isInfinitePageSize) {
          					normalizedPageSize =
          						"infinite";
          				}
          				else {
          					const numericPageSize =
          						Number(rawPageSize);
          
          					normalizedPageSize =
          						Number.isInteger(
          							numericPageSize
          						) &&
          						numericPageSize > 0
          							? Math.min(
          								numericPageSize,
          								500
          							)
          							: 25;
          				}
          
          				pagination.pageIndex = 0;
          				pagination.pageSize =
          					normalizedPageSize;
          
          				pagination.mode = null;
          
          				pagination.currentPageToken = null;
          				pagination.previousPageToken = null;
          				pagination.nextPageToken = null;
          				pagination.lastPageToken = null;
          
          				pagination.pageTokenHistory = [];
          
          				pagination.hasPreviousPage = false;
          				pagination.hasNextPage = false;
          
          				pagination.totalCount = null;
          				pagination.pageCount = null;
          
          				pagination.paginated = false;
          				pagination.loading = false;
          			};
          			const stringifyActionError = (error) => {
          				try{
          					if(typeof error === ''string''){
          						return error;
          					}
          					if(error?.message != undefined){
          						return String(error.message);
          					}
          					return JSON.stringify(error);
          				}
          				catch(e){
          					return String(error);
          				}
          			};
          //			const handleExecuteViewError = (scope, error) => {
          //				page.local.alreadyFinished = true;
          //				page.local.loadingError = true;
          //				page.local.spinner = false;
          //				page.local.count = 0;
          //				page.ref.detectChanges();
          //				page.c8o.log.error("[DoCallViewLiveAndFillFormList] " + scope + " failed " + stringifyActionError(error));
          //				return null;
          //			};
          			const handleExecuteViewError = (
          				scope,
          				error,
          				paginationState = null
          			) => {
          				if (paginationState != null) {
          					paginationState.loading = false;
          				}
          			
          				page.local.alreadyFinished = true;
          				page.local.loadingError = true;
          				page.local.spinner = false;
          				page.local.count = 0;
          			
          				page.ref.detectChanges();
          			
          				page.c8o.log.error(
          					"[DoCallViewLiveAndFillFormList] " +
          					scope +
          					" failed " +
          					stringifyActionError(error)
          				);
          			
          				return null;
          			};
          			const logLiveChangesError = (scope, error) => {
          				page.local.live = null;
          				page.c8o.log.error("[DoCallViewLiveAndFillFormList] live changes " + scope + " error (non blocking) " + stringifyActionError(error));
          			};
          			const readEditionFilters = () => {
          				if(page["getEditionFilters"] != undefined){
          					return page["getEditionFilters"]();
          				}
          				let filters;
          				let local_str_data = window.localStorage.getItem("c8oforms_edition_filters");
          				try{
          					filters = JSON.parse(local_str_data);
          				}
          				catch(e){
          					console.error(e);
          				}
          				return filters == null ? {} : filters;
          			};
          			// Rows of a request issued before a delete made from this client answered may still
          			// hold the deleted forms (recorded by formListChanged in selectorPage): drop them, so a
          			// late response never brings a deleted card back. At worst this hides forms that are
          			// gone anyway; requests issued after the delete are left untouched.
          			const withoutRemovedForms = (docs, requestId) => {
          				const removed = page.local.removedFormIds;
          				if (removed == null || !Array.isArray(docs) || Number(requestId) > removed.upTo) {
          					return docs;
          				}
          				return docs.filter((doc) => doc == null || removed.ids.indexOf(doc._id) < 0);
          			};
          			const applyExecuteViewResponse = (
          				response,
          				currentRequestId,
          				pagination,
          				contextKey
          			) => {
          				if (
          					currentRequestId !==
          					page.local.requestCounter
          				) {
          					/*
          					 * Réponse d''une ancienne requête :
          					 * on ne modifie ni la liste ni la pagination.
          					 */
          					return null;
          				}
          
          				const result =
          					response != null &&
          					response.res != null
          						? response.res
          						: {};
          
          				const docs =
          					Array.isArray(result.docs)
          						? result.docs
          						: [];
          
          				const responsePagination =
          					result.pagination != null &&
          					typeof result.pagination == "object"
          						? result.pagination
          						: {};
          
          				const isPaginated =
          					responsePagination.paginated === true;
          
          				const responseMode =
          					responsePagination.mode != null
          						? String(
          							responsePagination.mode
          						)
          						: null;
          
          				const rawResponsePageSize =
          					responsePagination.pageSize;
          
          				const isInfiniteResponse =
          					responseMode === "infinite" ||
          					(
          						typeof rawResponsePageSize ===
          							"string" &&
          						rawResponsePageSize
          							.toLowerCase()
          							.trim() === "infinite"
          					);
          
          				pagination.loading = false;
          				pagination.paginated =
          					isPaginated;
          
          				/*
          				 * Synchronisation de la taille de page.
          				 */
          				if (isInfiniteResponse) {
          					pagination.pageSize =
          						"infinite";
          				}
          				else {
          					const numericResponsePageSize =
          						Number(rawResponsePageSize);
          
          					if (
          						Number.isInteger(
          							numericResponsePageSize
          						) &&
          						numericResponsePageSize > 0
          					) {
          						pagination.pageSize =
          							Math.min(
          								numericResponsePageSize,
          								500
          							);
          					}
          				}
          
          				if (isPaginated) {
          					pagination.mode =
          						responsePagination.mode
          							?? null;
          
          					pagination.hasPreviousPage =
          						responsePagination.hasPreviousPage === true;
          
          					pagination.previousPageToken =
          						responsePagination.previousPageToken
          							?? null;
          
          					pagination.hasNextPage =
          						responsePagination.hasNextPage === true;
          
          					pagination.nextPageToken =
          						responsePagination.nextPageToken
          							?? null;
          
          					pagination.lastPageToken =
          						responsePagination.lastPageToken
          							?? null;
          
          					const totalCount =
          						Number(
          							responsePagination.totalCount
          						);
          
          					pagination.totalCount =
          						Number.isInteger(totalCount) &&
          						totalCount >= 0
          							? totalCount
          							: null;
          
          					const pageCount =
          						Number(
          							responsePagination.pageCount
          						);
          
          					pagination.pageCount =
          						Number.isInteger(pageCount) &&
          						pageCount >= 0
          							? pageCount
          							: null;
          
          					/*
          					 * Pour out_folder, le backend connaît l''index exact
          					 * de la page. On l''utilise comme source de vérité.
          					 */
          					const currentPageIndex =
          						Number(
          							responsePagination.currentPageIndex
          						);
          
          					if (
          						pagination.mode ===
          							"out_folder_offset" &&
          						Number.isInteger(
          							currentPageIndex
          						) &&
          						currentPageIndex >= 0
          					) {
          						pagination.pageIndex =
          							currentPageIndex;
          					}
          
          					const numericPageSize =
          						Number(
          							responsePagination.pageSize
          						);
          
          					if (
          						Number.isInteger(
          							numericPageSize
          						) &&
          						numericPageSize > 0
          					) {
          						pagination.pageSize =
          							numericPageSize;
          					}
          				}
          				else {
          					pagination.mode = null;
          
          					pagination.pageIndex = 0;
          
          					pagination.currentPageToken = null;
          					pagination.previousPageToken = null;
          					pagination.nextPageToken = null;
          					pagination.lastPageToken = null;
          
          					pagination.pageTokenHistory = [];
          
          					pagination.hasPreviousPage = false;
          					pagination.hasNextPage = false;
          
          					pagination.totalCount = null;
          					pagination.pageCount = null;
          				}
          				
          //				console.log(pagination)
          
          				page.local.count = docs.length;
          
          				page.local.returnedCount =
          					Number.isFinite(
          						Number(
          							responsePagination.returnedCount
          						)
          					)
          						? Number(
          							responsePagination.returnedCount
          						)
          						: docs.length;
          
          				page.local.alreadyFinished = true;
          				page.local.loadingError = false;
          
          				page.ref.detectChanges();
          
          setTimeout(() => {
              // Filtered here, when applied, not above: a delete may have answered since.
              page["list"] = withoutRemovedForms(docs, currentRequestId);
              // Stamped with the rows it describes, and cleared in the same change
              // detection pass: clearing the spinner earlier let the empty state flash
              // over the previous list on every refresh that went from empty to filled.
              page.local.listContextKey = contextKey;
              page.local.spinner = false;
              page.local.refreshing = false;
              page.local.done = true;
              page.ref.detectChanges();
              requestAnimationFrame(() => {
                  const currentPage = page as any;
                  if (
                      typeof currentPage.refreshViewportMetrics ===
                          "function"
                  ) {
                      currentPage.refreshViewportMetrics(6);
                  }
                  else if (
                      typeof currentPage.viewport?.checkViewportSize ===
                          "function"
                  ) {
                      currentPage.viewport.checkViewportSize();
                  }
              });
          });
          
          				return null;
          			};
          			return new Promise(async (resolveAsync) => {
          				try{
          				page.local.count = 0;
          				let countFolders = 0;
          				let countOut = 0;
          				let view = page.local["view"] != undefined ? page.local["view"] : "formulaires";
          				let ddoc = page.local["ddoc"] != undefined ? page.local["ddoc"] : (page.local.published ? "published_formsV2" : "formsV2");
          				page["local"].response2Array = [];
          				page["local"].response2ArrayForms = []
          //				page["list"] = [];
          				if (!Array.isArray(page["list"])) {
          					page["list"] = [];
          				}
          				page.local.spinner = true;
          				page.local.hash = undefined;
          				page.local.hashFolders = undefined;
          				page.local.count = undefined;
          				const normalizeActiveSearchQuery = (value) => {
          						return value == null
          							? ""
          							: ("" + value).trim();
          					};
          					const getActiveSearchQuery = () => {
          						return typeof page["getActiveSearchQuery"] === "function"
          							? page["getActiveSearchQuery"].call(page)
          							: normalizeActiveSearchQuery(page["query"]);
          					};
          					const hasActiveSearch = () => {
                              const normalizedQuery =
                                  getActiveSearchQuery();
                              return normalizedQuery != ""
                                  || (Array.isArray(page.local.searchTags) && page.local.searchTags.length > 0)
                                  || (Array.isArray(page.local.searchSubTags) && page.local.searchSubTags.length > 0)
                                  || (Array.isArray(page.local._searchUser) && page.local._searchUser.length > 0);
          					};
          //				let execQuery = () => {
          //					page.local.requestCounter++;
          //					let currentRequestId = page.local.requestCounter;
          //					if (hasActiveSearch()) {
          //						const searchStack = props && props.stack ? props.stack : {root: {scope: {}, in: {}, out: event}};
          //						const searchParent = props ? props.parent : null;
          //						this.STS1605199050514(page, {stack: searchStack, parent: searchParent, out: event}, Object.assign({}, searchStack["root"].in, {call: currentRequestId}), event)
          //							.catch((error) => {
          //								return handleExecuteViewError("searchForm live refresh", error);
          //							});
          //						return;
          //					}
          //					// if we are inside a folder
          //					if (page.local.folder != "") {
          //						let filters = readEditionFilters();
          //						page.local.filters = filters;
          //						page.c8o.callJsonObject("C8Oforms.APIV2_ExecuteView", { target: page.local["ddoc"] + "/in_folders", "acl": true, dynamicParams: JSON.stringify({ folder: page.local.folder, filters: filters }) })
          //							.then((response: any, parameters: any) => {
          //								console.log(response);
          //								if (currentRequestId !== page.local.requestCounter) {
          //							        // This answer comes from an obsolete query; we ignore it
          //							    }
          //								else{
          //									page.local.count = response.res.docs.length;
          //									page.local.alreadyFinished = true;
          //									page.local.loadingError = false;
          //									page.local.spinner = false;
          //									page.ref.detectChanges();
          //									setTimeout(() => {
          //										page["list"] = response.res.docs;
          //										if (page.local.done) {
          //											page["viewport"].setRenderedRange({ start: 0, end: (page["viewport"].getRenderedRange()).end + 1 });
          //										}
          //										else {
          //											page.local.done = true;
          //										}
          //										page.ref.detectChanges();
          //									});
          //									
          //								}
          //								return null;
          //								
          //							})
          //							.fail((error) => {
          //								if (currentRequestId !== page.local.requestCounter) {
          //									return null;
          //								}
          //								return handleExecuteViewError("APIV2_ExecuteView in_folders", error);
          //							})
          //					}
          //					// if we are not into a folder
          //					else {
          //						// view for documents inside of folder
          //						let filters = readEditionFilters();
          //						page.local.filters = filters;
          //						page.c8o.callJsonObject("C8Oforms.APIV2_ExecuteView", { target: page.local["ddoc"] + "/out_folder", "acl": true, dynamicParams: JSON.stringify({ folder: page.local.folder, filters: filters }) })
          //							.then((response: any, parameters: any) => {
          //								console.log(response);
          //								if (currentRequestId !== page.local.requestCounter) {
          //							        // This answer comes from an obsolete query; we ignore it
          //							    }
          //								else{
          //									page.local.count = response.res.docs.length;
          //									page.local.alreadyFinished = true;
          //									page.local.loadingError = false;
          //									page.local.spinner = false;
          //									page.ref.detectChanges();
          //									setTimeout(() => {
          //										page["list"] = response.res.docs;
          //										if (page.local.done) {
          //											page["viewport"].setRenderedRange({ start: 0, end: (page["viewport"].getRenderedRange()).end + 1 });
          //										}
          //										else {
          //											page.local.done = true;
          //										}
          //										page.ref.detectChanges();
          //									});
          //								}
          //								return null;
          //							})
          //							.fail((error) => {
          //								if (currentRequestId !== page.local.requestCounter) {
          //									return null;
          //								}
          //								return handleExecuteViewError("APIV2_ExecuteView out_folder", error);
          //							})
          //					}
          //				}
          				// --- Live refresh coalescing ------------------------------------------------
          				// The changes feed is the shared c8oforms_fs feed: every save the user can see
          				// raises an event. Calling execQuery() per event bumps requestCounter, which
          				// invalidates the response in flight, then starts another request. When another
          				// tab of the same account autosaves every ~2s while the server answers in
          				// 15-30s, no response is ever applied and the list stays on its skeletons.
          				// Rule: at most one live refresh in flight, at most one queued behind it.
          				const LIVE_REFRESH_DEBOUNCE_MS = 250;
          				// A C8oPromise is not guaranteed to settle. Without a deadline a hung request
          				// would freeze every later live refresh, which is worse than the defect fixed
          				// here, so the gate reopens on its own after this delay.
          				const LIVE_REFRESH_MAX_WAIT_MS = 45000;
          
          				const liveRefreshState = () => {
          					if (page.local.liveRefresh == null) {
          						page.local.liveRefresh = {timer: null, expiryTimer: null, queued: false, reset: false};
          					}
          					return page.local.liveRefresh;
          				};
          
          				// Called wherever the feed is cancelled: the timer belongs to this page
          				// instance and to this invocation, and must never outlive either.
          				const clearLiveRefreshTimer = () => {
          					const state = liveRefreshState();
          					if (state.timer != null) {
          						clearTimeout(state.timer);
          					}
          					if (state.expiryTimer != null) {
          						clearTimeout(state.expiryTimer);
          					}
          					state.timer = null;
          					state.expiryTimer = null;
          					state.queued = false;
          					state.reset = false;
          				};
          
          				const requestIsInFlight = () => {
          					if (page.local.inFlightRequestId == null) {
          						return false;
          					}
          					if (Date.now() - Number(page.local.inFlightSince || 0) > LIVE_REFRESH_MAX_WAIT_MS) {
          						page.local.inFlightRequestId = null;
          						return false;
          					}
          					return true;
          				};
          
          				const markRequestStarted = (requestId) => {
          					page.local.inFlightRequestId = requestId;
          					page.local.inFlightSince = Date.now();
          				};
          
          				// Keyed on the request id, never on a boolean: requestCounter is also advanced
          				// from searchApp/searchForm, so a request can be superseded without ever owning
          				// the gate again. Only the request that took the gate may release it.
          				const markRequestFinished = (requestId) => {
          					if (page.local.inFlightRequestId !== requestId) {
          						return;
          					}
          					page.local.inFlightRequestId = null;
          					const state = liveRefreshState();
          					if (state.queued) {
          						state.queued = false;
          						if (state.expiryTimer != null) {
          							clearTimeout(state.expiryTimer);
          							state.expiryTimer = null;
          						}
          						scheduleLiveRefresh(state.reset);
          					}
          				};
          
          				const scheduleLiveRefresh = (reset) => {
          					const state = liveRefreshState();
          					if (reset === true) {
          						state.reset = true;
          					}
          					if (state.timer != null) {
          						return;
          					}
          					state.timer = setTimeout(() => {
          						state.timer = null;
          						// The page may have been left or re-rooted meanwhile: never issue a query
          						// nor a detectChanges() against a destroyed view.
          						if (page.local.live == null || page["closing"] === true) {
          							return;
          						}
          						if (requestIsInFlight()) {
          							state.queued = true;
          							// Replayed by markRequestFinished. The request in flight may never settle and
          							// the deadline is only read when a refresh is attempted: attempt one just
          							// after it, or a local change would wait for an unrelated feed event.
          							if (state.expiryTimer == null) {
          								const waited = Date.now() - Number(page.local.inFlightSince || 0);
          								state.expiryTimer = setTimeout(() => {
          									state.expiryTimer = null;
          									if (state.queued) {
          										state.queued = false;
          										scheduleLiveRefresh(state.reset);
          									}
          								}, Math.max(LIVE_REFRESH_MAX_WAIT_MS - waited, 0) + LIVE_REFRESH_DEBOUNCE_MS);
          							}
          							return;
          						}
          						const resetPagination = state.reset;
          						state.reset = false;
          						// No forced resetPagination: a live refresh re-issues the page the user is
          						// reading instead of sending them back to page 1 behind their back.
          						execQuery({resetPagination: resetPagination, live: true});
          					}, LIVE_REFRESH_DEBOUNCE_MS);
          				};
          
          				// Identity of the view on screen. Serialised with sorted keys so that two
          				// equivalent objects always produce the same string.
          				const listContextKey = (ddoc, folder, filters) => JSON.stringify(
          					{ddoc: ddoc, folder: folder, filters: filters},
          					(key, value) => (value != null && typeof value === "object" && !Array.isArray(value))
          						? Object.keys(value).sort().reduce((sorted, name) => { sorted[name] = value[name]; return sorted; }, {})
          						: value
          				);
          
          				let execQuery = (
          					options = {}
          				) => {
          					page.local.requestCounter =
          						Number(page.local.requestCounter) + 1;
          				
          					const currentRequestId =
          						page.local.requestCounter;
          
          					// Taken before the search delegation below, so the search path is inside
          					// the gate too: it is the one that used to restart on every change event.
          					markRequestStarted(currentRequestId);
          						
          					const paginationContext =
          						ensurePaginationState();
          
          					const pagination =
          						paginationContext.pagination;
          
          					if (
          						options["resetPagination"] === true
          					) {
          						resetPaginationState(
          							pagination
          						);
          					}
          
          					/*
          					 * À calculer après resetPaginationState afin de prendre
          					 * en compte la valeur finale de pageSize.
          					 */
          					const infiniteMode =
          						pagination.pageSize ===
          							"infinite";
          				
          				
          					if (
          						options["resetPagination"] === true
          					) {
          						resetPaginationState(
          							pagination
          						);
          					}
          				
          					/*
          					 * La recherche utilise une autre shared action.
          					 * Sa migration est traitée séparément.
          					 */
          					if (hasActiveSearch()) {
          						const searchStack =
          							props && props.stack
          								? props.stack
          								: {
          									root: {
          										scope: {},
          										in: {},
          										out: event
          									}
          								};
          				
          						const searchParent =
          							props
          								? props.parent
          								: null;
          				
          						this.STS1605199050514(
          							page,
          							{
          								stack: searchStack,
          								parent: searchParent,
          								out: event
          							},
          							Object.assign(
          								{},
          								searchStack["root"].in,
          								{
          									call:
          										currentRequestId,
          				
          									activeSearchRefresh:
          										true,
          				
          									resetPagination:
          										options[
          											"resetPagination"
          										] === true
          								}
          							),
          							event
          						)
          						.catch((error) => {
          							return handleExecuteViewError(
          								"searchForm live refresh",
          								error,
          								pagination
          							);
          						})
          						.then(
          							() => markRequestFinished(currentRequestId),
          							() => markRequestFinished(currentRequestId)
          						);
          				
          						return;
          					}
          				
          					const currentDdoc =
          						getCurrentDdoc();
          				
          					const folder =
          						page.local.folder != null
          							? page.local.folder
          							: "";
          				
          					const targetSuffix =
          						folder != ""
          							? "in_folders"
          							: "out_folder";
          				
          					const executeViewTarget =
          						currentDdoc +
          						"/" +
          						targetSuffix;
          				
          					const filters =
          						readEditionFilters();
          				
          					page.local.filters = filters;
          				
          					const dynamicParams = {
          						folder,
          						filters,
          
          						pagination: {
          							/*
          							 * Peut être un nombre ou la chaîne "infinite".
          							 */
          							pageSize:
          								pagination.pageSize,
          
          							/*
          							 * Un bookmark n''a aucun sens lorsque la
          							 * pagination serveur est désactivée.
          							 */
          							pageToken:
          								infiniteMode
          									? null
          									: pagination
          										.currentPageToken
          						}
          					};
          				
          					const contextKey = listContextKey(currentDdoc, folder, filters);
          
          					/*
          					 * Skeletons are skipped only for a live refresh of the SAME view as the one
          					 * on screen. The key is stamped when the rows are applied, not when the
          					 * request starts, so it describes what is actually displayed: after a failed
          					 * or superseded query it still names the previous view and the skeletons come
          					 * back, instead of leaving another view rows visible with no loading cue.
          					 */
          					const silentRefresh =
          						options["live"] === true &&
          						page.local.listContextKey === contextKey &&
          						page["list"] != null &&
          						page["list"].length > 0;
          
          					// Drives the thin progress cue above the results: the only signal that the
          					// list is updating when the skeletons are skipped.
          					page.local.refreshing = silentRefresh;
          					page.local.refreshingRequestId = currentRequestId;
          
          					pagination.loading = true;
          					page.local.spinner = !silentRefresh;
          					page.local.loadingError = false;
          				
          					page.ref.detectChanges();
          				
          					return page.c8o.callJsonObject(
          						"C8Oforms.APIV2_ExecuteView",
          						{
          							target: executeViewTarget,
          							acl: true,
          							dynamicParams:
          								JSON.stringify(
          									dynamicParams
          								)
          						}
          					)
          					.then((
          						response: any,
          						parameters: any
          					) => {
          						markRequestFinished(currentRequestId);
          
          						return applyExecuteViewResponse(
          							response,
          							currentRequestId,
          							pagination,
          							contextKey
          						);
          					})
          					.fail((error) => {
          						markRequestFinished(currentRequestId);
          						// Only the request that turned the cue on may turn it off: a superseded request
          						// failing late must not hide the cue of the one now running.
          						if (page.local.refreshingRequestId === currentRequestId) {
          							page.local.refreshing = false;
          						}
          
          						if (
          							currentRequestId !==
          							page.local.requestCounter
          						) {
          							return null;
          						}
          				
          						return handleExecuteViewError(
          							"APIV2_ExecuteView " +
          								targetSuffix,
          							error,
          							pagination
          						);
          					});
          				};
                          execQuery({
                              resetPagination:
                                  vars != null &&
                                  vars["resetPagination"] === true
                          });
          				// Entry point for the actions that change the list from this client (delete,
          				// duplicate, import): showing their result must not depend on the changes feed.
          				// Same debounced, gated and silent path as a live change, so a local action and
          				// the feed event it also raises end up in a single request. Reassigned by every
          				// invocation, like the feed itself, so it always drives the current query.
          				// reveal: a form was added (duplicate, import). Ids are creation timestamps and the
          				// edition list sorts on them, newest first, so that list goes back to page 1 where
          				// the new form is; any other list keeps the page being read.
          				page.local.requestListRefresh = (options: any = {}) => scheduleLiveRefresh(
          					options.resetPagination === true ||
          					(options.reveal === true && getCurrentDdoc() == "formsV2")
          				);
          				// live changes
          				let p = Pouch["default"];
          				const header = {
          					"x-convertigo-sdk": page.c8o.sdkVersion,
          				};
          				if (page.c8o.xsrfUsed) {
          					const headerName = "x-xsrf-token";
          					const fetch = "Fetch";
          					header[headerName] = window.localStorage.getItem(headerName) != undefined ? window.localStorage.getItem(headerName) : fetch;
          				}
          				Object.assign(header, page.c8o.headers);
          				let remotePouchHeader = {
          					fetch: (url, opts) => {
          						opts.credentials = ''include'';
          						for (let key in header) {
          							opts.headers.set(key, header[key]);
          						}
          						var fecthPouch: Promise<Response> = p.fetch(url, opts);
          						fecthPouch.then((response) => {
          							page.c8o.session.sort(response, header, null, null, null);
          						})
          							.catch((e) => {
          								console.error(e);
          							});
          						return fecthPouch;
          					}
          				};
          				let isValidTimestamp = (value)=> {
          					try{
          						const timestamp = parseInt(value, 10); // Convertir la valeur en nombre entier en base 10
          					    const date = new Date(timestamp);
          					    // Vérifier si le timestamp est un nombre, si la date est valide et si le timestamp est positif
          					    return !isNaN(timestamp) && !isNaN(date.getTime()) && timestamp >= 0;
          					}
          					catch(e){
          						return false;
          					}
          				    
          				}
          				let isValidPublishTimestamp = (value)=> {
          					try{
          						let arg1 = value.substring(0,10);
          						if(arg1 != "published_"){
          							return false;
          						}
          						value = value.substring(10);
          						const timestamp = parseInt(value, 10); // Convertir la valeur en nombre entier en base 10
          					    const date = new Date(timestamp);
          					    // Vérifier si le timestamp est un nombre, si la date est valide et si le timestamp est positif
          					    return !isNaN(timestamp) && !isNaN(date.getTime()) && timestamp >= 0;
          					}
          					catch(e){
          						return false;
          					}
          				    
          				}
          				
          				console.log(p);
          				
          				try{
          					let dbChanges = new p(page.c8o.fullSyncEndpoint+"/c8oforms_fs", remotePouchHeader);
          					console.log(dbChanges);
          					// This shared action re-subscribes on every user action, so a timer armed by
          					// the previous invocation would fire inside this one with a stale closure.
          					clearLiveRefreshTimer();
          					if(page.local.live && page.local.live.cancel != undefined){
          						try{
          							await page.local.live.cancel();
          						}
          						catch(cancelError){
          							logLiveChangesError("cancel", cancelError);
          						}
          					}
          					page.local.live = dbChanges.changes({
          						live: true, since: ''now''
          					})
          //					.on(''change'', function (change) {
          //						console.log(change);
          //						// handle change
          //						if(page.local["ddoc"] == "formsV2" && (isValidTimestamp(change.id) || change.id.indexOf("folder_") == 0)){
          //							execQuery();
          //						}
          //						else if(page.local["ddoc"] == "published_formsV2" && (isValidPublishTimestamp(change.id)  || change.id.indexOf("folder_") == 0)){
          //							execQuery();
          //						}
          //					}).on(''complete'', function (info) {
          //						console.log(info);
          //					}).on(''error'', function (err) {
          //						logLiveChangesError("stream", err);
          //					});
          					.on("change", function(change) {
          						console.log(change);
          					
          						if (
          							getCurrentDdoc() ==
          								"formsV2" &&
          							(
          								isValidTimestamp(change.id) ||
          								change.id.indexOf("folder_") == 0
          							)
          						) {
          							scheduleLiveRefresh(false);
          						}
          						else if (
          							getCurrentDdoc() ==
          								"published_formsV2" &&
          							(
          								isValidPublishTimestamp(change.id) ||
          								change.id.indexOf("folder_") == 0
          							)
          						) {
          							scheduleLiveRefresh(false);
          						}
          					})
          				}
          				catch(liveError){
          					logLiveChangesError("init", liveError);
          				}
          				// end lives changes
          				}
          				catch(error){
          					handleExecuteViewError("setup", error);
          				}
          				finally{
          					resolveAsync();
          					resolve();
          				}
          			});
          '
  app_ts_imports: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
      - com.twinsoft.convertigo.beans.common.XMLVector: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ sha256 }'
            - java.lang.String: 
              - ↑value: js-sha256
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '* as Pouch'
            - java.lang.String: 
              - ↑value: pouchdb-browser
  package_dependencies: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
      - com.twinsoft.convertigo.beans.common.XMLVector: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: js-sha256
            - java.lang.String: 
              - ↑value: 0.9.0
↓keys [ngx.components.UIStackVariable-1567094333170]: 