↓CustomAction [mobile.components.UICustomAction-1604330914949]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          		    let cpt  = 0;
          		    let checkhasBeenProcessed = async () => {
              		    try{
              		        cpt ++;
              		        page.c8o.log.debug("[check pwa has build] n°"+cpt+ " started");
              		        let viewPwa = await page.c8o.callJson("fs://c8oforms_fs.view", "ddoc","pwas", "view", "toBeDone").async();
              		        if(viewPwa.rows.length > 0){
              		            page.c8o.log.debug("[check pwa has build] n°"+cpt+ " "+viewPwa.rows.length+" build(s) have not started");
              		            for(let elem of viewPwa.rows){
              		                let pingIt = await page.c8o.callJson("fs://c8oforms_fs.post", "_id", elem.id, "ping", "pong", "_use_policy", "merge").async();
              		                page.c8o.log.debug("[check pwa has build] n°"+cpt+ " try to restart build " + elem.id);
              		            }
              		            page.c8o.log.debug("[check pwa has build] n°"+cpt+ " we will wait 10000 ms before checking if every build had started");
              		            if(cpt < 6){
              		                setTimeout(() => {
              	                        checkhasBeenProcessed();
              	                    }, 10000)
              		            }
              		        }
              		        else{
              		            page.c8o.log.debug("[check pwa has build] n°"+cpt+ " all build seems to have started correctly");
              		        }
              		       
              		    }
              		    catch(e){
              		        page.c8o.log.debug("[check pwa has build] has encontred an error", e);
              		    }
              		    
              		}
          		    page.c8o.log.debug("[check pwa has build] will wait 5000 ms before checking that pwa build has started");
              		setTimeout(() => {
                          checkhasBeenProcessed();
                      }, 5000);
              		resolve();
          