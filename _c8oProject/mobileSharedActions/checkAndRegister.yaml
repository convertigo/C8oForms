↓CheckAndRegisterV2 [ngx.components.UICustomAsyncAction-1749655468133]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '		try {
          			let parentPage;
          			let cancel = event.srcElement.value == "";
          			let cause = cancel ? "empty_name_notAllowed": "nameAlreadyexits";
          			if(page["pageOwner"]){
          				parentPage = page["pageOwner"];
          			}
          			else{
          				parentPage = page;
          			}
          			//func
          			const getTranslateAndToast = (label)=>{
          				parentPage.showToast(parentPage.translate.instant(label), {
          					position: "bottom",
          					cssClass: "toastError",
          					"duration": 10000,
          					"showCloseButton": true,
          					"closeButtonText": parentPage.translate.instant("close")
          				});
          			}
          			
          			if(!cancel){
          			    for(let item of parentPage["formsList"]){
          			        if(item["name"] == event.srcElement.value){
          						// technical ID lust be unique
          			            cancel = true;
          			            break;
          			        }
          			        else if(item["type"] == "ion-card" && item["children"] != undefined){
          			            for(let child of item["children"]){
          			                if(child["name"] == event.srcElement.value){
          								// technical ID lust be unique
          								cancel = true;
          			                    break;
          			                }
          			            }
          			        }
          			    }
          			}
          			if(!cancel){
          				for(let flow of parentPage.form.flows){
          					for(let item of flow.elements){
          			        	if(item["name"] == event.srcElement.value){
          							cancel = true;
          							break;
          				        }
          					}
          			    }
          			}
          			
          			if(!cancel){
          				const regex = /^[a-zA-Z0-9_$][a-zA-Z0-9_$ -]*$/;
          				if(!regex.test(event.srcElement.value)){
          					cancel = true;
          					cause = "nameInvalid";
          				}
          			}
          			if(cancel){
          				// technical ID lust be unique
          				getTranslateAndToast(cause);
          				let originalName = vars.item.name;
          	            vars.item.name = vars.item.name + " ";
          	            page.tick();
          	            vars.item.name = originalName;
          	            page.tick();
          			}
          			else{
          				vars.item.name = event.srcElement.value;
          				parentPage.updateState();
          			}
          			
          		} catch (e) {
          		
          		} finally {
          			return;
          		}
          '
  ↓item [ngx.components.UIControlVariable-1749656032525]: 
    varValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: script:params.item
↓item [ngx.components.UIStackVariable-1749656024009]: 