↓entry [ngx.components.UIStackVariable-1790182000501]: 
  comment: Version to download, as listed by APIV2_ListApplicationHistory.
↓Export [ngx.components.UICustomAsyncAction-1790182000503]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '		// Same download mechanism as the application export (popOverExport).
          		try {
          			const form = page["navParams"]?.data?.form;
          			const link = document.createElement("a");
          			link.href = `${page.c8o.endpoint}/.bin?__sequence=APIV2_ExportApplicationVersion&id=${encodeURIComponent(form._id)}&entryId=${encodeURIComponent(vars.entry.id)}`;
          			link.download = `${form.name || form._id}.c8oforms`;
          			link.rel = "noopener";
          			link.style.display = "none";
          			document.body.appendChild(link);
          			link.click();
          			link.remove();
          		}
          		catch (e) {
          			page.c8o.log.error("[history] unable to export a version", e);
          		}
          '
  comment: Downloads the version as a .c8oforms file through APIV2_ExportApplicationVersion.
  ↓entry [ngx.components.UIControlVariable-1790182000502]: 
    varValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: script:params.entry
