↓fs [ngx.components.UIStackVariable-1610051895823]: 
  value: true
↓CustomAsyncAction [ngx.components.UICustomAsyncAction-1693478840785]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '		/*inner marker removed!*/
          		console.log(''CTS1693478840785'',page, vars)
          		let dontReload = false;
          		let args: any = null;
          		let verb: any = null;
          		let respInit: any = null;
          		page.local.hash = undefined;
          		page.local.sizeWallPaper = undefined;
          		let isProd = page.local.published == true || page.local.anonymous == true;
          		try{
          			page.local._formId = JSON.parse(JSON.stringify(page.local.formId));
          		}
          		catch(e){
          			
          		}
          		
          		// if in fullsync mode
          		if (vars.fromId) {
          			let r: any
          			try {
          				r = await page.c8o.callJsonObject("C8Oforms.APIV2_getDocument", {id:vars.fromId, fromResponse: "true"}).async();
          			}
          			catch(e){
          				page.c8o.log.error("[callViewLiveAndFillFormListViewer] unable to get response", e);
          				r = null;
          			}
          			 
          			if(page.local.formId == r?.res?.formId || (page.local.anonymous == true && page.global.standalonePwa == r?.res?.formId?.substring(0, r.res?.formId?.length - 10))){
          				page.local.myId = vars.fromId;
          				page.local.myFormId = page.local.myId;
          				page.local.myRev = r.res._rev;
          				page.local.fromResponse = true;
          			}
          			else{
          				page.local.myId = page.local.user+''_''+page.local.formId+''_''+new Date().getTime();
          				page.local.myFormId = page.local.myId;
          			}
          			respInit = r?.res?.resp;
          		}
          		else{
          			if(page.local.anonymous == true){
          				
          			}
          			else{
          				page.local.myId = page.local.user+''_''+page.local.formId+''_''+new Date().getTime();
          				page.local.myFormId = page.local.myId;
          			}
          			
          		}
          		if (vars.fs) {
          			let key = page.local["formId"];
          			if (respInit != null) {
          				page.local["respInit"] = respInit;
          				page["edit"] = true;
          			}
          			
          			
          			args = {
          				"id": key,
          				[C8oLocalCache.PARAM]: new C8oLocalCache(Priority.SERVER, 3.154e+10)
          			}
          			verb = ".APIV2_getDocument";
          		}
          		// if in sequence mode
          		else {
          			page.global.errorViewer = false;
          			// Fix 2.1.17: this branch expects viewerPage to validate `i` first.
          			// Calling getAnonymousForm with an invalid key breaks anonymous and
          			// authentication-required links.
          			page.local.user = page["navParams"].get(''i'');
          			args = {
          				"login": page.local.user,
          				"__disableAutologin": true,
          				[C8oLocalCache.PARAM]: new C8oLocalCache(Priority.SERVER, 3.154e+10)
          			}
          			verb = ".getAnonymousForm";
          		}
          
          		try {
          			//Define functions
          			let runNonBlockingInitStep = async (name, func) => {
          				try {
          					return await func();
          				}
          				catch (e) {
          					page.c8o.log.error("[callViewLiveAndFillFormListViewer] non-blocking init step failed: " + name, e);
          					return null;
          				}
          			}
          			let normalizeViewerDisabledComponents = (doc: any) => {
          				if (doc == null) {
          					return doc;
          				}
          				let disabledIds = {};
          				let markDisabled = (item: any) => {
          					if (item == null) {
          						return;
          					}
          					if (item.id != undefined) {
          						disabledIds["" + item.id] = true;
          					}
          					if (Array.isArray(item.children)) {
          						for (let child of item.children) {
          							markDisabled(child);
          						}
          					}
          				};
          				let collectDisabled = (list: any, parentDisabled = false) => {
          					if (!Array.isArray(list)) {
          						return;
          					}
          					for (let item of list) {
          						if (item == null) {
          							continue;
          						}
          						let disabled = parentDisabled || item?.config?.componentDisabled === true || (item.parentRef != undefined && disabledIds["" + item.parentRef] === true);
          						if (disabled) {
          							markDisabled(item);
          						}
          						collectDisabled(item.children, disabled);
          					}
          				};
          				let getDisabledRefId = (ref: any) => {
          					return ref != null && typeof ref == "object" ? ref.id : ref;
          				};
          				let filterDisabledRefs = (refs: any) => {
          					if (!Array.isArray(refs)) {
          						return refs;
          					}
          					return refs.filter((ref) => disabledIds["" + getDisabledRefId(ref)] !== true);
          				};
          				let filterDisabled = (list: any, parentDisabled = false) => {
          					if (!Array.isArray(list)) {
          						return [];
          					}
          					let filtered = [];
          					for (let item of list) {
          						if (item == null) {
          							continue;
          						}
          						let disabled = parentDisabled || item?.config?.componentDisabled === true || (item.parentRef != undefined && disabledIds["" + item.parentRef] === true);
          						if (disabled) {
          							markDisabled(item);
          							continue;
          						}
          						if (Array.isArray(item.children)) {
          							item.children = filterDisabled(item.children, false);
          						}
          						item.childrenRefs = filterDisabledRefs(item.childrenRefs);
          						item.childrenRefsElse = filterDisabledRefs(item.childrenRefsElse);
          						filtered.push(item);
          					}
          					return filtered;
          				};
          				collectDisabled(doc.formulaire, false);
          				collectDisabled(doc.actions, false);
          				if (Array.isArray(doc.flows)) {
          					for (let flow of doc.flows) {
          						collectDisabled(flow?.elements, false);
          					}
          				}
          				if (Array.isArray(doc.formulaire)) {
          					doc.formulaire = filterDisabled(doc.formulaire, false);
          				}
          				if (Array.isArray(doc.actions)) {
          					doc.actions = filterDisabled(doc.actions, false);
          				}
          				if (Array.isArray(doc.flows)) {
          					for (let flow of doc.flows) {
          						if (Array.isArray(flow?.elements)) {
          							flow.elements = filterDisabled(flow.elements, false);
          						}
          					}
          				}
          				return doc;
          			};
          			page.local.callEndInitC8o = async (response, newHash) => {
          				page.local.canRespond = true;				
          				await runNonBlockingInitStep("checkCanRespond", async () => {
          					return await (page as any).checkCanRespond(response.rows[0].value.doc, false);
          				});
          				//page.ref.reattach();
          
          				// remove errorViewer
          				page.local.errorViewer = false;
          				//updates value except page.formsList
          				page["form"] = response.rows[0].value.doc;
          				page["local"]["_rev"] = response.rows[0].value.doc[''_rev''];
          				page["link"] = response.rows[0].value.doc["wallpaper"]["link"];
          				if (response.rows[0].value.doc.collabsResponse != undefined && response.rows[0].value.doc.collabsResponse.length > 0) {
          					if (page.local.grp == undefined) {
          						page.local.grp = {};
          					}
          					response.rows[0].value.doc.collabsResponse.forEach((e) => {
          						page.local.grp[e] = true;
          					})
          				}
          				var isInitial = true;
          				// if response is diffrent from previous
          				if (page.local.hash == undefined) {
          					// this was inital loading of form so do nothing
          				}
          				// if there is an update while consulting 
          				else {
          					isInitial = false;
          					// if we are in production 
          					if (isProd) {
          						dontReload = true;
          						//return;//window.location.reload();
          					}
          					// if we are in preview
          					else {
          						if (!page.local.displayIngToast) {
          							page.local.displayIngToast = true;
          							let toastCtrl = page.getInstance(ToastController);
          							let toast = toastCtrl.create({
          								message: page["translate"].instant("the_form_has_been_updated"),
          								duration: 3000,
          								position: ''bottom'',
          								["buttons"]: [{ text: page["translate"].instant("close"), role: ''cancel'', side: ''end'' }],
          								cssClass: "toastSuccess"
          							}).then((res) => {
          								res.present();
          							})
          							setTimeout(() => {
          								page.local.displayIngToast = false;
          							}, 3000)
          							if (page.local.databsesTosync != undefined) {
          								for (let db in page.local.databsesTosync) {
          									page.c8o.callJsonObject("fs://"+db+".replicate_pull",{cancel: true});
          									page.c8o.log.debug("canceled " + db + " pull replication (brefore updating form)");
          								}
          							}
          							if (page.local.liveToCancel != undefined) {
          								for (let liveQuery of page.local.liveToCancel) {
          									page.c8o.cancelLive(liveQuery);
          									page.c8o.log.debug("canceled " + liveQuery + " live query (brefore updating form)");
          								}
          								page.local.liveToCancel = [];
          							}
          						}
          
          
          					}
          				}
          				page.local.hash = newHash;
          				if (isInitial) {
          					page.local.sourceValue = {};
          					page.local.functionsToBeCalled = {};
          				}
          				if (!dontReload) {
          
          				}
          				try {
          					if (isInitial) {
          						page["local"]["pages"] = response.rows[0].value.doc["pages"]
          						for (let i in page.local.pages) {
          							if (page["local"]["pages"][i]["enabled"] == undefined) {
          								page["local"]["pages"][i]["enabled"] = true;
          							}
          						}
          						page["local"]["_currentIndex"] = 0;
          						Object.defineProperty(page.local, "currentIndex", {
          						  get() {
          						    return page["local"]["_currentIndex"]
          						  },
          						  set(value) {
          							if(value == undefined){
          								console.warn("page.local.currentIndex was undefined, defaulted to 0 to avoid missing pages");
          								value = 0;
          							}
          						    page["local"]["_currentIndex"] = value;
          						    try {
          						    	setTimeout(() => {
          						    		page["initCurrentPageSources"]();
          						    	});
          						    }
          						    catch (e) {
          						    	
          						    }
          						  }
          						});
          					}
          					else {
          						// get previous pageTechNameByIndex
          						let indexPageTechName = page["local"]["pages"].map((x) => x.pageTechName);
          						for (let i in response.rows[0].value.doc["pages"]) {
          							// getting previous status of pageTechName if exists
          							let ArrayCorrespondigPage = page["local"]["pages"].filter((x) => x.pageTechName == response.rows[0].value.doc["pages"][i].pageTechName);
          							if (ArrayCorrespondigPage.length > 0) {
          								response.rows[0].value.doc["pages"][i]["enabled"] = ArrayCorrespondigPage[0]["enabled"];
          							}
          							else {
          								response.rows[0].value.doc["pages"][i]["enabled"] = true;
          							}
          						}
          						page["local"]["pages"] = response.rows[0].value.doc["pages"];
          					}
          				}
          				catch (e) {
          					page["local"]["pages"] = undefined;
          					page["local"]["currentIndex"] = 0;
          					console.error("catch1", e);
          				}
          				page.c8o.log.debug("CallViewLiveAndFillFormList: formsList object has been updated from live view");
          
          				// Load wallpaper
          				if (response.rows[0].value.doc["wallpaper"]["enabled"] && page["form"]["wallpaper"]["type"] != undefined && page["form"]["wallpaper"]["type"] != ''color'') {
          					if (page.local.url != page.c8o.fullSyncEndpoint + "/c8oforms_fs/" + page.form["_id"] + "/wallpaper") {
          						page.local.url = page.c8o.fullSyncEndpoint + "/c8oforms_fs/" + page.form["_id"] + "/wallpaper";
          						page["enabled"] = response.rows[0].value.doc["wallpaper"]["enabled"];
          					}
          				}
          				else {
          					page["enabled"] = response.rows[0].value.doc["wallpaper"]["enabled"];
          				}
          
          				// if we have reloaded the form charge again the currents values
          				if (!isInitial) {
          					respInit = { ...page["formsSubmit"] };
          					page.local.functionsToBeCalled = {};
          					(page as any).functionsById = {};
          					(page as any).previousAndPostFunctions = {};
          				}
          				let config = (page.form as any)?.config;
          				if(config == undefined){
          					config = (page as any).legacyConfig;
          				}
          				document.documentElement.style.setProperty(''--base-font-size-viewer'', config?.[page.local.currentBreakpoint]?.["font-size"]?.value + config?.[page.local.currentBreakpoint]?.["font-size"]?.unit);
          				document.documentElement.style.setProperty(''--config-margin-viewer'', ''0 ''+config?.[page.local.currentBreakpoint]?.["horizontalMargin"]?.value + config?.[page.local.currentBreakpoint]?.["horizontalMargin"]?.unit); 
          				document.documentElement.style.setProperty(''--config-max-width-viewer'', config?.[page.local.currentBreakpoint]?.["width"]?.value + config?.[page.local.currentBreakpoint]?.["width"]?.unit);
          				page.local.initializedSourcePages = {};
          				page.local.initializingSourcePages = {};
          				
          				// calling fillFormSubmit
          				await page["fillFormSubmit"](response.rows[0].value.doc.formulaire, page["redList"], page["formsSubmit"], respInit);
          				// then affetct value of form
          				page["formsList"] = page["form"]["formulaire"];
          				//affect page title
          				page["setTitleRGAA"]();
          				// calling fillActionsVars
          //				if (page["form"]["actions"] != undefined) {
          //					page["fillActionsVars"](page["form"]["actions"])
          //					// calling fillActions
          //					await page["fillActions"](page["form"]["actions"], null, null);
          //				}
          
          //				if (page["form"]["flows"]?.length > 0) {
          //					let formulas = page["form"]["flows"].find(x=>x.id == "formulas");
          //					formulas = formulas.elements;
          //					page["fillActionsVars"](formulas)
          //					// calling fillActions
          //					await page["fillActions"](formulas, null, null);
          //				}
          				await runNonBlockingInitStep("fillActions", async () => {
          					if (page["form"]["flows"]?.length > 0) {
          						for(let flow of page["form"]["flows"]){
          							let elem = flow.elements;
          							page["fillActionsVars"](elem);
          						}
          						let formulas = page["form"]["flows"].find(x=>x.id == "formulas")?.elements;
          						if(formulas != undefined){
          							await page["fillActions"](formulas);
          						}
          					}
          					else{
          						page["fillActionsVars"](page["form"]["actions"])
          						// calling fillActions
          						await page["fillActions"](page["form"]["actions"]);
          					}
          				});
          				await runNonBlockingInitStep("initCurrentPageSources", async () => {
          					return await page["initCurrentPageSources"](true);
          				});
          
          
          				//²if(isInitial){
          				// check conditions
          				await runNonBlockingInitStep("checkForAnyCondition", async () => {
          					page["checkForAnyCondition"]();
          				});
          				//}	
          
          				// do stuff to support upload files
          				if (page.global["__C8O_TODELETE"] == undefined) {
          					page.global["__C8O_TODELETE"] = [];
          				}
          
          				for (let item of page["formsList"]) {
          					if (item[''type''] == "file") {
          						if (page.global[page["idFiles"]][item["id"]] == null) {
          							page.global[page["idFiles"]][item["id"]] = [];
          						}
          					}
          					if (item["type"] == "ion-card" && item["children"] != undefined) {
          						for (let child of item["children"]) {
          							if (child[''type''] == "file") {
          								if (page.global[page["idFiles"]][child["id"]] == null) {
          									page.global[page["idFiles"]][child["id"]] = [];
          								}
          							}
          						}
          					}
          				}
          				return;
          			}
          			let checkOffline = async (response) => {
          				//check for offline sources
          				page.global.waitFSBaseStatus = {};
          				page.local["databsesTosync"] = {};
          				page["getDatabasesToSync"](page.local["databsesTosync"], response.rows[0].value.doc.formulaire);
          				if (Object.keys(page.local["databsesTosync"]).length > 0) {
          					page.local.isOfflinesource = true;
          					//page.ref.detach();
          					let hasToRestart = await page["getFullsyncSources"](response.rows[0].value.doc.formulaire);
          					if (hasToRestart != undefined && hasToRestart.restart == true) {
          						return hasToRestart;
          					}
          					else {
          						page["startSyncDatabases"](page.local["databsesTosync"]);
          						let promisesSync = [];
          						Object.keys(page.local.promisesFS).forEach((e) => {
          							promisesSync.push(page.local.promisesFS[e]);
          						});
          						Promise.all(promisesSync)
          							.then(() => {
          								//page.local.callEndInitC8o();
          							})
          						return { restart: false };
          					}
          
          
          				}
          				else {
          					//page.local.callEndInitC8o();
          					return { restart: false };
          				}
          			}
          			//end define functions
          			let exec = async () => {
          				let response: any = await page.c8o.callJsonObject("C8Oforms"+verb, args).async();
          				let cond: boolean;
          				let normalizedRows: any[] = [];
          				if (vars.fs) {
          					cond = !page.local.submitting;
          					if (response.res) {
          						//by formating as it, emulating view old view structure
          						response = { rows: [{ id: response.res._id, key: response.res._id, value: { doc: response.res } }] };
          					}
          					normalizedRows = Array.isArray(response?.rows) ? response.rows : [];
          				}
          				else {
          					cond = response?.transaction != undefined && response?.transaction?.document != undefined && response?.transaction?.document?.couchdb_output != undefined;
          					response = response?.transaction?.document?.couchdb_output;
          					// Fix 2.1.17: invalid anonymous responses may not contain rows.
          					// Keep rows normalized so viewer errors never crash on `.length`.
          					normalizedRows = Array.isArray(response?.rows) ? response.rows : [];
          					if (normalizedRows[0]?.doc != null) {
          						var myIdP = normalizedRows[0].doc._id;
          						normalizedRows[0].value = {doc: normalizedRows[0].doc};
          						if (page.local.grp == undefined) {
          							page.local.grp = {};
          						}
          						const anonymousIndex = typeof myIdP === "string" ? myIdP.indexOf("_anonymous") : -1;
          						if (anonymousIndex > -1) {
          							const key = "_C8O_HIDDEN_" + myIdP.substring(0, anonymousIndex);
          							page.local.grp[key] = true;
          						}
          						page.local.formId = normalizedRows[0].doc._id;
          						if(!page.local.fromResponse){
          							const array = new Uint32Array(2);
          							const cryptoApi = globalThis?.crypto ?? (window as any)?.crypto ?? (window as any)?.msCrypto;
          							if (cryptoApi?.getRandomValues != undefined) {
          								cryptoApi.getRandomValues(array);
          							}
          							else {
          								array[0] = Math.floor(Math.random() * 0xffffffff);
          								array[1] = Math.floor(Math.random() * 0xffffffff);
          							}
          			                let random = array[0] * array[1];
          			                let formId = page.local.formId.substring(0, page.local.formId.length -10);
          			                page.local.myId = ''anonymous-''+ random +''_''+ page.local.formId + ''_'' + page["epoch"];
          			                page.local.myFormId = page.local.myId;
          						}
          					}
          					else {
          						cond = false;
          					}
          				}
          				if (normalizedRows[0]?.value?.doc != null) {
          					normalizeViewerDisabledComponents(normalizedRows[0].value.doc);
          				}
          				let newHash: any = undefined;
          				try {
          					if (normalizedRows[0] != undefined) {
          						newHash = sha256(JSON.stringify(normalizedRows[0]));
          					}
          				}
          				catch (e) { }
          				if (cond && normalizedRows[0] != undefined && newHash != page.local.hash && normalizedRows.length > 0) {
          
          
          					let res = await checkOffline(response)
          					if (res != undefined && res.restart == true) {
          
          					}
          					else {
          						await page.local.callEndInitC8o(response, newHash);
          					}
          				}
          				else if (normalizedRows.length == 0) {
          					page.local.spinner = false
          					page.local.errorMsgViewer = ''error_not_found'';
          					page.local.errorViewer = true;
          					page[''events''].publish("anyInitalSyncFinished");
          				}
          				else if (response?.document != undefined && response?.document?.error != undefined) {
          					if (response.document.error.message == "Max number of sessions exceeded for Standard Edition") {
          						page.c8o.log.fatal("Max number of sessions exceeded for Standard Edition")
          					}
          
          				}
          				else {
          					page.c8o.log.error("[CallViewLiveAndFillFormListViewer] invalid response shape " + JSON.stringify({
          						hasResponse: response != null,
          						hasRows: Array.isArray(response?.rows),
          						keys: response ? Object.keys(response) : null
          					}));
          					page.local.spinner = false
          					page.local.errorMsgViewer = ''error_not_found'';
          					page.local.errorViewer = true;
          					page[''events''].publish("anyInitalSyncFinished");
          				}
          			}
          			//initial
          			await exec();
          			//live
          			if(!isProd){
          				try {
          					let p = Pouch["default"];
          					const header = {
          						"x-convertigo-sdk": page.c8o.sdkVersion,
          					};
          					if (page.c8o.xsrfUsed) {
          						const headerName = "x-xsrf-token";
          						const fetch = "Fetch";
          						header[headerName] = window.localStorage.getItem(headerName) != undefined ? window.localStorage.getItem(headerName) : fetch;
          					}
          					Object.assign(header, page.c8o.headers);
          					let remotePouchHeader = {
          						fetch: (url, opts) => {
          							opts.credentials = ''include'';
          							for (let key in header) {
          								opts.headers.set(key, header[key]);
          							}
          							var fecthPouch: Promise<Response> = p.fetch(url, opts);
          							fecthPouch.then((response) => {
          								page.c8o.session.sort(response, header, null, null, null);
          							})
          								.catch((e) => {
          	
          								});
          							return fecthPouch;
          						}
          					};
          					let dbChanges = new p(page.c8o.fullSyncEndpoint+"/c8oforms_fs", remotePouchHeader);
          					page.local.live = dbChanges.changes({
          						live: true, since: ''now''
          					})
          					.on(''change'', async (change) => {
          						// handle change
          						if(change.id == page.local["formId"]){
          							await exec();
          							page.ref.detectChanges();
          						}
          					}).on(''complete'', function (info) {
          					}).on(''error'', function (err) {
          						console.error("an error occured", err);
          					});
          				}
          				catch (e) {
          	
          				}
          			}
          			page.ref.detectChanges();
          			
          
          		}
          		catch (e) {
          			page.c8o.log.error("An error occured into CallViewLiveAndFillFormList viewer " + JSON.stringify(e.message));
          			throw e;
          		}
          '
  app_ts_imports: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
      - com.twinsoft.convertigo.beans.common.XMLVector: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ ToastController }'
            - java.lang.String: 
              - ↑value: ionic-angular
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '* as _'
            - java.lang.String: 
              - ↑value: lodash
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ sha256 }'
            - java.lang.String: 
              - ↑value: js-sha256
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ Priority }'
            - java.lang.String: 
              - ↑value: c8osdkangular
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: '{ C8oLocalCache }'
            - java.lang.String: 
              - ↑value: c8osdkangular
  package_dependencies: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
      - com.twinsoft.convertigo.beans.common.XMLVector: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: lodash
            - java.lang.String: 
              - ↑value: 4.17.20
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.XMLVector
          - com.twinsoft.convertigo.beans.common.XMLVector: 
            - java.lang.String: 
              - ↑value: js-sha256
            - java.lang.String: 
              - ↑value: 0.9.0
  ↓Failure_Handler [ngx.components.UIActionFailureEvent-1693478878149]: 
    ↓IfElse [ngx.components.UIDynamicIf-1693478878152]: 
      beanData: '{"ionBean":"IfElseAction","condition":"script:page.global.pwa == true || page.local.anonymous == true"}'
      ↓CustomAction [ngx.components.UICustomAction-1693478878155]: 
        actionValue: 
          - xmlizable: 
            - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
            - com.twinsoft.convertigo.beans.common.FormatedContent: 
              →: |
                		    page.local.errorMsgViewer ="Mon super message assez long et vla";
                		    page.local.errorViewer = true;
                		resolve();
                
        isEnabled: false
      ↓InvokeSharedAction [ngx.components.UIDynamicInvoke-1693478878158]: 
        beanData: '{"ionBean":"InvokeAction"}'
        stack: C8Oforms.MobileApplication.Application.setLocal
        ↓val [ngx.components.UIControlVariable-1693478878161]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:'error_not_found'
        ↓path [ngx.components.UIControlVariable-1693478878164]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:'errorMsgViewer'
      ↓InvokeSharedAction1 [ngx.components.UIDynamicInvoke-1693478878167]: 
        beanData: '{"ionBean":"InvokeAction"}'
        stack: C8Oforms.MobileApplication.Application.setLocal
        ↓val [ngx.components.UIControlVariable-1693478878170]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:true
        ↓path [ngx.components.UIControlVariable-1693478878173]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:'errorViewer'
      ↓PublishEventInitalSyncFinished [ngx.components.UIDynamicAction-1693478878176]: 
        beanData: |
          '{
           "ionBean": "PublishEventAction",
           "topic": "plain:anyInitalSyncFinished",
           "data": "script"
          }'
      ↓Else_Handler [ngx.components.UIActionElseEvent-1693478878179]: 
        ↓RootPage [ngx.components.UIDynamicAction-1693478878182]: 
          beanData: '{"ionBean":"RootPageAction","page":"plain:C8Oforms.MobileApplication.Application.selectorPage"}'
  ↓fs [ngx.components.UIControlVariable-1693478874959]: 
    varValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: script:stack.callViewLiveAndFillFormListViewer.in.vars.fs
  ↓fromId [ngx.components.UIControlVariable-1697472800388]: 
    varValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: script:stack.callViewLiveAndFillFormListViewer.in.vars.fromId
↓fromId [ngx.components.UIStackVariable-1697472767987]: 
  value: 