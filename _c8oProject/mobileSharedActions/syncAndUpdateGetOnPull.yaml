↓DoGetDoc [ngx.components.UICustomAsyncAction-1696259419797]: 
  actionValue: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
      - com.twinsoft.convertigo.beans.common.FormatedContent: 
        →: |
          '		try {
          			page.global.localform = {};
          		   
          		    /**
          		     * DESCRIBES PATH TO TITLE (SIMPLE) OF EACH ELEMENT
          		    */
          		    
          		    const pathToTitle = {
          		            "text" : {"location":"config.label"},
          		            "ion-label": {"location":"config.text"},
          		            "checkbox":{"location":"config.label"},
          		            "checkbox_group":{"location":"config.label"},
          		            "radio":{"location":"config.label"},
          		            "radio_group":{"location":"config.label"},
          		            "slider":{"location":"config.label"},
          		            "select":{"location":"config.label"},
          		            "datetime":{"location":"config.label"},
          		            "time":{"location":"config.label"},
          		            "img":{"location":"config.label"},
          		            "barcode":{"location":"config.label"},
          		            "file":{"location":"config.label"},
          		            "signature":{"location":"config.label"},
          		            "ion-card":{"location":"config.title.text"}
          		            };
          		            
          		    /**
                       * DESCIBES PATH TO TITLE (HTML) OF EACH ELEMENT THAT WE WANTS TO CREATE
                      */
          		    
          		    const pathToCopy = {
                              "text" : {"location":"config.html"},
                              "ion-label": {"location":"config.html"},
                              "checkbox":{"location":"config.html"},
                              "checkbox_group":{"location":"config.html"},
                              "radio":{"location":"config.html"},
                              "radio_group":{"location":"config.html"},
                              "slider":{"location":"config.html"},
                              "select":{"location":"config.html"},
                              "datetime":{"location":"config.html"},
                              "time":{"location":"config.html"},
                              "img":{"location":"config.html"},
                              "barcode":{"location":"config.html"},
                              "file":{"location":"config.html"},
                              "signature":{"location":"config.html"},
                              "ion-card":{"location":"config.title.html"}
                              };
          		  
          		    /**
          		     * GET VALUE THAT WE WANTS TO COPY
          		    */
          		    const getValueToCopy = (obj: Array<any>, path: string, i: number = 0): any =>{
          		        const arr = path.split(".")
                          const elem = obj[arr[i]];
                          if(i === arr.length -1){
                              //finished
                              return elem;
                          }
                          else{
                              //call cursive
                             return getValueToCopy(elem, path, i + 1);
                          }
                      };
                      
                      /**
                       * COPY VALUE TO NEW PATH
                      */
                      const copyValueToNewPath = (obj: Array<any>, path: string, value: string, i: any) :void =>{
                          try{
                              if(eval("obj["+i+"]."+ pathToCopy[obj[i].type].location + "== undefined")){
                                  eval("obj["+i+"]."+ pathToCopy[obj[i].type].location + "=''"+ value+"''");
                              }
                          }
                          catch(e){
                              page.c8o.log.error("error", e);
                          }
                      }
                      
                      /**
                       * UPDATE FORM TO SUPPORT PAGE
                      */
          		    const updateToPages = (obj: Array<any>): string => {
          		        let pageTechName = "Page_" + new Date().getTime();
          		        for(let i in obj){
          		            try{
          	                    obj[i]["config"]["page"] = pageTechName;
          		            }
          		            catch(e){
          		                console.error("error during migration: \n",e);
          		            }
          		        }
          		        return pageTechName;
          		    };
          		    
          		    /**
                       * UPDATE FORM TO CREATE HTML PATH 
                      */
                      const updateHtml = (obj: Array<any>): void => {
                          for(let i in obj){
                              try{
                                  var valToCopy = getValueToCopy(obj[i], pathToTitle[obj[i].type].location);
                                  copyValueToNewPath(obj, pathToCopy[obj[i].type].location, valToCopy, i);
                              }
                              catch(e){
                                  console.error("error during migration: \n",e);
                              }
                          }
                      };
                      
                      /**
                       * UPDATE FORM TO SUPPORT CONDITIONS
                      */
                      const updateToConditions = (obj: Array<any>): void => {
                          for(let i in obj){
                              try{
                                  if(obj[i]["type"] == "radio_group" || obj[i]["type"] == "checkbox_group"){
                                          for(let child of obj[i]["children"]){
          									if(child["id"] == undefined){
          										child["id"] = (Math.random() + "").substring(2);
          									}
                                          }
                                          for(let child of obj[i]["lines"]){
          									if(child["id"] == undefined){
                                              	child["id"] = (Math.random() + "").substring(2);
          									}
                                          }
                                  }
                                  else if(obj[i]["type"] == "select" || obj[i]["type"] == "radio" || obj[i]["type"] == "checkbox"){
                                      for(let child of obj[i]["children"]){
          								if(child["id"] == undefined){
                                          	child["id"] = (Math.random() + "").substring(2);
                                      	}
          							}
                                  }
                                  else if(obj[i]["type"] == "ion-card" && obj[i]["children"] != undefined){
                                      updateToConditions(obj[i]["children"]);
                                  }
                              }
                              catch(e){
                                  console.error("error during migration: \n",e);
                              }
                          }
                      };
          
          			/**
          				FIX FORM GROUP PAGETECHNAME 
          			 */
          		    const fixGroupChildsPages = (obj: Array<any>): void => {
                          for(let i in obj){
                              try{
                                  if(obj[i]["type"] == "ion-card" && obj[i]["children"] != undefined){
          							let pageTechName = obj[i].config.page
          							for(let j in obj[i]["children"]){
          								obj[i]["children"][j].config.page = pageTechName;
          							}
          						}
          					}
                              catch(e){
                                  console.error("error during migration: \n",e);
                              }
                      	}
           			}
          		    page.global["__helpersRadio"] = (arr)=>{
                          for(let elem in arr){
                              if(arr[elem]["selected"]){
                                  return arr[elem]["value"];
                              }
                          }  
                        };
                        page.global["__helpersRadioGrp"] = (arr)=>{
                            let obj = 
                            {
                            "children":
                                {
                                    
                                }
                            }
                            for(let elem in arr){
                                obj["children"][arr[elem]["title"]] = arr[elem]["selected"]
                            }
                            return obj;
                          };
          			const updateToSimlplified = (obj: Array<any>)=>{
          				for(let i in obj){
                              try{
                                  if(obj[i]["type"] == "ion-card" && obj[i]["children"] != undefined){
          							if(obj[i].config.title.personalized == false){
          								obj[i].config.title.personalized = true;
          								obj[i].config.title.html = "<p style=\"text-align: center;\">"+obj[i].config.title.text+"</p>";
          							}
                                      updateToSimlplified(obj[i]["children"]);
                                  }
                                  else if(obj[i]["type"] == "description"){
                                      // do nothing
                                  }
                                  else {
                                      if(obj[i].config.personalized == false){
          								obj[i].config.personalized = true;
          								obj[i].config.html = "<p>"+obj[i].config.label+"</p>";
          							}
                                  }
                              }
                              catch(e){
                                  console.error("error during migration: \n",e);
                              }
                          }
          			}
          		  	const addNewProperties = (obj) => {
          				for(let p of obj.pages){
          					if(p["iconName"] == undefined || p["iconName"] == ""){
          						p["iconName"] = "book-outline";
          					}
          					if(p["positionTab"] == undefined || p["positionTab"] == ""){
          						p["positionTab"] = "bottom";
          					}
          					if(p["enabledTab"] == undefined || p["enabledTab"] == ""){
          						p["enabledTab"] = false;
          					}
          					if(p["included"] == undefined || p["included"] == ""){
          						p["included"] = true;
          					}
          					if(p["enabledButtons"] == undefined || p["enabledButtons"] == ""){
          						p["enabledButtons"] = true;
          					}
          					if(p["positionButtons"] == undefined || p["positionButtons"] == ""){
          						p["positionButtons"] = "standard";
          					}
          				}
          			}
          			// loopToForm
          			const addNewPropertiesLoopToForm = (obj) => {
          				obj["loopToForm"] = true;
          			}
          			const translateOldTsToNewTs = (obj: any) => {
          				for(let elem of obj){
          					if (elem?.sources) {
          						for (let source of elem.sources) {
          							if (source?.type == "ts" && source?.value?.indexOf("{{") != -1) {
          								source.type = "expression";
          								source.value = source.value.replace("{{", "").replace("}}", "");
          							}
          						}
          					}
          				}
          			}
          			const addSelfVarToCandidates = (obj: any) => {
          				for(let i in obj){
                              try{
                                  if(obj[i]["type"] == "ion-card" && obj[i]["children"] != undefined){
                                      addSelfVarToCandidates(obj[i]["children"]);
                                  }
                                  else if(obj[i]["type"] == "text" || obj[i]["type"] == "slider" || obj[i]["type"] == "datetime" || obj[i]["type"] == "time" || obj[i]["type"] == "barcode" ){
                                      if(obj[i].sources == undefined){
          							obj[i].sources = {
          				                    "self": {
          				                        "enabled": true,
          				                        "vars": {
          				                            "selfVar": {
          				                                str: "",
          				                                type: "text"
          				                            }
          				                        }
          				                    }
          				                };
          							}
                                  }
                              }
                              catch(e){
                                  console.error("error during migration: \n",e);
                              }
                          }
          			}
          			const addchildrenRefsToCards = (obj: any) => {
          				for(var i = 0; i < obj.length; i++){
          					try {
          						if (obj[i]["type"] == "ion-card" && obj[i]["children"] != undefined) {
          							obj[i].childrenRefs = [];
          							for (let j in obj[i]["children"]) {
          								obj[i].childrenRefs.push(obj[i]["children"][j].id);
          								//splice obj to insert childrenRefs after current card
          								obj[i]["children"][j].parentRef = obj[i].id;
          								obj.splice(+i + 1, 0, obj[i]["children"][j]);
          							}
          							delete obj[i]["children"];
          						}
          					}
          					catch (e) {
          						console.error("error during migration: \n", e);
          					}
          				}
          			}
          			const addConfig = (obj: any) => {
          				if (obj.config == undefined) {
          					obj.config = {
          						desktop: {
          							horizontalMargin: {unit: "px", value: "100"}, width:{unit: "px", value: "1280"}, "font-size": {unit: "px", value: "16"}
          						},
          						tablet: {
          							horizontalMargin: {unit: "px", value: "60"}, width: {unit: "px", value: "940"}, "font-size": {unit: "px", value: "16"}
          						},
          						phoneL: {
          							horizontalMargin: {unit: "px", value: "40"}, width: {unit: "%", value: "100"}, "font-size": {unit: "px", value: "16"}
          						},
          						phoneP: {
          							horizontalMargin: {unit: "px", value: "20"}, width: {unit: "%", value: "100"}, "font-size": {unit: "px", value: "16"}
          						}
          					}
          				}
          			}
          			const addFlows = (obj)=>{
          				if(obj.flows == undefined){
          					obj.flows = [{"id": "formulas", "elements": []}, {"id": "submit", elements: []}];
          				}
          				if(obj.actions){
          					obj.actions.forEach((action)=>{
          						if(action.type == "business_logic"){
          							obj.flows[0].elements.push(action);
          						}
          						if(action.type == "submit"){
          							obj.flows[1].elements.push(action);
          						}
          					});
          					obj.actions = null;
          				}
          			}
          			const addSourceEnabledToGrid = (obj) => {
          				for(var i = 0; i < obj.length; i++){
          					try {
          						if (obj[i]["type"] == "grid" || obj[i]["type"] == "gallery") {
          							if(obj[i].config.sourceEnabled == undefined){
          								obj[i].config.sourceEnabled = true;
          								obj[i].config.columns = [{name:"Make", type: "text"}, {name:"Model", type: "text"}, {name:"Price", type: "Number"}];
          							}
          							if(obj[i].config.columns == undefined){
          								obj[i].config.columns = [
          							         {
          							           "name": "Make",
          							           "type": "text"
          							         },
          							         {
          							           "name": "Model",
          							           "type": "text"
          							         },
          							         {
          							           "name": "Price",
          							           "type": "number"
          							         }
          								 ]
          							}
                                      if (obj[i]["type"] == "grid") {
                                          if(obj[i].config.gridFooterEnabled == undefined){
                                              obj[i].config.gridFooterEnabled = true;
                                          }
                                          if(obj[i].config.gridPaginationEnabled == undefined){
                                              obj[i].config.gridPaginationEnabled = true;
                                          }
                                          if(obj[i].config.gridRowsPerPage == undefined){
                                              obj[i].config.gridRowsPerPage = 10;
                                          }
                                      }
          						}
          					}
          					catch (e) {
          						console.error("error during migration: \n", e);
          					}
          				}
          			}
          			const addCheckMandatoryInCurrentPage = (obj) => {
          				for(var i = 0; i < obj.length; i++){
          					try {
          						if (obj[i]["type"] == "button") {
          							if(obj[i].config.checkMandatoryInCurrentPage == undefined){
          								obj[i].config.checkMandatoryInCurrentPage = false;
          							}
          						}
          					}
          					catch (e) {
          						console.error("error during migration: \n", e);
          					}
          				}
          			}
          			const addCheckMandatoryInCurrentPageForTabs = (obj) => {
          					for(var i = 0; i < obj.length; i++){
          						try {
          							if(obj[i].checkMandatoryInCurrentPage == undefined){
          								obj[i].checkMandatoryInCurrentPage = true;
          							}
          						}
          						catch (e) {
          							console.error("error during migration: \n", e);
          						}
          					}
          				}
          				
          			const addDescriptionInPages = (obj: any): void => {
          			  if (!obj || !Array.isArray(obj.pages) || !Array.isArray(obj.formulaire)) {
          			    console.error("Invalid object: pages or formulaire missing.");
          			    return;
          			  }
          	
          			  for (let i = 0; i < obj.pages.length; i++) {
          			    try {
          				  
          			      const currentPage = obj.pages[i];
          	
          			      /*if (!page || typeof currentPage.pageTechName !== "string") {
          			        continue;
          			      }*/
          				  
          				  obj.pages[i]["isNameDisplayed"] = true;
          	
          			      const desc = (typeof currentPage.desc === "string" ? currentPage.desc.trim() : "") + "";
          			      delete obj.pages[i].desc;
          			      if (!desc || desc.length === 0) {
          			        continue;
          			      }
          				  
          				  obj.pages[i]["isNameDisplayed"] = false;
          	
          			      const pageId = currentPage.pageTechName;
          	
          			      const descriptionComponent = {
          			        type: "description",
          			        config: {
          			          html: desc,
          			          personalized: true,
          			          page: pageId
          			        },
          			        id: Date.now() + i,
          			        name: `${(page as any).getNameDispo("description", null)}`
          			      };
          	
          			      const firstIndexForPage = obj.formulaire.findIndex((component: any) => {
          			        return component?.config?.page === pageId && component?.parentRef == null;
          			      });
          	
          			      if (firstIndexForPage !== -1) {
          			        obj.formulaire.splice(firstIndexForPage, 0, descriptionComponent);
          			      } else {
          			        obj.formulaire.push(descriptionComponent);
          			      }
          				  delete obj.pages[i].desc;
          			    } catch (e) {
          			      console.error("error during migration:\n", e);
          			    }
          			  }
          			};
          			
          			const addGlobalNavigationSettingsInApp = (obj: any): void => {
          			  try {
          				  if (!obj || !Array.isArray(obj.pages)) {
          				    console.error("Invalid object: pages missing.");
          				    return;
          				  }
          	
          				  const navigationKeys = [
          				    "positionTab",
          				    "enabledTab",
          				    "enabledButtons",
          				    "positionButtons",
          					"included",
          					"checkMandatoryInCurrentPage",
          				  ] as const;
          	
          				  const globalNavigation: Record<string, any> = {
          				    positionTab: null,
          				    enabledTab: null,
          				    enabledButtons: null,
          				    positionButtons: null,
          					included: null,
          					checkMandatoryInCurrentPage: null,
          				  };
          	
          				  let hasReference = false;
          	
          				  for (const currentPage of obj.pages) {
          				    try {
          				      const hasNavigationConfig = navigationKeys.some((key) =>
          				        Object.prototype.hasOwnProperty.call(currentPage, key)
          				      );
          	
          				      if (!hasNavigationConfig) {
          				        continue;
          				      }
          	
          				      if (!hasReference) {
          				        for (const key of navigationKeys) {
          				          globalNavigation[key] = Object.prototype.hasOwnProperty.call(currentPage, key)
          				            ? currentPage[key]
          				            : null;
          				        }
          	
          				        hasReference = true;
          				        continue;
          				      }
          	
          				      for (const key of navigationKeys) {
          				        if (
          				          globalNavigation[key] !== null &&
          				          Object.prototype.hasOwnProperty.call(currentPage, key) &&
          				          currentPage[key] !== globalNavigation[key]
          				        ) {
          				          globalNavigation[key] = null;
          				        }
          				      }
          				    } catch (e) {
          				      console.error("error during migration:\n", e);
          				    }
          				  }
          				  obj["globalNavigationEnabled"] = false;
          				  obj["navigation"] = globalNavigation;
          //				  console.log(obj);
          			  } catch(e) {
          				console.error(e);
          			  }
          			};
          			
          			const addGlobalNavigationIncludingIndividualNavigationInApp = (obj: any): void => {
          			  try {
          				  if (!obj || !Array.isArray(obj.pages)) {
          				    console.error("Invalid object: pages missing.");
          				    return;
          				  }
          	
          				  const navigationKeys = [
          				    "individualNavigation",
          				  ] as const;
          	
          				  for (let i = 0; i < obj.pages.length; i++) {
          				    try {
          				    
          				      const currentPage = obj.pages[i];
          				      const hasNavigationConfig = navigationKeys.some((key) =>
          				        Object.prototype.hasOwnProperty.call(currentPage, key)
          				      );
          	
          				      if (hasNavigationConfig) {
          				        continue;
          				      }
          
          			          obj.pages[i]["individualNavigation"] = Object.prototype.hasOwnProperty.call(currentPage, "individualNavigation")
          			            ? currentPage["individualNavigation"]
          			            : false;
          				    } catch (e) {
          				      console.error("error during migration:\n", e);
          				    }
          				  }
          				  obj["navigation"]["appliedConfiguration"] = null;
          			  } catch(e) {
          				console.error(e);
          			  }
          			};
          			
          			const setGlobalNavigationToTrue = (obj: any): void => {
          				obj["globalNavigationEnabled"] = true;
          				delete obj["navigation"]["appliedConfiguration"];
          			}
          			
          			const repairGhostComponents = (obj: any): void => {
          				if (!obj || !Array.isArray(obj.pages) || !Array.isArray(obj.formulaire)) {
          					console.error("Invalid object: pages or formulaire missing.");
          					return;
          				}
          				let validPages = {};
          				let hasValidPage = false;
          				for (let p = 0; p < obj.pages.length; p++) {
          					let currentPage = obj.pages[p];
          					if (currentPage != null && currentPage.pageTechName != null && currentPage.pageTechName !== "") {
          						validPages["" + currentPage.pageTechName] = true;
          						hasValidPage = true;
          					}
          				}
          				if (!hasValidPage) {
          					return;
          				}
          				let refKeys = ["childrenRefs", "childrenRefsElse"];
          				let idsToRemove = {};
          				let seenIds = {};
          				let markDescendants = (ids) => {
          					let changed = true;
          					while (changed) {
          						changed = false;
          						for (let i = 0; i < obj.formulaire.length; i++) {
          							let item = obj.formulaire[i];
          							if (item == null || item.id == null) {
          								continue;
          							}
          							if (ids["" + item.id]) {
          								for (let r = 0; r < refKeys.length; r++) {
          									let refs = item[refKeys[r]];
          									if (!Array.isArray(refs)) {
          										continue;
          									}
          									for (let j = 0; j < refs.length; j++) {
          										let refId = refs[j] != null && refs[j].id != null ? refs[j].id : refs[j];
          										if (refId != null && !ids["" + refId]) {
          											ids["" + refId] = true;
          											changed = true;
          										}
          									}
          								}
          								continue;
          							}
          							let parentId = item.parentRef != null ? "" + item.parentRef : null;
          							if (parentId != null && ids[parentId]) {
          								ids["" + item.id] = true;
          								changed = true;
          							}
          						}
          					}
          				};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					if (item != null && item.id != null) {
          						seenIds["" + item.id] = true;
          					}
          				}
          				let usedIds = seenIds;
          				let getNextMigrationId = () => {
          					let nextId = new Date().getTime();
          					while (usedIds["" + nextId]) {
          						nextId = nextId + 1;
          					}
          					usedIds["" + nextId] = true;
          					return nextId;
          				};
          				seenIds = {};
          				let duplicateIdMap = {};
          				let duplicateItems = {};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					if (item == null || item.id == null) {
          						continue;
          					}
          					let id = "" + item.id;
          					if (seenIds[id]) {
          						let itemPage = item.config != null ? item.config.page : "";
          						let newId = getNextMigrationId();
          						duplicateIdMap[id + "|" + itemPage] = newId;
          						item.id = newId;
          						duplicateItems["" + newId] = true;
          						id = "" + item.id;
          					}
          					seenIds[id] = true;
          					let itemPage = item.config != null ? item.config.page : null;
          					if (itemPage != null && itemPage !== "" && !validPages["" + itemPage]) {
          						idsToRemove[id] = true;
          					}
          				}
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					if (item == null || item.id == null || !duplicateItems["" + item.id]) {
          						continue;
          					}
          					let itemPage = item.config != null ? item.config.page : "";
          					if (item.parentRef != null && duplicateIdMap["" + item.parentRef + "|" + itemPage] != null) {
          						item.parentRef = duplicateIdMap["" + item.parentRef + "|" + itemPage];
          					}
          					for (let r = 0; r < refKeys.length; r++) {
          						let refs = item[refKeys[r]];
          						if (!Array.isArray(refs)) {
          							continue;
          						}
          						for (let j = 0; j < refs.length; j++) {
          							let refId = refs[j] != null && refs[j].id != null ? refs[j].id : refs[j];
          							if (refId != null && duplicateIdMap["" + refId + "|" + itemPage] != null) {
          								refs[j] = duplicateIdMap["" + refId + "|" + itemPage];
          							}
          						}
          					}
          				}
          				markDescendants(idsToRemove);
          				obj.formulaire = obj.formulaire.filter((item) => item != null && item.id != null && !idsToRemove["" + item.id]);
          				let byId = {};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					byId["" + obj.formulaire[i].id] = obj.formulaire[i];
          				}
          				idsToRemove = {};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					if (item.parentRef != null && byId["" + item.parentRef] == null) {
          						idsToRemove["" + item.id] = true;
          					}
          				}
          				markDescendants(idsToRemove);
          				obj.formulaire = obj.formulaire.filter((item) => item != null && item.id != null && !idsToRemove["" + item.id]);
          				byId = {};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					byId["" + obj.formulaire[i].id] = obj.formulaire[i];
          				}
          				let ownerByChildId = {};
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					for (let r = 0; r < refKeys.length; r++) {
          						let refs = item[refKeys[r]];
          						if (!Array.isArray(refs)) {
          							continue;
          						}
          						let cleanedRefs = [];
          						let localRefs = {};
          						for (let j = 0; j < refs.length; j++) {
          							let refId = refs[j] != null && refs[j].id != null ? refs[j].id : refs[j];
          							if (refId == null || byId["" + refId] == null || "" + refId == "" + item.id || localRefs["" + refId] || ownerByChildId["" + refId]) {
          								continue;
          							}
          							localRefs["" + refId] = true;
          							ownerByChildId["" + refId] = item.id;
          							cleanedRefs.push(refId);
          						}
          						item[refKeys[r]] = cleanedRefs;
          					}
          				}
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					if (item.parentRef != null && byId["" + item.parentRef] != null && ownerByChildId["" + item.id] == null) {
          						let parent = byId["" + item.parentRef];
          						if (!Array.isArray(parent.childrenRefs)) {
          							parent.childrenRefs = [];
          						}
          						parent.childrenRefs.push(item.id);
          						ownerByChildId["" + item.id] = parent.id;
          					}
          				}
          				for (let i = 0; i < obj.formulaire.length; i++) {
          					let item = obj.formulaire[i];
          					let ownerId = ownerByChildId["" + item.id];
          					if (ownerId != null && byId["" + ownerId] != null) {
          						item.parentRef = ownerId;
          						let parentPage = byId["" + ownerId].config != null ? byId["" + ownerId].config.page : null;
          						if (parentPage != null && parentPage !== "") {
          							item.config = item.config || {};
          							item.config.page = parentPage;
          						}
          					}
          					else if (item.parentRef != null) {
          						delete item.parentRef;
          					}
          					if (item.sources != null && item.id != null) {
          						for (let sourceKey of Object.keys(item.sources)) {
          							let source = item.sources[sourceKey];
          							let formsConfig = source != null && source.vars != null ? source.vars.forms_config : null;
          							if (formsConfig != null && formsConfig.str != null && formsConfig.str !== "") {
          								try {
          									let conf = JSON.parse(formsConfig.str);
          									conf.source_id = item.id;
          									formsConfig.str = JSON.stringify(conf);
          								}
          								catch (e) {
          									console.error(e);
          								}
          							}
          						}
          					}
          				}
          			};
                    // Calling sync continuous
                    page["datetimeconfig"][''fr''] = false;
                    page["datetimeconfig"][''en''] = false;
                    page["datetimeconfig"][''es''] = false;
                    
                    page["datetimeconfig"][page.global.language] = true;
                    let t = true;
                    let done_call_action = false;
                    
                    
          			let execQuery = async ()=>{
          				let doc =  (await page.c8o.callJsonObject("C8Oforms.APIV2_getDocument", {id: page.local.formId}).async()).res;
          				if(doc){
          					page["formsList"] = doc.formulaire;
          	                page["form"] = doc;
          	                page.ref.detectChanges();
                          }
          			};
          			// exec First launch
          			await execQuery();
          			
          			//migration
          			let flag = false;
                                if(page["form"]["technicalVersion"] == undefined){
          						if((page["form"] as any).pages == undefined){
          							updateHtml((page["form"] as any).formulaire); 
                                    	let pageTechName = updateToPages((page["form"] as any).formulaire);
                                    	let desc = (page["form"] as any).descform;
                                    	(page["form"] as any)["pages"] = [{name : "Page 1", pageTechName:pageTechName, desc:desc}];
          						}
          						(page["form"] as any)["technicalVersion"] = "1.0.2";
                                  flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.0") {
                                    let pageTechName = updateToPages((page["form"] as any).formulaire); 
                                    (page["form"] as any)["technicalVersion"] = "1.0.2";
                                    let desc = (page["form"] as any).descform;
                                    (page["form"] as any)["pages"] = [{name : "Page 1", pageTechName:pageTechName, desc: desc}];
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.1") {
                                    let pageTechName = updateToPages((page["form"] as any).formulaire); 
                                    (page["form"] as any)["technicalVersion"] = "1.0.2";
                                    let desc = (page["form"] as any).descform;
                                    for(let i in (page["form"] as any)["pages"]){
                                        (page["form"] as any)["pages"][i].desc = desc;
                                    }
                                    
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.2") {
                                    updateToConditions((page["form"] as any).formulaire);  
                                    (page["form"] as any)["technicalVersion"] = "1.0.3";
                                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.3") {
                                    fixGroupChildsPages((page["form"] as any).formulaire);  
                                    (page["form"] as any)["technicalVersion"] = "1.0.4";
                                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.4") {
                                    updateToSimlplified((page["form"] as any).formulaire);  
                                    (page["form"] as any)["technicalVersion"] = "1.0.5";
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.5") {
                                    addNewProperties((page["form"] as any));  
                                    (page["form"] as any)["technicalVersion"] = "1.0.6";
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.6") {
                                    addNewPropertiesLoopToForm((page["form"] as any));  
                                    (page["form"] as any)["technicalVersion"] = "1.0.7";
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.7") {
                                    addSelfVarToCandidates((page["form"] as any).formulaire);  
                                    (page["form"] as any)["technicalVersion"] = "1.0.8";
                                    flag = true;
                                }
                                if((page["form"] as any)["technicalVersion"] == "1.0.8") {
                                    addchildrenRefsToCards((page["form"] as any).formulaire);  
                                    (page["form"] as any)["technicalVersion"] = "1.0.9";
                                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.9") {
                                    addConfig((page["form"] as any));  
                                    (page["form"] as any)["technicalVersion"] = "1.0.10";
                                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.10") {
                                    addFlows((page["form"] as any));  
                                    (page["form"] as any)["technicalVersion"] = "1.0.11";
                                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.11") {
          						  	addSourceEnabledToGrid((page["form"] as any).formulaire);
          		                    (page["form"] as any)["technicalVersion"] = "1.0.12";
          		                    flag = true;
                                }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.12") {
          							addCheckMandatoryInCurrentPage((page["form"] as any).formulaire);
          							(page["form"] as any)["technicalVersion"] = "1.0.13";
          							flag = true;
          					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.13") {
            							 addCheckMandatoryInCurrentPageForTabs((page["form"] as any).pages);
            							(page["form"] as any)["technicalVersion"] = "1.0.14";
            							flag = true;
            					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.14") {
          					  		 addDescriptionInPages((page["form"] as any));
          					  		(page["form"] as any)["technicalVersion"] = "1.0.15";
          					  		flag = true;
          					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.15") {
          					  		 repairGhostComponents((page["form"] as any));
          					  		(page["form"] as any)["technicalVersion"] = "1.0.16";
          					  		flag = true;
          					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.16") {
            					  		 addDescriptionInPages((page["form"] as any));
            					  		(page["form"] as any)["technicalVersion"] = "1.0.17";
            					  		flag = true;
            					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.17") {
          				    		 addGlobalNavigationSettingsInApp((page["form"] as any));
          				    		(page["form"] as any)["technicalVersion"] = "1.0.18";
          				    		flag = true;
          					  }
            					  if((page["form"] as any)["technicalVersion"] == "1.0.18") {
            				    		 addGlobalNavigationIncludingIndividualNavigationInApp((page["form"] as any));
            				    		(page["form"] as any)["technicalVersion"] = "1.0.19";
            				    		flag = true;
            					  }
          					  if((page["form"] as any)["technicalVersion"] == "1.0.19") {
          					  	 setGlobalNavigationToTrue((page["form"] as any));
          					  	(page["form"] as any)["technicalVersion"] = "1.0.20";
          					  	flag = true;
          					  }
                                if(flag){
                                    page[''updateState'']();
                                }
          			
          			
                      // live changes
                      let p = Pouch["default"];
          			const header = {
          				"x-convertigo-sdk": page.c8o.sdkVersion,
          			};
          			if (page.c8o.xsrfUsed) {
          				const headerName = "x-xsrf-token";
          				const fetch = "Fetch";
          				header[headerName] = window.localStorage.getItem(headerName) != undefined ? window.localStorage.getItem(headerName) : fetch;
          			}
          			Object.assign(header, page.c8o.headers);
          			let remotePouchHeader = {
          				fetch: (url, opts) => {
          					opts.credentials = ''include'';
          					for (let key in header) {
          						opts.headers.set(key, header[key]);
          					}
          					var fecthPouch: Promise<Response> = p.fetch(url, opts);
          					fecthPouch.then((response) => {
          						page.c8o.session.sort(response, header, null, null, null);
          					})
          					.catch((e) => {
          
          					});
          					return fecthPouch;
          				}
          			};
          			console.log(page.c8o.fullSyncEndpoint+"/c8oforms_fs", remotePouchHeader)
          			let dbChanges = new p(page.c8o.fullSyncEndpoint+"/c8oforms_fs", remotePouchHeader);
          			page.local.live = dbChanges.changes({
          				live: true, since: ''now''
          				})
          				.on(''change'', (change)=> {
          					// if change is on current id
          					if(change.id === page["form"]["_id"]){
          						setTimeout(()=>{
          							for (let currentChange of change.changes){
          								if(page["__updateStateLocalWriteInFlight"] === true || currentChange.rev == page["__updateStateLastOwnRev"]){
          									continue;
          								}
          								if(+(currentChange.rev.split("-")[0]) > +(page["form"]["_rev"].split("-")[0])){
          									execQuery();
          									break;
          								}
          							}
          						});
          						
          					}
          				}).on(''complete'', function (info) {
          					// changes() was canceled
          				}).on(''error'', function (err) {
          					console.error("an error occured", err);
          				});
          	            // end lives changes
          			
          			
          		} catch (e) {
          			console.error("GetDOcerror", e);
          		} finally {
          			return;
          		}
          '