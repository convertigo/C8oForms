scriptContent: 
  - xmlizable: 
    - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
    - com.twinsoft.convertigo.beans.common.FormatedContent: 
      →: |
        '/*Begin_c8o_CompImport*/
        /*End_c8o_CompImport*/
        /*Begin_c8o_CompDeclaration*/
        	/*End_c8o_CompDeclaration*/
        /*Begin_c8o_CompConstructor*/
        		/*End_c8o_CompConstructor*/
        /*Begin_c8o_CompInitialization*/
        		/*End_c8o_CompInitialization*/
        /*Begin_c8o_CompFinalization*/
        		/*End_c8o_CompFinalization*/
        /*Begin_c8o_CompChanges*/
        		/*End_c8o_CompChanges*/
        /*Begin_c8o_CompDoCheck*/
        		/*End_c8o_CompDoCheck*/
        /*Begin_c8o_CompAfterContentInit*/
        		/*End_c8o_CompAfterContentInit*/
        /*Begin_c8o_CompAfterContentChecked*/
        		/*End_c8o_CompAfterContentChecked*/
        /*Begin_c8o_CompAfterViewInit*/
        		/*End_c8o_CompAfterViewInit*/
        /*Begin_c8o_CompAfterViewChecked*/
        		/*End_c8o_CompAfterViewChecked*/
        /*Begin_c8o_CompFunction*/
            private parseDropIndex(value: any): Array<number> {
                if (Array.isArray(value)) {
                    return value.map((entry) => Number(entry)).filter((entry) => Number.isFinite(entry));
                }
                if (value == null || value === "") {
                    return [];
                }
                return String(value).split(",").map((entry) => Number(entry)).filter((entry) => Number.isFinite(entry));
            }
            private getOwnerPage(): any {
                return (this as any).pageOwner || (this as any).owner || this;
            }
            private hasDragType(event: any, type: string): boolean {
                var types = event?.dataTransfer?.types;
                if (types == null) {
                    return false;
                }
                if (typeof types.includes === "function") {
                    return types.includes(type);
                }
                if (typeof types.contains === "function") {
                    return types.contains(type);
                }
                return Array.prototype.indexOf.call(types || [], type) !== -1;
            }
            private isFormsDrag(event: any): boolean {
                if (this.hasDragType(event, "__c8oformsdrag")) {
                    return true;
                }
                var ownerPage = this.getOwnerPage();
                return ownerPage?.local?.dragging === true && ownerPage?.local?.draggingActions !== true && ownerPage?.local?.draggingFlows !== true;
            }
            private isContainerFormType(type: any): boolean {
                return type === "group" || type === "ion-card" || type === "layout";
            }
            private isDraggingContainerFormElement(event: any): boolean {
                var ownerPage = this.getOwnerPage();
                if (this.isContainerFormType(ownerPage?.local?.draggingType)) {
                    return true;
                }
                return this.hasDragType(event, "group") || this.hasDragType(event, "ion-card") || this.hasDragType(event, "layout");
            }
            private getDragData(event: any, key: string): any {
                try {
                    const value = event?.dataTransfer?.getData?.(key);
                    return value == null || value === "" ? null : value;
                }
                catch (e) {
                    return null;
                }
            }
            private getDraggingType(event: any): string {
                const dataType = this.getDragData(event, "type");
                if (dataType != null) {
                    return String(dataType);
                }
                const ownerPage = this.getOwnerPage();
                if (ownerPage?.local?.draggingType != null) {
                    return String(ownerPage.local.draggingType);
                }
                return this.hasDragType(event, "layout") ? "layout" : "";
            }
            private getRelationTargetParentId(): any {
                const targetParentId = (this as any).relationTargetParentId;
                return targetParentId == null || targetParentId === "" ? null : targetParentId;
            }
            private getRelationTargetIndex(): any {
                const destination = this.parseDropIndex((this as any).i);
                if (destination.length < 2) {
                    return null;
                }
                const targetIndex = destination[destination.length - 1];
                return Number.isFinite(targetIndex) ? targetIndex : null;
            }
            private resolveRelationElement(page: any, ref: any): any {
                if (ref == null || ref === "") {
                    return null;
                }
                if (typeof page?.getElementByID === "function") {
                    try {
                        const resolved = page.getElementByID(ref);
                        if (resolved != null) {
                            return resolved;
                        }
                    }
                    catch (e) {
                    }
                }
                const refId = typeof page?.getRelationElementId === "function" ? page.getRelationElementId(ref) : (typeof ref === "object" ? ref?.id : ref);
                if (refId == null || !Array.isArray(page?.formsList)) {
                    return null;
                }
                return page.formsList.find((item: any) => "" + item?.id === "" + refId) || null;
            }
            private getRelationDraggedElementId(event: any, page: any, origin: Array<number>): any {
                const explicitId = this.getDragData(event, "relationElementId") || page?.local?.relationElementId;
                if (explicitId != null && explicitId !== "") {
                    return explicitId;
                }
                return this.getMovedFormElementId(page, origin);
            }
            public getNestedLayoutDropValidation(event?: any): any {
                const page = this.getOwnerPage();
                const draggedType = this.getDraggingType(event);
                const targetParentId = this.getRelationTargetParentId();
                const originMode = this.getDragData(event, "origin") || page?.local?.draggingOrigin;
                const operation = originMode === "new" ? "create" : (originMode === "moved" ? "move" : "");
                if (this.card !== true || draggedType !== "layout" || targetParentId == null) {
                    return { allowed: false, reason: "UNSUPPORTED_RELATION_OPERATION" };
                }
                const targetParent = this.resolveRelationElement(page, targetParentId);
                if (targetParent == null) {
                    return { allowed: false, reason: "TARGET_PARENT_NOT_FOUND", targetParentId };
                }
                if (targetParent.type !== "layout") {
                    return { allowed: false, reason: "TARGET_NOT_LAYOUT", targetParentId };
                }
                if (!Array.isArray(targetParent.childrenRefs)) {
                    return { allowed: false, reason: "INVALID_TARGET_INDEX", targetParentId };
                }
                const rawTargetIndex = this.getRelationTargetIndex();
                const targetIndex = rawTargetIndex == null ? targetParent.childrenRefs.length : Number(rawTargetIndex);
                if (!Number.isFinite(targetIndex) || targetIndex < 0 || targetIndex > targetParent.childrenRefs.length) {
                    return { allowed: false, reason: "INVALID_TARGET_INDEX", targetParentId, targetIndex: rawTargetIndex };
                }
                if (operation === "create") {
                    return { allowed: true, operation, targetParentId, targetIndex };
                }
                if (operation !== "move") {
                    return { allowed: false, reason: "UNSUPPORTED_RELATION_OPERATION", targetParentId };
                }
                const origin = this.parseDropIndex(this.getDragData(event, "index") || page?.local?.draggingOriginIndex);
                const draggedId = this.getRelationDraggedElementId(event, page, origin);
                if (draggedId == null || draggedId === "") {
                    return { allowed: false, reason: "DRAGGED_ELEMENT_NOT_FOUND", targetParentId };
                }
                const dragged = this.resolveRelationElement(page, draggedId);
                if (dragged == null) {
                    return { allowed: false, reason: "DRAGGED_ELEMENT_NOT_FOUND", draggedId, targetParentId };
                }
                if (dragged.type !== "layout") {
                    return { allowed: false, reason: "UNSUPPORTED_RELATION_OPERATION", draggedId, targetParentId };
                }
                if ("" + draggedId === "" + targetParentId) {
                    return { allowed: false, reason: "SELF_RELATION", draggedId, targetParentId };
                }
                if (typeof page?.prepareRelationMove !== "function") {
                    return { allowed: false, reason: "INVALID_RELATION_MOVE", draggedId, targetParentId };
                }
                const movePlan = page.prepareRelationMove(draggedId, targetParentId, targetIndex, page.formsList);
                if (movePlan == null || movePlan.valid !== true) {
                    return { allowed: false, reason: movePlan?.error?.code || "INVALID_RELATION_MOVE", draggedId, targetParentId, targetIndex };
                }
                return { allowed: true, operation, draggedId, targetParentId, targetIndex, movePlan };
            }
            private getRelationId(page: any, ref: any): string | null {
                if (typeof page?.getRelationElementId === "function") {
                    try {
                        return page.getRelationElementId(ref);
                    }
                    catch (e) {
                    }
                }
                if (ref == null || ref === "") {
                    return null;
                }
                if (typeof ref === "object") {
                    return ref.id == null || ref.id === "" ? null : "" + ref.id;
                }
                return "" + ref;
            }
            private buildRelationIndex(page: any, elements: any[] | null): Map<string, any> {
                if (typeof page?.buildElementIndex === "function") {
                    try {
                        return page.buildElementIndex(elements);
                    }
                    catch (e) {
                    }
                }
                const elementIndex = new Map<string, any>();
                const visitedObjects = new Set<any>();
                const visit = (items: any[] | null) => {
                    if (!Array.isArray(items)) {
                        return;
                    }
                    for (let item of items) {
                        if (item == null || typeof item !== "object" || visitedObjects.has(item)) {
                            continue;
                        }
                        visitedObjects.add(item);
                        const itemId = this.getRelationId(page, item);
                        if (itemId != null && !elementIndex.has(itemId)) {
                            elementIndex.set(itemId, item);
                        }
                        if (Array.isArray(item.children)) {
                            visit(item.children);
                        }
                    }
                };
                visit(elements);
                return elementIndex;
            }
            private sameRelationValue(page: any, left: any, right: any): boolean {
                if (left === right) {
                    return true;
                }
                const leftId = this.getRelationId(page, left);
                const rightId = this.getRelationId(page, right);
                return leftId != null && rightId != null && leftId === rightId;
            }
            private sameRelationRefs(page: any, left: any, right: any): boolean {
                if (left === right) {
                    return true;
                }
                if (!Array.isArray(left) || !Array.isArray(right) || left.length !== right.length) {
                    return false;
                }
                for (let index = 0; index < left.length; index++) {
                    if (!this.sameRelationValue(page, left[index], right[index])) {
                        return false;
                    }
                }
                return true;
            }
            private getRootTargetIndex(page: any, destinationValue: any): number {
                const destination = this.parseDropIndex(destinationValue);
                const elementsLength = Array.isArray(page?.formsList) ? page.formsList.length : 0;
                let targetIndex = Number.isFinite(destination[0]) ? destination[0] : elementsLength;
                return Math.max(0, Math.min(targetIndex, elementsLength));
            }
            private captureRelationSubtreeSnapshot(page: any, root: any, elements: any[], relationIndex: Map<string, any> | null = null): any {
                const index = relationIndex || this.buildRelationIndex(page, elements);
                const rootId = this.getRelationId(page, root);
                const entries: any[] = [];
                const visitedIds = new Set<string>();
                const visit = (item: any, isRoot: boolean) => {
                    const itemId = this.getRelationId(page, item);
                    if (item == null || typeof item !== "object" || itemId == null || visitedIds.has(itemId)) {
                        return;
                    }
                    visitedIds.add(itemId);
                    entries.push({
                        id: itemId,
                        item,
                        isRoot,
                        hadParentRef: Object.prototype.hasOwnProperty.call(item, "parentRef"),
                        parentRef: item.parentRef,
                        hadChildrenRefs: Object.prototype.hasOwnProperty.call(item, "childrenRefs"),
                        childrenRefs: Array.isArray(item.childrenRefs) ? item.childrenRefs.concat([]) : item.childrenRefs
                    });
                    if (Array.isArray(item.childrenRefs)) {
                        for (let childRef of item.childrenRefs) {
                            const childId = this.getRelationId(page, childRef);
                            const child = childId != null ? index.get(childId) : null;
                            if (child != null) {
                                visit(child, false);
                            }
                        }
                    }
                };
                visit(root, true);
                return { rootId, entries };
            }
            public prepareRelationMoveToRoot(page: any, draggedRef: any, targetIndex: any, elements: any[] | null = page?.formsList): any {
                if (!Array.isArray(elements)) {
                    return { valid: false, error: { code: "INVALID_RELATION_MODEL" } };
                }
                const draggedId = this.getRelationId(page, draggedRef);
                if (draggedId == null) {
                    return { valid: false, error: { code: "ELEMENT_NOT_FOUND" } };
                }
                const relationIndex = this.buildRelationIndex(page, elements);
                const dragged = relationIndex.get(draggedId);
                if (dragged == null || dragged.type !== "layout") {
                    return { valid: false, error: { code: "ELEMENT_NOT_FOUND", elementId: draggedId } };
                }
                const flatOccurrences = elements.filter((item: any) => this.getRelationId(page, item) === draggedId).length;
                if (flatOccurrences !== 1) {
                    return { valid: false, error: { code: "FLAT_OCCURRENCE_INVALID", elementId: draggedId, occurrences: flatOccurrences } };
                }
                const sourceIndex = elements.findIndex((item: any) => this.getRelationId(page, item) === draggedId);
                const sourceParentId = this.getRelationId(page, dragged.parentRef);
                const parentsContainingChild: string[] = [];
                const originalChildrenRefsByParent: any[] = [];
                for (let parent of elements) {
                    const parentId = this.getRelationId(page, parent);
                    if (parentId == null || !Array.isArray(parent?.childrenRefs)) {
                        continue;
                    }
                    if (parent.childrenRefs.some((ref: any) => this.getRelationId(page, ref) === draggedId)) {
                        parentsContainingChild.push(parentId);
                        originalChildrenRefsByParent.push({
                            parent,
                            parentId,
                            hadChildrenRefs: Object.prototype.hasOwnProperty.call(parent, "childrenRefs"),
                            childrenRefs: parent.childrenRefs.concat([])
                        });
                    }
                }
                let requestedTargetIndex = Number(targetIndex);
                if (!Number.isFinite(requestedTargetIndex)) {
                    requestedTargetIndex = elements.length;
                }
                requestedTargetIndex = Math.max(0, Math.min(requestedTargetIndex, elements.length));
                let normalizedTargetIndex = requestedTargetIndex;
                if (sourceIndex > -1 && sourceIndex < normalizedTargetIndex) {
                    normalizedTargetIndex--;
                }
                normalizedTargetIndex = Math.max(0, Math.min(normalizedTargetIndex, Math.max(0, elements.length - 1)));
                const subtreeSnapshot = this.captureRelationSubtreeSnapshot(page, dragged, elements, relationIndex);
                return {
                    valid: true,
                    dragged,
                    draggedId,
                    sourceIndex,
                    sourceParentId,
                    targetKind: "root",
                    requestedTargetIndex,
                    targetIndex: normalizedTargetIndex,
                    parentIdsToDetachFrom: Array.from(new Set(parentsContainingChild)),
                    originalChildrenRefsByParent,
                    originalFlatOrder: elements.concat([]),
                    hadParentRef: Object.prototype.hasOwnProperty.call(dragged, "parentRef"),
                    parentRef: dragged.parentRef,
                    subtreeSnapshot,
                    normalizedContradiction: sourceParentId != null && parentsContainingChild.indexOf(sourceParentId) === -1
                };
            }
            private restoreRelationMoveToRoot(plan: any, elements: any[]): void {
                if (Array.isArray(plan?.originalFlatOrder)) {
                    elements.splice(0, elements.length, ...plan.originalFlatOrder);
                }
                for (let snapshot of plan?.originalChildrenRefsByParent || []) {
                    if (snapshot.parent == null) {
                        continue;
                    }
                    if (snapshot.hadChildrenRefs) {
                        if (!Array.isArray(snapshot.parent.childrenRefs)) {
                            snapshot.parent.childrenRefs = [];
                        }
                        snapshot.parent.childrenRefs.splice(0, snapshot.parent.childrenRefs.length, ...snapshot.childrenRefs);
                    }
                    else {
                        delete snapshot.parent.childrenRefs;
                    }
                }
                if (plan?.dragged != null) {
                    if (plan.hadParentRef) {
                        plan.dragged.parentRef = plan.parentRef;
                    }
                    else {
                        delete plan.dragged.parentRef;
                    }
                }
                for (let entry of plan?.subtreeSnapshot?.entries || []) {
                    if (entry.item == null) {
                        continue;
                    }
                    if (entry.hadParentRef) {
                        entry.item.parentRef = entry.parentRef;
                    }
                    else {
                        delete entry.item.parentRef;
                    }
                    if (entry.hadChildrenRefs) {
                        if (!Array.isArray(entry.item.childrenRefs)) {
                            entry.item.childrenRefs = [];
                        }
                        entry.item.childrenRefs.splice(0, entry.item.childrenRefs.length, ...entry.childrenRefs);
                    }
                    else {
                        delete entry.item.childrenRefs;
                    }
                }
            }
            public validateRelationMoveToRootResult(page: any, plan: any, elements: any[] | null = page?.formsList): any {
                const diagnostic: any = { valid: true, errors: [], warnings: [] };
                const addError = (code: string, details: any = {}) => {
                    diagnostic.valid = false;
                    diagnostic.errors.push({ code, ...details });
                };
                if (!Array.isArray(elements) || plan == null) {
                    addError("INVALID_RELATION_MODEL");
                    return diagnostic;
                }
                const relationIndex = this.buildRelationIndex(page, elements);
                const dragged = relationIndex.get(plan.draggedId);
                if (dragged == null) {
                    addError("ELEMENT_NOT_FOUND", { elementId: plan.draggedId });
                    return diagnostic;
                }
                const draggedOccurrences = elements.filter((item: any) => this.getRelationId(page, item) === plan.draggedId).length;
                if (draggedOccurrences !== 1) {
                    addError("FLAT_OCCURRENCE_INVALID", { elementId: plan.draggedId, occurrences: draggedOccurrences });
                }
                if (this.getRelationId(page, dragged.parentRef) != null) {
                    addError("ROOT_PARENT_REF_NOT_CLEARED", { elementId: plan.draggedId, parentRef: this.getRelationId(page, dragged.parentRef) });
                }
                for (let parent of elements) {
                    const parentId = this.getRelationId(page, parent);
                    if (parentId == null || !Array.isArray(parent?.childrenRefs)) {
                        continue;
                    }
                    if (parent.childrenRefs.some((ref: any) => this.getRelationId(page, ref) === plan.draggedId)) {
                        addError("ROOT_CHILD_STILL_REFERENCED_BY_PARENT", { elementId: plan.draggedId, parentId });
                    }
                }
                for (let entry of plan.subtreeSnapshot?.entries || []) {
                    const current = relationIndex.get(entry.id);
                    if (current == null) {
                        addError("SUBTREE_ELEMENT_MISSING", { elementId: entry.id });
                        continue;
                    }
                    const occurrences = elements.filter((item: any) => this.getRelationId(page, item) === entry.id).length;
                    if (occurrences !== 1) {
                        addError("SUBTREE_FLAT_OCCURRENCE_INVALID", { elementId: entry.id, occurrences });
                    }
                    if (!this.sameRelationRefs(page, entry.childrenRefs, current.childrenRefs)) {
                        addError("SUBTREE_CHILDREN_REFS_CHANGED", { elementId: entry.id });
                    }
                    if (entry.isRoot !== true) {
                        if (!this.sameRelationValue(page, entry.parentRef, current.parentRef)) {
                            addError("DESCENDANT_PARENT_REF_CHANGED", { elementId: entry.id });
                        }
                        if (this.getRelationId(page, current.parentRef) == null) {
                            addError("DESCENDANT_BECAME_ROOT", { elementId: entry.id });
                        }
                    }
                }
                if (typeof page?.hasRelationCycle === "function") {
                    try {
                        if (page.hasRelationCycle(plan.draggedId, elements, relationIndex)) {
                            addError("RELATION_CYCLE", { elementId: plan.draggedId });
                        }
                    }
                    catch (e) {
                    }
                }
                return diagnostic;
            }
            public moveRelationElementToRoot(page: any, draggedRef: any, targetIndex: any, elements: any[] | null = page?.formsList, options: any = null): any {
                const plan = this.prepareRelationMoveToRoot(page, draggedRef, targetIndex, elements);
                if (plan == null || plan.valid !== true) {
                    return { success: false, error: plan?.error || { code: "INVALID_RELATION_MOVE_TO_ROOT" }, rolledBack: false };
                }
                try {
                    for (let snapshot of plan.originalChildrenRefsByParent) {
                        if (Array.isArray(snapshot.parent?.childrenRefs)) {
                            const keptRefs = snapshot.parent.childrenRefs.filter((ref: any) => this.getRelationId(page, ref) !== plan.draggedId);
                            snapshot.parent.childrenRefs.splice(0, snapshot.parent.childrenRefs.length, ...keptRefs);
                        }
                    }
                    if (options?.forceFailureAfterDetach === true) {
                        throw { code: "FORCED_ROLLBACK" };
                    }
                    const currentIndex = elements.findIndex((item: any) => this.getRelationId(page, item) === plan.draggedId);
                    if (currentIndex < 0) {
                        throw { code: "ELEMENT_NOT_FOUND", elementId: plan.draggedId };
                    }
                    elements.splice(currentIndex, 1);
                    if (plan.hadParentRef) {
                        delete plan.dragged.parentRef;
                    }
                    else {
                        delete plan.dragged.parentRef;
                    }
                    const insertionIndex = Math.max(0, Math.min(plan.targetIndex, elements.length));
                    elements.splice(insertionIndex, 0, plan.dragged);
                    const postValidation = this.validateRelationMoveToRootResult(page, plan, elements);
                    if (postValidation.valid !== true) {
                        throw { code: "POST_MUTATION_INTEGRITY_FAILURE", diagnostic: postValidation };
                    }
                    return {
                        success: true,
                        movedId: plan.draggedId,
                        sourceParentId: plan.sourceParentId,
                        targetKind: "root",
                        targetIndex: insertionIndex,
                        normalizedContradiction: plan.normalizedContradiction,
                        diagnostic: postValidation
                    };
                }
                catch (e) {
                    this.restoreRelationMoveToRoot(plan, elements);
                    return {
                        success: false,
                        error: {
                            code: e?.code || "RELATION_MOVE_TO_ROOT_FAILED",
                            diagnostic: e?.diagnostic
                        },
                        rolledBack: true
                    };
                }
            }
            public getRootLayoutDropValidation(event?: any, destinationValue: any = null): any {
                const page = this.getOwnerPage();
                const draggedType = this.getDraggingType(event);
                const originMode = this.getDragData(event, "origin") || page?.local?.draggingOrigin;
                if (this.card === true || page?.action === true || page?.local?.flow === true || draggedType !== "layout" || originMode !== "moved") {
                    return { allowed: false, reason: "UNSUPPORTED_RELATION_OPERATION" };
                }
                const origin = this.parseDropIndex(this.getDragData(event, "index") || page?.local?.draggingOriginIndex);
                const draggedId = this.getRelationDraggedElementId(event, page, origin);
                if (draggedId == null || draggedId === "") {
                    return { allowed: false, reason: "DRAGGED_ELEMENT_NOT_FOUND" };
                }
                const dragged = this.resolveRelationElement(page, draggedId);
                if (dragged == null || dragged.type !== "layout") {
                    return { allowed: false, reason: "DRAGGED_ELEMENT_NOT_FOUND", draggedId };
                }
                const requestedTargetIndex = this.getRootTargetIndex(page, destinationValue == null ? (this as any).i : destinationValue);
                const movePlan = this.prepareRelationMoveToRoot(page, draggedId, requestedTargetIndex, page.formsList);
                if (movePlan == null || movePlan.valid !== true) {
                    return { allowed: false, reason: movePlan?.error?.code || "INVALID_RELATION_MOVE_TO_ROOT", draggedId, targetKind: "root" };
                }
                return { allowed: true, operation: "move", targetKind: "root", draggedId, requestedTargetIndex, targetIndex: movePlan.targetIndex, movePlan };
            }
            public isNestedLayoutDropAllowed(event?: any): boolean {
                return this.getNestedLayoutDropValidation(event)?.allowed === true;
            }
            public canAcceptDrop(event?: any): boolean {
                if (this.card === true && this.isFormsDrag(event) && this.isDraggingContainerFormElement(event)) {
                    return this.isNestedLayoutDropAllowed(event);
                }
                return true;
            }
        
        	private getCardChildrenLength(page: any, parentIndex: number): number {
        		const targetCardChild = this.targetCardChild || "childrenRefs";
        		const formItem = page?.formsList?.[parentIndex];
        		const flowItem = page?.form?.flows?.[page?.local?.currentIndex]?.elements?.[parentIndex];
        		const children = formItem?.[targetCardChild] ?? formItem?.childrenRefs ?? formItem?.children ?? flowItem?.[targetCardChild] ?? flowItem?.childrenRefs ?? flowItem?.children ?? [];
        		return Array.isArray(children) ? children.length : 0;
        	}
        
        	private normalizeDestinationIndex(page: any, destination: Array<number>, origin: Array<number>): Array<number> {
        		const normalized = [...destination];
        		if (this.card === true && normalized.length < origin.length && normalized.length > 0) {
        			const length = this.getCardChildrenLength(page, normalized[0]);
        			normalized.push(length);
        		}
        		return normalized;
        	}
        
        	private getMovedFormElementId(page: any, origin: Array<number>): any {
        		if (!Array.isArray(origin) || !Number.isFinite(origin[0])) {
        			return null;
        		}
        		if (Number.isFinite(origin[1])) {
        			const parent = page?.formsList?.[origin[0]];
        			return parent?.childrenRefs?.[origin[1]] ?? parent?.children?.[origin[1]]?.id ?? null;
        		}
        		return page?.formsList?.[origin[0]]?.id ?? null;
        	}
        
        	private isFormElementOrDescendant(page: any, candidate: any, movedId: any): boolean {
        		if (candidate == null || movedId == null) {
        			return false;
        		}
        		let current = candidate;
        		let guard = 0;
        		while (current != null && guard < 1000) {
        			if ("" + current.id === "" + movedId) {
        				return true;
        			}
        			if (current.parentRef == null || current.parentRef === "") {
        				return false;
        			}
        			current = page?.formsList?.find((item: any) => "" + item?.id === "" + current.parentRef);
        			guard++;
        		}
        		return false;
        	}
        
        	private isDropInsideMovedFormElement(page: any, event: any, destinationValue: any): boolean {
        		const dataTransfer = event?.dataTransfer;
        		const types = dataTransfer?.types;
        		const isFormsDrag = typeof types?.includes === "function" ? types.includes("__c8oformsdrag") : (typeof types?.contains === "function" ? types.contains("__c8oformsdrag") : Array.prototype.indexOf.call(types || [], "__c8oformsdrag") !== -1);
        		if (dataTransfer == null || dataTransfer.getData("origin") !== "moved" || !isFormsDrag) {
        			return false;
        		}
        		const origin = this.parseDropIndex(dataTransfer.getData("index"));
        		const destination = this.parseDropIndex(destinationValue);
        		if (!Array.isArray(origin) || !Array.isArray(destination) || destination.length < 2 || !Number.isFinite(destination[0])) {
        			return false;
        		}
        		const movedId = this.getMovedFormElementId(page, origin);
        		const destinationParent = page?.formsList?.[destination[0]];
        		return this.isFormElementOrDescendant(page, destinationParent, movedId);
        	}
        
        	private isEquivalentMoveDrop(event: any): boolean {
        		const dataTransfer = event?.dataTransfer;
        		if (dataTransfer == null || dataTransfer.getData("origin") !== "moved") {
        			this.setEquivalentMoveDrop(false);
        			return false;
        		}
        
        		const origin = this.parseDropIndex(dataTransfer.getData("index"));
        		const destination = this.normalizeDestinationIndex(this.pageOwner || this.owner || this, this.parseDropIndex(this.i), origin);
        		if (origin.length === 0 || destination.length === 0 || origin.length !== destination.length) {
        			this.setEquivalentMoveDrop(false);
        			return false;
        		}
        
        		const sameParent = origin.slice(0, -1).every((entry, index) => entry === destination[index]);
        		const originPosition = origin[origin.length - 1];
        		const destinationPosition = destination[destination.length - 1];
        		const equivalent = sameParent && (destinationPosition === originPosition || destinationPosition === originPosition + 1);
        		this.setEquivalentMoveDrop(equivalent);
        		return equivalent;
        	}
        
        	private isEquivalentMoveIndex(origin: Array<number>): boolean {
        		const page = this.pageOwner || this.owner || this;
        		const destination = this.normalizeDestinationIndex(page, this.parseDropIndex(this.i), origin);
        		if (origin.length === 0 || destination.length === 0 || origin.length !== destination.length) {
        			return false;
        		}
        		const sameParent = origin.slice(0, -1).every((entry, index) => entry === destination[index]);
        		const originPosition = origin[origin.length - 1];
        		const destinationPosition = destination[destination.length - 1];
        		return sameParent && (destinationPosition === originPosition || destinationPosition === originPosition + 1);
        	}
        
        	private setEquivalentMoveDrop(value: boolean): void {
        		if (this.local != null) {
        			this.local.skipEquivalentMoveDrop = value;
        			if (value === true) {
        				this.local.isOver = false;
        			}
        		}
        	}
        
        	public shouldSkipEquivalentMoveDrop(event: any): boolean {
        		const skip = this.isEquivalentMoveDrop(event);
        		if (skip === true) {
        			event?.preventDefault?.();
        			event?.stopPropagation?.();
        		}
        		return skip;
        	}
        
        	public shouldSkipInvalidFormMoveDrop(event: any): boolean {
        		const page = this.pageOwner || this.owner || this;
        		const skip = this.isDropInsideMovedFormElement(page, event, this.i);
        		if (skip === true) {
        			event?.preventDefault?.();
        			event?.stopPropagation?.();
        			this.setEquivalentMoveDrop(false);
        			if (event?.type === "drop" && typeof page?.["cleanupDragState"] === "function") {
        				page["cleanupDragState"]("sharedDropIndicator.invalid-self-drop");
        			}
        		}
        		return skip;
        	}
        
        	public shouldHideEquivalentMoveDrop(): boolean {
        		
        		return this.local?.skipEquivalentMoveDrop === true;
        //		const page = this.pageOwner || this.owner || this;
        //		const local = page?.local;
        //		if (local?.draggingOrigin !== "moved") {
        //			return this.local?.skipEquivalentMoveDrop === true;
        //		}
        //		return this.local?.skipEquivalentMoveDrop === true || this.isEquivalentMoveIndex(this.parseDropIndex(local.draggingOriginIndex));
        	}
        	/*End_c8o_CompFunction*/
        '
↓id [ngx.components.UICompVariable-1600442603219]: 
↓i [ngx.components.UICompVariable-1600442609749]: 
↓card [ngx.components.UICompVariable-1600443852860]: 
  value: false
↓action [ngx.components.UICompVariable-1634662823607]: 
  value: false
↓hideText [ngx.components.UICompVariable-1730726285709]: 
  value: false
↓Div1 [ngx.components.UIDynamicElement-1730726809426]: 
  beanData: '{"ionBean":"DivTag"}'
  tagName: div
  ↓_ngStyle_ [ngx.components.UIAttribute-1730726443417]: 
    attrName: '[ngStyle]'
    attrValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: 'script:{''display'': this.canAcceptDrop() ? null : ''none'', ''padding-top'': this.hideText ? ''10px'' : ''0'', ''padding-bottom'': this.hideText ? ''10px'' : ''0''}'
  ↓Div [ngx.components.UIDynamicElement-1600440331787]: 
    beanData: '{"ionBean":"DivTag"}'
    tagName: div
    ↓Style [ngx.components.UIStyle-1600440331799]: 
      styleContent: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
          - com.twinsoft.convertigo.beans.common.FormatedContent: 
            →: |
              'border-color: var(--ion-color-primary);
              border-style: dashed;
              height:100%;
              text-align:center;
              color: var(--ion-color-primary);
              background-color: rgba(var(--ion-color-primary-rgb), 0.1);
              border-width: 2px;
              border-radius: 10px !important;
              height: 50px;
              display: flex;
              align-items: center;
              justify-content: center;
              margin: 5px 0px;
              transition: 300ms linear;
              font-weight: 700;
              '
    ↓Event [ngx.components.UIControlEvent-1600442667546]: 
      attrName: (dragover)
      eventName: (dragover)
      ↓SetLocal [ngx.components.UIDynamicAction-1775210288804]: 
        beanData: |
          '{
           "ionBean": "SetLocalAction",
           "Property": "plain:isOver",
           "Value": "script:true"
          }'
        ↓allowDrop [ngx.components.UICustomAction-1600442667549]: 
          actionValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
              - com.twinsoft.convertigo.beans.common.FormatedContent: 
                →: |
                          if((this as any).canAcceptDrop?.(event) === false){
                              if ((page as any).local != null) {
                                  (page as any).local.isOver = false;
                              }
                              if (event?.dataTransfer != null) {
                                  event.dataTransfer.dropEffect = "none";
                              }
                              resolve();
                              return;
                          }
                          if((this as any).shouldSkipEquivalentMoveDrop?.(event) || (this as any).shouldSkipInvalidFormMoveDrop?.(event)){
                              resolve();
                              return;
                          }
                  		if((page as any).pageOwner != undefined){
                  			page = (page as any).pageOwner;
                  		}
                  		if(vars.card == true){
                  		    if(event.dataTransfer.types.includes("__c8oformsdrag")){
                                  if(event.dataTransfer.types.includes("group")){
                                      //page.c8o.log.debug("dropping  card into another card is not authorized");
                                  }
                                  else if(event.dataTransfer.types.includes("ion-card") || event.dataTransfer.types.includes("layout")){
                                      if(event.dataTransfer.types.includes("layout") && (this as any).isNestedLayoutDropAllowed?.(event) === true){
                                          event.preventDefault();
                                          page["recursive"](event.target, "before");
                                      }
                                  }
                                  else{
                                      event.preventDefault();
                                      page["recursive"](event.target, "before");
                                  }
                              }
                  			if(vars.flow && event.dataTransfer.types.includes("__c8oformsdragactions") || event.dataTransfer.types.includes("__c8oformsdragflows")){
                  				if(vars.for_loop || vars.if_else){
                  
                  				}
                  				else{
                  					event.preventDefault();
                  					page["recursive"](event.target, "before");
                  				}
                  			}
                  
                  		}
                  		else if(vars.formulas){
                  			if(event.dataTransfer.types.includes("__c8oformsdragactions") || event.dataTransfer.types.includes("__c8oformsdragflows")){
                  				//only business logic is authorized here
                  				if(event.dataTransfer.types.includes("business_logic")){
                  					event.preventDefault();
                  					page["recursive"](event.target, "before");
                                 }
                  			}
                  		}
                  		else{
                  		    if(event.dataTransfer.types.includes("__c8oformsdrag")){
                  	            event.preventDefault();
                  	            page["recursive"](event.target, "before");
                  	        }
                  			else if(event.dataTransfer.types.includes("__c8oformsdragactions")){
                  				event.preventDefault();
                  				page["recursive"](event.target, "before");
                  			}
                  			else if(event.dataTransfer.types.includes("__c8oformsdragflows")){
                  				event.preventDefault();
                  				page["recursive"](event.target, "before");
                  			}
                  		}
                  		resolve();
                  
          ↓card [ngx.components.UIControlVariable-1600443970442]: 
            varValue: 
              - xmlizable: 
                - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
                - MobileSmartSourceType: script:this.card
          ↓flow [ngx.components.UIControlVariable-1743158133593]: 
            varValue: 
              - xmlizable: 
                - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
                - MobileSmartSourceType: script:this.flow
          ↓formulas [ngx.components.UIControlVariable-1743685695983]: 
            varValue: 
              - xmlizable: 
                - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
                - MobileSmartSourceType: script:this.formulas
          ↓for_loop [ngx.components.UIControlVariable-1743685699329]: 
            varValue: 
              - xmlizable: 
                - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
                - MobileSmartSourceType: script:this.for_loop
          ↓if_else [ngx.components.UIControlVariable-1743685702544]: 
            varValue: 
              - xmlizable: 
                - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
                - MobileSmartSourceType: script:this.if_else
    ↓Event1 [ngx.components.UIControlEvent-1600442667552]: 
      attrName: (drop)
      eventName: (drop)
      ↓drop [ngx.components.UICustomAction-1600442667555]: 
        actionValue: 
          - xmlizable: 
            - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
            - com.twinsoft.convertigo.beans.common.FormatedContent: 
              →: |
                '        console.log(vars)
                        if((this as any).canAcceptDrop?.(event) === false){
                            const ownerPage: any = (page as any).pageOwner != undefined ? (page as any).pageOwner : page;
                            if (event?.dataTransfer != null) {
                                event.dataTransfer.dropEffect = "none";
                            }
                            if (typeof ownerPage["cleanupDragState"] === "function") {
                				ownerPage["cleanupDragState"]("sharedDropIndicator.drop.invalid");
                			}
                            resolve();
                            return;
                        }
                        if((this as any).shouldSkipEquivalentMoveDrop?.(event) || (this as any).shouldSkipInvalidFormMoveDrop?.(event)){
                            const ownerPage: any = (page as any).pageOwner != undefined ? (page as any).pageOwner : page;
                            if (typeof ownerPage["cleanupDragState"] === "function") {
                				ownerPage["cleanupDragState"]("sharedDropIndicator.drop.equivalent");
                			}
                			resolve();
                			return;
                		}
                		if((page as any).pageOwner != undefined){
                			page = (page as any).pageOwner;
                		}
                		try{
                			console.log("[DnD] drop target path", {
                				pageAction: page.action,
                				pageFlow: page.local?.flow,
                				currentPage: page.local?.currentPage,
                				currentIndex: page.local?.currentIndex,
                				index: vars.index,
                				card: vars.card,
                				targetCardChild: vars.targetCardChild
                			});
                			if(event.dataTransfer.types.includes("__c8oformsdrag")){
                				event.preventDefault();
                				// get origin coordinates
                				var arrayOrigin: Array<number> = event.dataTransfer.getData("index").split(",").map((e)=>{return +e});
                				// get destination coordinates
                				var arrayDestination: Array<number> = Array.isArray(vars.index) ? [...vars.index] : String(vars.index).split(",").map((e)=>{return +e});
                				if(vars.card && arrayDestination.length < 2){
                					let len = page["formsList"]?.[vars.index]?.["childrenRefs"]?.length ?? page["formsList"][vars.index]["children"].length;
                					arrayDestination.push(len)
                				}
                				const TypeOrigin = event.dataTransfer.getData("origin");
                				const nestedLayoutValidation = (this as any).getNestedLayoutDropValidation?.(event);
                				const rootLayoutValidation = nestedLayoutValidation?.allowed === true ? null : (this as any).getRootLayoutDropValidation?.(event, arrayDestination);
                				console.log(TypeOrigin)
                				if(nestedLayoutValidation?.allowed === true){
                					switch(TypeOrigin){
                						case "new":
                							const relationIdOf = (value: any) => typeof value === "object" ? value?.id : value;
                							const beforeIds = new Set((page["formsList"] || []).map((item: any) => "" + relationIdOf(item)).filter((id: any) => id !== "" && id !== "undefined" && id !== "null"));
                							page["addElement"](event.dataTransfer.getData("type"), arrayDestination);
                							const created = (page["formsList"] || []).find((item: any) => {
                								const itemId = relationIdOf(item);
                								return itemId != null && !beforeIds.has("" + itemId);
                							});
                							const createdId = relationIdOf(created);
                							const targetParent = (page["formsList"] || []).find((item: any) => "" + relationIdOf(item) === "" + nestedLayoutValidation.targetParentId);
                							const targetRefs = Array.isArray(targetParent?.childrenRefs) ? targetParent.childrenRefs : [];
                							const createdOccurrences = targetRefs.filter((ref: any) => "" + relationIdOf(ref) === "" + createdId).length;
                							const creationValid = created != null && created.type === "layout" && "" + relationIdOf(created.parentRef) === "" + nestedLayoutValidation.targetParentId && createdOccurrences === 1;
                							if(creationValid !== true){
                								if(createdId != null){
                									for(const candidate of (page["formsList"] || [])){
                										if(Array.isArray(candidate?.childrenRefs)){
                											candidate.childrenRefs.splice(0, candidate.childrenRefs.length, ...candidate.childrenRefs.filter((ref: any) => "" + relationIdOf(ref) !== "" + createdId));
                										}
                									}
                									const createdIndex = (page["formsList"] || []).findIndex((item: any) => "" + relationIdOf(item) === "" + createdId);
                									if(createdIndex > -1){
                										page["formsList"].splice(createdIndex, 1);
                									}
                								}
                								console.warn("[DnD] nested layout creation rolled back", { createdId, targetParentId: nestedLayoutValidation.targetParentId });
                								if(typeof page["updateState"] === "function"){
                									page["updateState"]();
                								}
                							}
                						break;
                						case "moved":
                							const relationResult = page["moveRelationElement"](nestedLayoutValidation.draggedId, nestedLayoutValidation.targetParentId, nestedLayoutValidation.targetIndex);
                							if(relationResult?.success === true){
                								page["updateState"]();
                							}
                							else{
                								console.warn("[DnD] nested layout move rejected", relationResult);
                							}
                						break;
                					}
                				}
                				else if(rootLayoutValidation?.allowed === true){
                					switch(TypeOrigin){
                						case "moved":
                							const rootRelationResult = (this as any).moveRelationElementToRoot(page, rootLayoutValidation.draggedId, rootLayoutValidation.requestedTargetIndex);
                							if(rootRelationResult?.success === true){
                								page["updateState"]();
                							}
                							else{
                								console.warn("[DnD] root layout move rejected", rootRelationResult);
                							}
                						break;
                					}
                				}
                				else{
                					switch(TypeOrigin){
                						case "new":
                							page["addElement"](event.dataTransfer.getData("type"), arrayDestination);
                						break;
                						case "moved":
                							page["moveElement"](arrayDestination, arrayOrigin);
                						break;
                					}
                				}
                			}
                			else if(page.action == true) {
                				event.preventDefault();
                				// get origin coordinates
                				var arrayOrigin: Array<number> = event.dataTransfer.getData("index").split(",").map((e)=>{return +e});
                				// get destination coordinates
                				var arrayDestination: Array<number> = Array.isArray(vars.index) ? [...vars.index] : String(vars.index).split(",").map((e)=>{return +e});
                				if(vars.card && arrayDestination.length < 2){
                					arrayDestination.push(page["formsList"][vars.index]["children"].length)
                				}
                				const TypeOrigin = event.dataTransfer.getData("origin");
                				switch(TypeOrigin){
                					case "new":
                						page["addElement"](event.dataTransfer.getData("type"), arrayDestination, false, true);
                					break;
                					case "moved":
                						page["moveActionElement"](arrayDestination, arrayOrigin);
                					break;
                				}
                
                			}
                			else if(page.local.flow == true) {
                				// get meta
                				let elems = null;
                				try{
                					if(event.dataTransfer.getData("custom") == "true"){
                						elems = JSON.parse(event.dataTransfer.getData("elems"));
                					}
                				}
                				catch(e){
                					console.log("error datatransfert", e);
                				}
                				const normalizeRefId = (ref: any) => typeof ref === "object" ? ref?.id : ref;
                				const resolveFlowOriginContext = () => {
                					const rawOriginFlowIndex =
                						event.dataTransfer.getData("indexoriginflow") ||
                						event.dataTransfer.getData("indexOriginFlow") ||
                						event.dataTransfer.getData("originFlowIndex");
                					const originFlowIndex = Number(rawOriginFlowIndex);
                					const originElementId = String(event.dataTransfer.getData("originElementId") || "");
                					const rawIndex = event.dataTransfer.getData("index");
                					let arrayOrigin: Array<number> = rawIndex != null && rawIndex !== "" ? rawIndex.split(",").map((e)=>{return +e}) : [];
                					let resolvedOriginCardChildOrigin = event.dataTransfer.getData("originCardChildOrigin") != null && event.dataTransfer.getData("originCardChildOrigin") != '''' ? event.dataTransfer.getData("originCardChildOrigin") : "childrenRefs";
                					if (!Number.isFinite(originFlowIndex)) {
                						console.error("[drop] invalid origin flow index", {
                							rawOriginFlowIndex,
                							currentIndex: page.local.currentIndex,
                							index: rawIndex,
                							origin: event.dataTransfer.getData("origin"),
                							type: event.dataTransfer.getData("type"),
                							types: Array.from(event.dataTransfer.types || [])
                						});
                						return null;
                					}
                					if ((!Array.isArray(arrayOrigin) || !Number.isFinite(arrayOrigin[0])) && originElementId !== "") {
                						const sourceElements = (page.form as any)?.flows?.[originFlowIndex]?.elements;
                						if (Array.isArray(sourceElements)) {
                							const rootIndex = sourceElements.findIndex((element: any) => {
                								return "" + element?.id === originElementId;
                							});
                							if (rootIndex > -1) {
                								arrayOrigin = [rootIndex];
                								console.warn("[drop] recovered origin index from originElementId", {
                									originElementId,
                									sourceFlowIndex: originFlowIndex,
                									arrayOrigin
                								});
                							}
                							else {
                								outerLoop:
                								for (let parentIndex = 0; parentIndex < sourceElements.length; parentIndex++) {
                									const parent = sourceElements[parentIndex];
                									for (let refKey of ["childrenRefs", "childrenRefsElse"]) {
                										const refs = parent?.[refKey];
                										if (!Array.isArray(refs)) {
                											continue;
                										}
                										const childIndex = refs.findIndex((ref: any) => {
                											const refId = normalizeRefId(ref);
                											return "" + refId === originElementId;
                										});
                										if (childIndex > -1) {
                											arrayOrigin = [parentIndex, childIndex];
                											resolvedOriginCardChildOrigin = refKey;
                											console.warn("[drop] recovered origin index from originElementId", {
                												originElementId,
                												sourceFlowIndex: originFlowIndex,
                												arrayOrigin
                											});
                											break outerLoop;
                										}
                									}
                								}
                							}
                						}
                					}
                					if (!Array.isArray(arrayOrigin) || !Number.isFinite(arrayOrigin[0])) {
                						console.error("[drop] invalid origin index", {
                							rawIndex,
                							originElementId,
                							originFlowIndex,
                							currentIndex: page.local.currentIndex
                						});
                						return null;
                					}
                					return {
                						arrayOrigin,
                						originElementId,
                						originFlowIndex,
                						originCardChildOrigin: resolvedOriginCardChildOrigin
                					};
                				};
                				// within a foor_loop or if_else
                				if(vars.card){
                					event.preventDefault();
                					// get destination coordinates
                				var arrayDestination: Array<number> = Array.isArray(vars.index) ? [...vars.index] : String(vars.index).split(",").map((e)=>{return +e});
                					const TypeOrigin = event.dataTransfer.getData("origin");
                					if(vars.card && arrayDestination.length < 2){
                						let len = page.form["flows"]?.[page.local.currentIndex]?.elements?.[vars.index]?.[vars.targetCardChild]?.length;
                						if(TypeOrigin == "moved"){
                							len ++;
                						}
                						arrayDestination.push(len)
                					}
                					vars.targetCardChild = vars.targetCardChild != null && vars.targetCardChild != '''' ? vars.targetCardChild : "childrenRefs";
                					vars.originCardChildOrigin = event.dataTransfer.getData("originCardChildOrigin") != null && event.dataTransfer.getData("originCardChildOrigin") != '''' ? event.dataTransfer.getData("originCardChildOrigin") : "childrenRefs";
                					switch(TypeOrigin){
                						case "new":
                							page["addElement"](event.dataTransfer.getData("type"), arrayDestination, false, false, true, vars.targetCardChild, elems);
                						break;
                						case "moved":
                							const originContext = resolveFlowOriginContext();
                							if (originContext == null) {
                								resolve();
                								return;
                							}
                							var arrayOrigin: Array<number> = originContext.arrayOrigin;
                							vars.originFlowIndex = originContext.originFlowIndex;
                							vars.originCardChildOrigin = originContext.originCardChildOrigin;
                							console.log("[drop] moveActionElement", {
                								arrayOrigin,
                								arrayDestination,
                								destinationFlowIndex: page.local.currentIndex,
                								originFlowIndex: vars.originFlowIndex,
                								originElementId: originContext.originElementId,
                								targetCardChild: vars.targetCardChild,
                								originCardChildOrigin: vars.originCardChildOrigin
                							});
                							page["moveActionElement"](arrayDestination, arrayOrigin, page.local.currentIndex, vars.originFlowIndex, vars.targetCardChild, vars.originCardChildOrigin);
                						break;
                					}
                				}
                				else{
                					event.preventDefault();
                					// get destination coordinates
                				var arrayDestination: Array<number> = Array.isArray(vars.index) ? [...vars.index] : String(vars.index).split(",").map((e)=>{return +e});
                					const TypeOrigin = event.dataTransfer.getData("origin");
                					vars.originCardChildOrigin = event.dataTransfer.getData("originCardChildOrigin") != null && event.dataTransfer.getData("originCardChildOrigin") != '''' ? event.dataTransfer.getData("originCardChildOrigin") : "childrenRefs";
                					switch(TypeOrigin){
                						case "new":
                							page["addElement"](event.dataTransfer.getData("type"), arrayDestination, false, false, true, ''childrenRefs'', elems);
                						break;
                						case "moved":
                							const originContext = resolveFlowOriginContext();
                							if (originContext == null) {
                								resolve();
                								return;
                							}
                							var arrayOrigin: Array<number> = originContext.arrayOrigin;
                							vars.originFlowIndex = originContext.originFlowIndex;
                							vars.originCardChildOrigin = originContext.originCardChildOrigin;
                							console.log("[drop] moveActionElement", {
                								arrayOrigin,
                								arrayDestination,
                								destinationFlowIndex: page.local.currentIndex,
                								originFlowIndex: vars.originFlowIndex,
                								originElementId: originContext.originElementId,
                								targetCardChild: ''childrenRefs'',
                								originCardChildOrigin: vars.originCardChildOrigin
                							});
                							page["moveActionElement"](arrayDestination, arrayOrigin, page.local.currentIndex, vars.originFlowIndex, ''childrenRefs'', vars.originCardChildOrigin);
                						break;
                					}
                				}
                			}
                
                		}
                		catch(e){
                			console.error("and error occured while dropping element", e);
                		}
                		if (typeof page["cleanupDragState"] === "function") {
                			page["cleanupDragState"]("sharedDropIndicator.drop");
                		}
                		resolve();
                '
        ↓index [ngx.components.UIControlVariable-1600442667558]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:this.i
        ↓card [ngx.components.UIControlVariable-1600444086116]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:this.card
        ↓targetCardChild [ngx.components.UIControlVariable-1743530028391]: 
          varValue: 
            - xmlizable: 
              - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
              - MobileSmartSourceType: script:this.targetCardChild
    ↓Attr [ngx.components.UIAttribute-1600442734693]: 
      attrName: id
      attrValue: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
          - MobileSmartSourceType: script:this.id
    ↓Directive [ngx.components.UIControlDirective-1730726339637]: 
      directiveName: If
      directiveSource: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
          - MobileSmartSourceType: script:!this?.hideText
      ↓Text [ngx.components.UIText-1600440331793]: 
        i18n: true
        textValue: 
          - xmlizable: 
            - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
            - MobileSmartSourceType: 'script:this.action == true ? (translate.get(''drop_notice_action'', {index: (this.i[0] + 1)}) | async) : (translate.get(''drop_notice'') | async)'
    ↓Break [ngx.components.UIDynamicElement-1600867644078]: 
      beanData: '{"ionBean":"Break"}'
      selfClose: true
      tagName: br
    ↓_ngStyle_ [ngx.components.UIAttribute-1730907277986]: 
      attrName: '[ngStyle]'
      attrValue: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
          - MobileSmartSourceType: 'script:{''height'': (this.shouldHideEquivalentMoveDrop() || !this.local?.isOver) ? ''50px'' : ''75px'', ''border-style'': (this.local?.isOver && !this.shouldHideEquivalentMoveDrop()) ? ''solid'' : ''dashed'', ''border-color'': (this.local?.isOver && !this.shouldHideEquivalentMoveDrop()) ? ''var(--ion-color-primary-shade)'' : ''var(--ion-color-primary)'', ''background-color'': (this.local?.isOver && !this.shouldHideEquivalentMoveDrop()) ? ''#e5e7eb'' : ''#e5e7eb'', ''box-shadow'': (this.local?.isOver && !this.shouldHideEquivalentMoveDrop()) ? ''0 0 0 2px #e5e7eb'' : ''0 0 0 2px #e5e7eb''}'
    ↓Style1 [ngx.components.UIStyle-1775041764430]: 
      styleContent: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
          - com.twinsoft.convertigo.beans.common.FormatedContent: 
            →: |
              'display: flex;
              align-items: center;
              '
    ↓Attr2 [ngx.components.UIAttribute-1775196528195]: 
      attrName: class
      attrValue: 
        - xmlizable: 
          - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
          - MobileSmartSourceType: plain:initial btn
    ↓Event2 [ngx.components.UIControlEvent-1775210270021]: 
      attrName: (dragleave)
      eventName: (dragleave)
      ↓SetLocal [ngx.components.UIDynamicAction-1775210316662]: 
        beanData: |
          '{
           "ionBean": "SetLocalAction",
           "Property": "plain:isOver",
           "Value": "script:false"
          }'
  ↓Style [ngx.components.UIStyle-1730726829291]: 
    styleContent: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.common.FormatedContent
        - com.twinsoft.convertigo.beans.common.FormatedContent: 
          →: |
            //height:100%;
            min-height:25px;
            text-align:center;
            //min-width:20px;
            
  ↓Attr1 [ngx.components.UIAttribute-1776787858444]: 
    attrName: '[style.visibility]'
    attrValue: 
      - xmlizable: 
        - ↑classname: com.twinsoft.convertigo.beans.ngx.components.MobileSmartSourceType
        - MobileSmartSourceType: 'script:this.shouldHideEquivalentMoveDrop() ? ''hidden'' : ''visible'''
↓flow [ngx.components.UICompVariable-1741167108304]: 
  value: false
↓targetCardChild [ngx.components.UICompVariable-1743530003136]: 
  value: '''childrenRefs'''
↓formulas [ngx.components.UICompVariable-1743685609062]: 
  value: false
↓for_loop [ngx.components.UICompVariable-1743685641719]: 
  value: false
↓if_else [ngx.components.UICompVariable-1743685645054]: 
  value: false
↓relationTargetParentId [ngx.components.UICompVariable-1785751497477]: 
  value: 