accessibility: Hidden
authenticatedContextRequired: true
comment: |
  Patch user.
  Applies partial updates to a user profile.
↓checkAdminRights [steps.SimpleStep-1739891580456]: 
  expression: include("js/isAdmin.js");
↓Sequence_admin_user_patch [steps.SimpleStep-1739891580459]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    
    let loglevel = "warn";
    let authenticatedUserID = context.getAuthenticatedUser();
    try{
       meta = JSON.parse(meta);
    }
    catch(e){
       meta = {};
    }
    // check only supported tags (_id _rev favorites published_First language provider mail editing_rights c8o_view_type_users name surname displayName picture advancedEditing admin ~c8oAcl)
    let args = {};
    let arrayAuthorized = ["_id", "_rev", "favorites", "published_first", "language", "provider", "mail", "editing_rights", "nocode_db_rights","c8o_view_type_users", "name", "surname", "displayName", "picture", "advancedEditing", "admin", "~c8oAcl", "formulas", "admin_readonly", "publication"];
    for (let i = 0; i < arrayAuthorized.length; i++) {
    	if (meta[arrayAuthorized[i]] !== undefined) {
    		args[arrayAuthorized[i]] = meta[arrayAuthorized[i]];
    	}
    }
    args["c8o_view_type_users"] = true;
    	
    // post doc 
    doc = toJSON(fsclient.postDocument(java.lang.String("c8oforms_fs"), toJettison(args), new HashMap(), enums.CouchPostDocumentPolicy.merge, new HashMap(), false));
    	
    var toto = "str";'
↓object [steps.JsonToXmlStep-1739891580462]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: doc.ok == true
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: success
↓meta [variables.RequestableVariable-1739891697299]: 