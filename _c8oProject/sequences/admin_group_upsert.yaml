accessibility: Hidden
authenticatedContextRequired: true
comment: Retrieves multiple user's information, requires admin privileges.
↓checkAdminRights [steps.SimpleStep-1743432684309]: 
  expression: include("js/isAdmin.js");
↓Sequence_admin_user_patch [steps.SimpleStep-1743435477654]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    
    let loglevel = "warn";
    let authenticatedUserID = context.getAuthenticatedUser();
    // used for test
    //const obj = {_id: "toto", editing_rights: true, nocode_db_rights: true, admin: true};
    //meta = JSON.stringify(obj);
    try{
       meta = JSON.parse(meta);
    }
    catch(e){
       meta = {};
    }
    let args = {};
    let arrayAuthorized = ["_id", "editing_rights", "nocode_db_rights", "admin", "formulas", "admin_readonly", "publication"];
    for (let i = 0; i < arrayAuthorized.length; i++) {
    	if (meta[arrayAuthorized[i]] !== undefined) {
    		args[arrayAuthorized[i]] = meta[arrayAuthorized[i]];
    	}
    }
    args["group"] = true;
    	
    // post doc 
    doc = toJSON(fsclient.postDocument(java.lang.String("c8oforms_fs"), toJettison(args), new HashMap(), enums.CouchPostDocumentPolicy.merge, new HashMap(), false));'
↓object [steps.JsonToXmlStep-1743435484237]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: doc.ok == true
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: success
↓meta [variables.RequestableVariable-1743432747840]: 
  value: '{}'
↓insertWithSuccess [core.TestCase]: 
  ↓meta [variables.TestCaseVariable-1743436630973]: 
    value: '{\"_id\":\"toto\",\"editing_rights\":true,\"nocode_db_rights\":true,\"admin\":false}'