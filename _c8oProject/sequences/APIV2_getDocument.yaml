accessibility: Hidden
comment: |
  Fetch form document.
  Retrieves form data with ACL filtering and computed flags.
↓getAuthorizedDoc [steps.SimpleStep-1695127936279]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    
    var authenticatedUserID = context.getAuthenticatedUser();
    
    let loglevel = "warn";
    let authGranted = false;
    let db = "c8oforms_fs";
    if (fromResponse == "true") {
       db = "c8oforms_response_fs";
    }
    var doc = getDoc(id, rev, db);
    // now check if doc is authorized for currentUser
    if (fromResponse != "true" && (doc._deleted == null && (((doc["~c8oAcl"] == null && doc.c8oGrp == null) || doc["~c8oAcl"] == authenticatedUserID)) || (doc["~c8oAcl"].indexOf("C8Oreserved_") == -1) && doc["~c8oAcl"].length == 64))  {
       authGranted = true;
    }
    if (fromResponse == "true" && doc._deleted == null && (((doc["~c8oAcl"] == null && doc.c8oGrp == null) || doc["~c8oAcl"] == authenticatedUserID)))  {
       authGranted = true;
    }
    // if acl is not enough to grant access, check with used groups and doc.grp is not null
    let grpIsArray = isArray(doc.c8oGrp);
    let grpIsObject = isObject(doc.c8oGrp);
    if (!authGranted && doc.c8oGrp != null && ((grpIsArray && doc.c8oGrp.length > 0) || (grpIsObject && Object.keys(doc.c8oGrp).length > 0))) {
       // call c8ofullsyncgrp =>  design/get_groups_by_user
       query = new java.util.HashMap();
       query.put(''reduce'', ''false'');
       keys = toJettison([authenticatedUserID]);
       rows = toJSON(fsclient.postView("c8ofullsyncgrp", ''design'', ''get_groups_by_user'', query, keys)).rows;
    
       // call c8oforms_fs =>  authentication/distinctGroups
       query = new java.util.HashMap();
       query.put(''reduce'', ''true'');
       query.put(''group'', ''true'');
       _use_keys = rows.map((x) => { return x.value });
       keys = toJettison(_use_keys);
       usedGroups = toJSON(fsclient.postView(db, ''authentication'', ''distinctGroups'', query, keys));
       usedGroups = usedGroups.rows.map((x) => { return x.key });
       if (usedGroups.length > 0 && ((grpIsArray && doc.c8oGrp.some(function (value) { return usedGroups.indexOf(value) !== -1; })) || (grpIsObject && usedGroups.some(function (group) { return (doc.c8oGrp[group] == true || doc.c8oGrp[group] == "true") })))) {
          authGranted = true;
       }
    }
    if (!authGranted) {
    	// check if key was provided
    	if("k" in this && k != null){
    		var myKey = sha256Base64(id + secretCSV);
    		if((""+k) === (""+myKey)){
    			authGranted = true;
    		}
    	}
    }
    if (!authGranted) {
    	// check if its an admin
    	try{
    		include("js/isAdmin.js");
    		authGranted = true;
    	}
    	catch(e){
    		doc = null;
    	}
    }
    if (doc && doc.error) {
       doc = null;
    }
    if (doc != null && exportForm == "true") {
       let _attachments = doc._attachments;
       doc["__importAttachments"] = {};
       if (_attachments) {
          let prefixForTempPath = Math.floor(new Date().getTime() * Math.random());
          for (var k in _attachments) {
             let attfile;
             try {
                // create an empty file attachment
                let attpath = "" + prefixForTempPath + "_" + k;
                attpath = theApp.filePropertyManager.getFilepathFromProperty(attpath, context.projectName);
                attfile = new java.io.File(attpath);
                let query = new HashMap();
                // query attachment from doc
                fsclient.getDocumentAttachment(db, doc._id, k, query, attfile);
    			debugger;
                var b64 = encodeFileToBase64Binary(attfile);
                b64 = "data:" + _attachments[k].content_type + ";base64," + b64 + "";
    			if(doc[k] == undefined){
    				doc["__importAttachments"][k] = b64
    			}
    			else{
    				doc[k].b64 = b64;
    			}
                
             }
             catch (e) {
    			debugger;
                doc.error = true;
                doc.errorMessage = "an error occured while trying to extract attachment: " + k;
    			var d = e;
                console.log("an error occured while trying to extract attachment: " + k, e, loglevel);
             }
             finally {
                try {
                   // in any case remove temp file attachment, error can occured if file where not created yet when error occured
                   attfile.delete();
                }
                catch (e) { }
             }
          }
       }
       delete doc._attachments;
       delete doc._rev;
       delete doc._c8oMeta;
    }
    
    if(target == "bin"){
    	val = encodeTxtToBase64Binary(JSON.stringify(doc) + "");
    }'
↓object2 [steps.JsonToXmlStep-1695130350652]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: doc
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: res
↓jIf [steps.IfStep-1699546839489]: 
  condition: target == "bin"
  ↓attachment [steps.ElementStep-1699547046875]: 
    expression: val
    nodeName: attachment
    ↓jAttribute2 [steps.AttributeStep-1699546834093]: 
      expression: encodeURIComponent(doc.name) + '.c8oforms'
      nodeName: name
    ↓jAttribute3 [steps.AttributeStep-1699546834096]: 
      nodeName: type
      nodeText: attachment
    ↓jAttribute [steps.AttributeStep-1699546834087]: 
      nodeName: content-type
      nodeText: application/json
    ↓jAttribute4 [steps.AttributeStep-1699547164119]: 
      nodeName: encoding
      nodeText: base64
↓id [variables.RequestableVariable-1695130384939]: 
  comment: Generic identifier for the targeted resource.
↓rev [variables.RequestableVariable-1695130389024]: 
  comment: Document revision ensuring optimistic locking.
↓fromResponse [variables.RequestableVariable-1697472248791]: 
  comment: Indicates the fetch originates from response doc context.
  value: false
↓exportForm [variables.RequestableVariable-1699527736739]: 
  comment: Form identifier used for export routines.
  value: false
↓target [variables.RequestableVariable-1699546875078]: 
  comment: Target identifier for the action.
↓k [variables.RequestableVariable-1759314150162]: 
  comment: Legacy key parameter forwarded to sub sequences.
↓secretCSV [variables.RequestableVariable-1759314250209]: 
  comment: Shared secret protecting CSV export endpoints.
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xdc98be9b69416c3ca72546af1d4a57b685a1c306b83721ab9176ca0ffd0180616d79f2049b0777b1c07ec172695c9cb73712010fc51b6c7e21f45be57694f590a4f816c3996503379188ab04629c95fd8433dec20c026b245b6294d6bad06653
  visibility: '-1'
↓_200 [core.TestCase]: 
  ↓id [variables.TestCaseVariable-1747735725988]: 
  ↓rev [variables.TestCaseVariable-1747735725990]: 
  ↓fromResponse [variables.TestCaseVariable-1747735725992]: 
    value: false
  ↓exportForm [variables.TestCaseVariable-1747735725994]: 
    value: false
  ↓target [variables.TestCaseVariable-1747735725996]: 