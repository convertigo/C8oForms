accessibility: Hidden
comment: |
  Export responses CSV.
  Builds a CSV stream for responses while respecting header order and privacy.
responseTimeout: 600
↓computeCSV [steps.SimpleStep-1699365547406]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    var Jsoup = use("org.jsoup.Jsoup");
    Otherkey = "Other";
    const logLevel = "warn";
    const DEFAULT_BATCH_SIZE = 1000;
    var excludedHeaderIndexes = {};
    
    var writeCsvRow = function(writer, row, options) {
    	var stringDelimiter = options.stringDelimiter || ''"'';
    	var fieldSeparator = options.fieldSeparator || '','';
    	var lineSeparator = "\r\n";
    	var csvLine = row.map(function(field) {
    		if (field === null || field === undefined) {
    			field = "";
    		}
    		if (Array.isArray(field)) {
    			field = field.join(",");
    		}
    		if (typeof field !== "string") {
    			field = "" + field;
    		}
    		field = field.replace(new RegExp(stringDelimiter, "g"), stringDelimiter + stringDelimiter);
    		return stringDelimiter + field + stringDelimiter;
    	}).join(fieldSeparator) + lineSeparator;
    	writer.write(csvLine);
    };
    
    var buildHeaderRow = function(headers, key) {
      var headerRow = [];
      excludedHeaderIndexes = {};
      if (!Array.isArray(headers)) {
        return headerRow;
      }
      for (var idx = 0; idx < headers.length; idx++) {
        try {
          var header = headers[idx];
          if (!header) {
            excludedHeaderIndexes[idx] = true;
            continue;
          }
    
          var labelValue = header[key] || "";
          if (key === "labelHtml") {
            try {
              var doc = Jsoup.parse(labelValue);
              labelValue = "" + doc.body().text();
            } catch (e) {}
          }
    
          if (header.type === "checkbox_group" || header.type === "radio_group") {
            var categories = header.categories || [];
            if (categories.length === 0) {
              // si pas de catégories, garde quand même une colonne
              headerRow.push(labelValue);
            } else {
              // ➜ AJOUTE une colonne par catégorie
              for (var c = 0; c < categories.length; c++) {
                headerRow.push(labelValue + "_" + categories[c]);
              }
            }
          } else {
            // champ simple ➜ une seule colonne
            headerRow.push(labelValue);
          }
        } catch (e) {
          headerRow.push("");
        }
      }
      return headerRow;
    };
    
    
    var buildDataRow = function(nestedResponse, reverse_separator) {
      if (!Array.isArray(nestedResponse)) {
        return [];
      }
      var currentLineResponse = [];
    
      for (var colIndex = 0; colIndex < nestedResponse.length; colIndex++) {
        if (excludedHeaderIndexes && excludedHeaderIndexes[colIndex]) {
          continue;
        }
        var col = nestedResponse[colIndex];
    
        if (col == null) {
          // ➜ préserver l’alignement
          currentLineResponse.push("");
          continue;
        }
    
        switch (col.type) {
          case "radio_group":
          case "checkbox_group":
            // On construit N cellules (N = nb de catégories)
            // col.value = tableau d’options (OK/NOK/NA/...), chacune avec un .data de longueur N
            var nbCols = 0;
            if (Array.isArray(col.value) && col.value.length > 0 && Array.isArray(col.value[0].data)) {
              nbCols = col.value[0].data.length;
            }
    
            // pré-alloue les N cellules à ""
            for (var j = 0; j < nbCols; j++) {
              currentLineResponse.push("");
            }
    
            // remplit avec “OK|NOK|NA|...” selon data[j] == 1
            // (on concatène si plusieurs valeurs sont vraies)
            for (var subIndex = 0; subIndex < col.value.length; subIndex++) {
              var subcol = col.value[subIndex];
              var optName = subcol.name || "";
              for (var j2 = 0; j2 < subcol.data.length; j2++) {
                if (subcol.data[j2] == 1) {
                  var cellIdx = currentLineResponse.length - nbCols + j2; // les N dernières qu’on vient de pousser
                  var prev = currentLineResponse[cellIdx];
                  currentLineResponse[cellIdx] = prev
                    ? (prev + reverse_separator + optName)
                    : optName;
                }
              }
            }
            break;
    
          default:
            var value = col.value;
            if (Array.isArray(value)) {
              currentLineResponse.push(value.join(","));
            } else {
              currentLineResponse.push(value != null ? ("" + value) : "");
            }
            break;
        }
      }
    
      // nettoyage (sécurité)
      for (var k = 0; k < currentLineResponse.length; k++) {
        if (currentLineResponse[k] == null) currentLineResponse[k] = "";
      }
      return currentLineResponse;
    };
    
    
    let res = {};
    // map params
    var formId = decodeURIComponent(f);
    var name = decodeURIComponent(n);
    var offset = decodeURIComponent(o);
    var separator = (decodeURIComponent(s) == ";" || decodeURIComponent(s) == ",") ? decodeURIComponent(s) : ";";
    var encoding = (decodeURIComponent(e) != null && decodeURIComponent(e) != "") ? e : "UTF-8";
    var string_separator = (decodeURIComponent(se) == "''" || decodeURIComponent(se) == ''"'') ? decodeURIComponent(se) : ''"'';
    var column_name = decodeURIComponent(c);
    var version = decodeURIComponent(v);
    var lang = decodeURIComponent(l);
    var privacy = decodeURIComponent(p);
    var reverse_separator = (separator == ";" ? "," : ";");
    var skipParam = decodeURIComponent(sk);
    var limitParam = decodeURIComponent(li);
    var skip = 0;
    var limit = null;
    try {
    	if (skipParam != null && skipParam !== "") {
    		skip = Math.max(0, parseInt(skipParam, 10));
    	}
    }
    catch (e) { }
    try {
    	if (limitParam != null && limitParam !== "") {
    		var parsedLimit = parseInt(limitParam, 10);
    		if (!isNaN(parsedLimit) && parsedLimit > 0) {
    			limit = parsedLimit;
    		}
    	}
    }
    catch (e) { }
    
    var nameWithoutUnicode = (name.replace(/[^\x00-\x7F]/g, "")) + ".csv";
    var headerKey = column_name != "id" ? "labelHtml" : "id";
    
    var batchSize = DEFAULT_BATCH_SIZE;
    if (limit != null && limit < batchSize) {
    	batchSize = limit;
    }
    
    var csvDir = new java.io.File(context.getProjectDirectory() + "/csv");
    if (!csvDir.exists()) {
    	csvDir.mkdirs();
    }
    var fp = context.getProjectDirectory() + "/csv/" + formId + ".csv";
    var options = {
    	fieldSeparator: separator,
    	stringDelimiter: string_separator
    };
    var writer = null;
    var rowsWritten = 0;
    var headerWritten = false;
    
    try {
    	writer = new java.io.BufferedWriter(new java.io.OutputStreamWriter(new java.io.FileOutputStream(fp, false), encoding));
    	var currentSkip = skip;            // <- AU LIEU de currentOffset
    	var remaining  = limit;
    	var more = true;
    
    	while (more) {
    	  var currentLimit = batchSize;
    	  if (remaining != null) {
    	    currentLimit = Math.min(currentLimit, remaining);
    	    if (currentLimit <= 0) break;
    	  }
    
    	  // 🔧 IMPORTANT: l’API attend skip/limit (PAS offset)
    	  var metaPayload = { 
    	    skip: currentSkip,            // <- remplace offset: currentOffset
    	    limit: currentLimit,
    	    lang: lang
    	  };
    	  if (privacy === "id")   metaPayload.authorId   = true;
    	  if (privacy === "name") metaPayload.authorName = true;
    
    	  var call = callSequence("C8Oforms", "APIV2_getResponses", {
    	    k: k, formId: formId, version: version, summary: false, csv: true,
    	    meta: JSON.stringify(metaPayload)
    	  });
    
    	  var payload = (call && call.document) ? call.document.res : null;
    	  if (!payload || !payload.response) break;
    	   
    	  // entêtes 1x
    	  var headers = payload.response.value || [];
    	  if (!headerWritten) {
    	    var headerRow = buildHeaderRow(headers, headerKey);
    	    writeCsvRow(writer, headerRow, options);
    	    headerWritten = true;
    	  }
    
    	  // données
    	  var rows = payload.response.nestedResponses || [];
    	  
    	  if (rows.length === 0) break;
    
    	  for(var e of rows){
    			if (!Array.isArray(e)) {
    				continue;
    			}
    			var row = buildDataRow(e, reverse_separator);
    		    writeCsvRow(writer, row, options);
    		    rowsWritten++;
    	  }
    
    	  // ➕ avance le curseur avec la vraie quantité retournée
    	  currentSkip += rows.length;
    
    	  // limite globale éventuelle
    	  if (remaining != null) {
    	    remaining -= rows.length;
    	    if (remaining <= 0) break;
    	  }
    
    	  // sortie sûre si moins que demandé
    	  if (rows.length < currentLimit) more = false;
    
    	  // aide GC
    	  rows = null; headers = null; payload = null; call = null;
    	}
    	
    }
    catch (e) {
    	console.log("error", e, logLevel);
    	res["error"] = e;
    }
    finally {
    	if (writer != null) {
    		try {
    			writer.flush();
    			writer.close();
    		}
    		catch (e) { }
    	}
    }
    
    context.addFileToDeleteAtEndOfSession(new File(fp));
    res["rowsWritten"] = rowsWritten;
    res["filePath"] = fp;
    res["format"] = { separator: separator, stringDelimiter: string_separator, encoding: encoding };'
↓object2 [steps.JsonToXmlStep-1699365547409]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: res
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: res
↓Element [steps.XMLElementStep-1707220656205]: 
  nodeName: attachment
  ↓jAttribute [steps.AttributeStep-1707220656208]: 
    nodeName: content-type
    nodeText: application/csv
  ↓jAttribute1 [steps.AttributeStep-1707220656211]: 
    expression: context.getProjectDirectory() + "/csv/" + formId + ".csv";
    nodeName: local-url
  ↓jAttribute2 [steps.AttributeStep-1707220656214]: 
    expression: nameWithoutUnicode
    nodeName: name
  ↓jAttribute3 [steps.AttributeStep-1707220656217]: 
    nodeName: type
    nodeText: attachment
↓f [variables.RequestableVariable-1707232389123]: 
  comment: formId
↓n [variables.RequestableVariable-1707232389126]: 
  comment: name
↓o [variables.RequestableVariable-1707232389129]: 
  comment: offset
  value: 0
↓s [variables.RequestableVariable-1707232389132]: 
  comment: separator
  value: ;
↓e [variables.RequestableVariable-1707232389135]: 
  comment: encoding
  value: UTF-8
↓se [variables.RequestableVariable-1707232389138]: 
  comment: string_separator
  value: '"'
↓c [variables.RequestableVariable-1707232389141]: 
  comment: column_name
  value: id
↓v [variables.RequestableVariable-1707232389144]: 
  comment: version
  value: 
↓l [variables.RequestableVariable-1707232389147]: 
  comment: lang
  value: en
↓p [variables.RequestableVariable-1707232389150]: 
  comment: privacy
  value: ${C8Oforms.default_csv_privacy =name}
↓k [variables.RequestableVariable-1759313696879]: 
  comment: formId
↓sk [variables.RequestableVariable-1759419415185]: 
  comment: formId
  value: 0
↓li [variables.RequestableVariable-1759419415852]: 
  comment: formId
