↓computeResponses [steps.SimpleStep-1697532248606]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    Otherkey = "Other";
    const logLevel = "warn";
    let res = {};
    let i = 0;
    let flatResponses = [];
    let table = [];
    // start functions
    // used to format responses, will be used recursively
    let extractResponse = (response, j, type, _id) => {
    	switch (type) {
    		case "text":
    		case "datetime":
    		case "time":
    		case "barcode":
    			if (response.value != "") {
    				flatResponses[j].value.push(response.value);
    			}
    			break;
    		case "signature":
    		case "img":
    			if (response.value != "") {
    				if (i == 0) {
    					try { flatResponses[j].att_type = response.att_type == true; } catch (e) { }
    				}
    				if (flatResponses[j].att_type) {
    					let _val = context.getConvertigoUrl() + "fullsync/c8oforms_response_fs/" + encodeURIComponent(_id) + "/" + encodeURIComponent(response.name);
    					flatResponses[j].value.push(_val);
    				}
    				else {
    					flatResponses[j].value.push(response.value);
    				}
    			}
    			break;
    		case "location":
    			if (Object.keys(response.value.gps).length > 0) {
    				if (response["value"]["bounding_box"] != undefined) {
    					response.value = JSON.stringify(response["value"]["bounding_box"]);
    				}
    				else if (response["value"]["addr"] != undefined && response["value"]["addr"]["FormattedAddress"] != undefined && response["value"]["addr"]["FormattedAddress"] != "") {
    					response.value = response["value"]["addr"]["FormattedAddress"];
    				}
    				else {
    					response.value = { text: "Latitude : " + response["value"]["gps"]["latitude"] + ", Longitude : " + response["value"]["gps"]["longitude"] + ", précision : " + ("" + response["value"]["gps"]["accuracy"]).split(".")[0] + " métres", gps: response["value"]["gps"] };
    				}
    				flatResponses[j].value.push(response.value);
    			}
    			break;
    		case "grid":
    			if (response.value != null && response.value != "" && JSON.stringify(response.value) != "{}") {
    				let val;
    				if (!(response.value instanceof String)) {
    					val = JSON.stringify(response.value);
    				}
    				else{
    					val = response.value;
    				}
    				flatResponses[j].value.push(val);
    			}
    			break;
    		case "file":
    			if (response.value != "") {
    				let first = true;
    				let values = [];
    				if (response.value instanceof Array) {
    					for (let entryFile of response.value) {
    						if (typeof (entryFile) === "string") {
    							values.push({ "name": entryFile.split("_C80C80_")[1], "url": context.getConvertigoUrl() + "fullsync/c8oforms_response_fs/" + encodeURIComponent(_id) + "/" + encodeURIComponent(entryFile) });
    						}
    					}
    				}
    				flatResponses[j].value = flatResponses[j].value.concat(values);
    			}
    			break;
    		case "checkbox":
    		case "radio":
    		case "select":
    			for (let k in response["children"]) {
    				if (i == 0) {
    					if (flatResponses[j].value[k] == undefined) {
    						flatResponses[j].value[k] = 0;
    					}
    					if (response["children"][k].other == true) {
    						flatResponses[j].categories[k] = Otherkey;
    					}
    					else {
    						flatResponses[j].categories[k] = response["children"][k]["value"].length > 12 ? response["children"][k]["value"].substring(0, 12) + "..." : response["children"][k]["value"];
    					}
    				}
    				if (response["children"][k]["selected"] == true) {
    					flatResponses[j].value[k]++
    				}
    			}
    			break;
    		case "checkbox_group":
    			let m = 0;
    			for (let k in response["children"]) {
    				flatResponses[j]["categories"][m] = k.length > 12 ? k.substring(0, 12) + "..." : k;
    				for (let l in response["children"][k]) {
    					if (flatResponses[j]["value"][+l] == undefined) {
    						flatResponses[j]["value"][+l] = {};
    					}
    					if (flatResponses[j]["value"][+l]["name"] == undefined) {
    						flatResponses[j]["value"][+l]["name"] = response["children"][k][l]["value"].length > 12 ? response["children"][k][l]["value"].substring(0, 12) + "..." : response["children"][k][l]["value"];
    					}
    					if (flatResponses[j]["value"][+l]["data"] == undefined) {
    						flatResponses[j]["value"][+l]["data"] = [];
    					}
    					if (flatResponses[j]["value"][+l]["data"][m] == undefined) {
    						flatResponses[j]["value"][+l]["data"][m] = 0;
    					}
    					if (response["children"][k][l]["selected"] == true) {
    						flatResponses[j]["value"][+l]["data"][m]++;
    					}
    				}
    				m++;
    			}
    			break;
    		case "radio_group":
    			m = 0;
    			for (let k in response["children"]) {
    				flatResponses[j]["categories"][m] = k.substring(0, 12) + "...";
    				for (let l in response["children_label"]) {
    					if (flatResponses[j]["value"][+l] == undefined) {
    						flatResponses[j]["value"][+l] = {};
    					}
    					if (flatResponses[j]["value"][+l]["name"] == undefined) {
    						flatResponses[j]["value"][+l]["name"] = response["children_label"][+l]["value"].length > 12 ? response["children_label"][+l]["value"].substring(0, 12) + "..." : response["children_label"][+l]["value"];
    					}
    					if (flatResponses[j]["value"][+l]["data"] == undefined) {
    						flatResponses[j]["value"][+l]["data"] = [];
    					}
    					if (flatResponses[j]["value"][+l]["data"][m] == undefined) {
    						flatResponses[j]["value"][+l]["data"][m] = 0;
    					}
    					if (response["children_label"][l].value == response["children"][k]) {
    						flatResponses[j]["value"][+l]["data"][m]++;
    					}
    				}
    				m++;
    			}
    			break;
    		case "slider":
    			if (response["value"] !== "") {
    				if (flatResponses[j]["categories"].length === 0) {
    					flatResponses[j]["categories"].push("");
    				}
    				if (table == undefined) {
    					table = [];
    				}
    				if (table[j] == undefined) {
    					table[j] = {};
    				}
    				if (table[j][response["value"]] == undefined) {
    					table[j][response["value"]] = flatResponses[j]["value"].push({ data: [1], name: response["value"] }) - 1;
    				}
    				else {
    					let index = table[j][response["value"]];
    					let count = flatResponses[j]["value"][index]["data"][0];
    					flatResponses[j]["value"][index]["data"] = [count + 1];
    				}
    			}
    			break;
    		case "actionValue":
    				if(response["valueType"] == "attachment"){
    					if (i == 0) {
    						flatResponses[j].att_type = true;
    					}					
    					flatResponses[j].value.push({ "name": response.value, "url": context.getConvertigoUrl() + "fullsync/c8oforms_response_fs/" + encodeURIComponent(_id) + "/" + encodeURIComponent(response.value) });
    				}
    				else{
    					flatResponses[j].value.push(response.value);
    				}
    			break;
    	}
    }
    //start code
    let resp;
    try {
    	resp = (callSequence("C8Oforms", "APIV2_getDocument", { id: formId })).document.res;
    }
    catch (e) {
    
    }
    // if authorized and exists doc
    if (resp) {
    	res["published_form"] = resp;
    	let currentVersion;
    	// define all existing version for this form
    	res["versions"] = createArray(resp.version);
    	if (version && res.versions.indexOf(+version) != -1) {
    		currentVersion = +version;
    	}
    	else {
    		currentVersion = +resp.version;
    	}
    	query = new java.util.HashMap();
    	query.put(''reduce'', ''false'');
    	query.put(''startkey'', JSON.stringify([formId, "" + currentVersion]));
    	query.put(''endkey'', JSON.stringify([formId, "" + currentVersion]));
    	query.put("include_docs", "true");
    	let responses;
    	responses = toJSON(fsclient.getView("c8oforms_response_fs", ''read_responses'', ''getResponsesByVersion'', query)).rows;
    	res.response = { length: responses.length };
    
    	let sortedsIds = [];
    	const pageOrder = {};
    
    	res["published_form"].pages.forEach((page, index) => {
    		let _tempArr = res["published_form"].formulaire.filter((x) => {
    			return x.config.page == page.pageTechName;
    		}).flatMap((x) => {
    			if (x.type === "ion-card") {
    				let prefix = x.name;
    				const _temp_subArray = x.children.map((y) => {
    					return { path: prefix + ".children." + y.name, type: y.type };
    				});
    				return _temp_subArray;
    			}
    			else {
    				return { path: x.name, type: x.type };
    
    			}
    		});
    		sortedsIds = sortedsIds.concat(_tempArr);
    	});
    	// sorted responses for current versions
    	//res["sorted"] = sortedsIds;
    
    	let len = sortedsIds.length;
    	let lenResponses = responses.length;
    	res["raw_responses"] = responses;
    
    	// go over each fields of the form
    	for (let i = 0; i < len; i++) {
    		// go over each responses
    		for (let j = 0; j < lenResponses; j++) {
    			// create resp struct
    			let s = sortedsIds[i].path.split(".");
    			let response = responses[j].doc.resp;
    			let elem;
    			for (let part of s) {
    				response = response[part];
    				elem = part;
    			}
    			if (response) {
    				if (j == 0) {
    					flatResponses[i] = { "id": elem, att_type: false, "label": response.label, "labelHtml": response.labelHtml, "type": response.type, value: [], categories: [], "name": response.name };
    				}
    				extractResponse(response, i, sortedsIds[i].type, responses[j].id);
    			}
    		}
    	}
    	let k = +("" + flatResponses.length);
    	let lenActions = res["published_form"].actions.filter((x)=>{return x.type === "submit"}).length;
    	// go over each actions of the form
    	for(i = 0; i < lenActions; i++){
    		// go over each responses
    		for (let j = 0; j < lenResponses; j++) {
    			if(responses[j].doc.actionsValue){
    				let keys = Object.keys(responses[j].doc.actionsValue);
    				let indexName = keys[i];
    				let response = responses[j].doc.actionsValue[indexName];
    				
    				
    				if (response) {
    					response.type = "actionValue";
    					if (j == 0) {
    						flatResponses[k + i] = { "id": indexName, att_type: false, "label": indexName, "labelHtml": indexName, "type": "actionValue", value: [], "name": indexName };
    					}
    					extractResponse(response, k + i, response.type, responses[j].id);
    				}
    			}
    			
    		}
    		
    	}
    	res["published_form"].pages
    	res.response.value = flatResponses;
    	res["currentVersion"] = currentVersion;
    
    }
    
    '
↓object2 [steps.JsonToXmlStep-1697532248609]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: res
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: res
↓formId [variables.RequestableVariable-1697532236824]: 
  value: published_1697531850821
↓version [variables.RequestableVariable-1697536095803]: 