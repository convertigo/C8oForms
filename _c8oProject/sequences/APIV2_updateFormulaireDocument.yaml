accessibility: Hidden
authenticatedContextRequired: true
comment: |
  Update form document.
  Applies draft updates, merges metadata and handles attachments.
↓updateOrCreateForm [steps.SimpleStep-1696261644247]: 
  expression: |
    '// Imports and helpers functions
    include("js/common.js");
    include("js/form_import.js");
    include("js/application_history.js");
    
    let loglevel = "warn";
    let authenticatedUserID = context.getAuthenticatedUser();
    let authGranted = false;
    let new_doc = false;
    // draft the caller may edit, as it was before this save (#1359)
    let historyDraft = null;
    
    try {
    	meta = JSON.parse(meta);
    }
    catch (e) {
    	meta = {};
    }
    include("js/form_write_guard.js");
    var formWriteGuard = c8oFormWriteGuard.prepare(meta, function (id) { return getDoc(id, null, "c8oforms_fs"); });
    if (formWriteGuard.error) {
    	doc = formWriteGuard.error;
    } else {
    // if we are creating a new form from a template
    if (meta.template) {
    	let tag = meta["tag"];
    	meta = getDoc(meta.templateId, { attachments: true }, "c8oforms_fs");
    	if (meta.c8o_view_type_template == true) {
    		new_doc = true;
    		// delete somes entries
    		delete meta.c8o_view_type_template;
    		delete meta.id;
    		delete meta._id;
    		delete meta._rev;
    		delete meta.rev;
    		delete meta._c8oMeta;
    
    		//add some entries
    		meta.c8o_view_type_drafts_form = true;
    		meta._id = "" + new Date().getTime();
    		meta["pages"][0]["iconName"] = "sticky-note";
    		meta["creator"] = authenticatedUserID;
    		meta["navigation"] = {
    		    positionTab: null,
    		    enabledTab: null,
    		    enabledButtons: null,
    		    positionButtons: null,
    			included: null,
    			checkMandatoryInCurrentPage: null,
    			buttonsBehavior: "scroll",
                    buttonsOrientation: "horizontal",
                    buttonsVerticalPosition: "left",
    		};
    		meta["~c8oAcl"] = authenticatedUserID;
    		// add timestamp to store creation date
    		meta.creationDate = "" + new Date().getTime();
    		if(tag != null && tag != ""){
    			if(!Array.isArray(tag)){
    				meta["tag"] = [tag];
    			}
    		}
    
    		// if no actions existing create default array
    		if(meta.flows == undefined){
    			meta.flows = [{"id": "formulas", "elements": []}, {"id": "submit", elements: []}];
    		}
    		if(meta.actions != undefined){
    			meta.actions.forEach((action)=>{
    				if(action.type == "business_logic"){
    					meta.flows[0].elements.push(action);
    				}
    				if(action.type == "submit"){
    					meta.flows[1].elements.push(action);
    				}
    			});
    			meta.actions = null;
    		}
    	}
    	else {
    		doc = { error: "unauthorized" };
    	}
    }
    else {
    	let existing_doc = formWriteGuard.enabled ? formWriteGuard.document : (meta._id != undefined ? getDoc(meta._id, null, "c8oforms_fs") : false);
    	//check if new doc
    	if (!existing_doc) {
    		new_doc = true;
    		let random = "assets/images/svg/imgplaceholder/placeholder" + (Math.floor(Math.random() * Math.floor(3))) + ".svg";
    		meta["_id"] = "" + new Date().getTime();
    		//migrate source form id if from import
    		try{
    			if(meta["fromImport"] == true){
    				c8oFormImport.sanitizeStructure(meta);
    				// export metadata and history bookkeeping of the source application
    				delete meta["__c8oformsExport"];
    				delete meta["historyCheckpointAt"];
    				// #1359: the first version of the imported application is recorded as an import
    				meta["historyNextOrigin"] = "import";
    				for(var key_form in meta["formulaire"]){
    					var elem =  meta["formulaire"][key_form];
    					if(elem.sources){
    						for(let key_source in elem.sources){
    							if(elem.sources[key_source].vars && elem.sources[key_source].vars.forms_config && elem.sources[key_source].vars.forms_config.str){
    								var sourceConfig = JSON.parse(elem.sources[key_source].vars.forms_config.str);
    								sourceConfig.form_id = meta["_id"];
    								meta["formulaire"][key_form].sources[key_source].vars.forms_config.str = JSON.stringify(sourceConfig);
    							}
    						}
    					}
    				}
    				if(meta["flows"]){
    					for(var key_flow in meta["flows"]){
    						for(var key_flow_elem in meta["flows"][key_flow].elements){
    							var elem =  meta["flows"][key_flow].elements[key_flow_elem];
    							if(elem.sources){
    								for(let key_source in elem.sources){
    									if(elem.sources[key_source].vars && elem.sources[key_source].vars.forms_config && elem.sources[key_source].vars.forms_config.str){
    										var sourceConfig = JSON.parse(elem.sources[key_source].vars.forms_config.str);
    										sourceConfig.form_id = meta["_id"];
    										meta["flows"][key_flow].elements[key_flow_elem].sources[key_source].vars.forms_config.str = JSON.stringify(sourceConfig);
    									}
    								}
    							}
    							if(elem.actions){
    								for(let key_action in elem.actions){
    									if(elem.actions[key_action].vars && elem.actions[key_action].vars.forms_config && elem.actions[key_action].vars.forms_config.str){
    										var sourceConfig = JSON.parse(elem.actions[key_action].vars.forms_config.str);
    										sourceConfig.form_id = meta["_id"];
    										meta["flows"][key_flow].elements[key_flow_elem].actions[key_action].vars.forms_config.str = JSON.stringify(sourceConfig);
    									}
    								}
    							}
    						}
    					}
    				}
    			}
    		}
    		catch(e){
    			
    		}
    		meta["loopToForm"] = meta["loopToForm"] != null ? meta["loopToForm"] : true;
    		meta["progressIndicator"] = meta["progressIndicator"] != null ? meta["progressIndicator"] : false;
    		meta["formulaire"] = meta["formulaire"] != null ? meta["formulaire"] : [];
    		meta["wallpaper"] = meta["wallpaper"] != null ? meta["wallpaper"] : { ''link'': null, ''enabled'': false, ''index'': null, ''random'': random };
    		meta["thumbnail"] = meta["thumbnail"] != null ? meta["thumbnail"] : { ''enabled'': false, ''index'': null, ''random'': random };
    		meta["descform"] = meta["descform"] != null ? meta["descform"] : "";
    		meta["namePosition"] = meta["namePosition"] != null ? meta["namePosition"] : "text-center";
    		meta["descformPosition"] = meta["descformPosition"] != null ? meta["descformPosition"] : "text-left";
    		if (meta["navigation"]) {			
    			meta["navigation"] = {
    			    positionTab: meta["navigation"]["positionTab"] != null ? meta["navigation"]["positionTab"] : null,
    			    enabledTab: meta["navigation"]["enabledTab"] != null ? meta["navigation"]["enabledTab"] : null,
    			    enabledButtons: meta["navigation"]["enabledButtons"] != null ? meta["navigation"]["enabledButtons"] : null,
    			    positionButtons: meta["navigation"]["positionButtons"] != null ? meta["navigation"]["positionButtons"] : null,
    				included: meta["navigation"]["included"] != null ? meta["navigation"]["included"] : null,
    				checkMandatoryInCurrentPage: meta["navigation"]["checkMandatoryInCurrentPage"] != null ? meta["navigation"]["checkMandatoryInCurrentPage"] : null,
    				buttonsBehavior: meta["navigation"]["buttonsBehavior"] != null && ("" + meta["navigation"]["buttonsBehavior"]).trim() !== "" ? meta["navigation"]["buttonsBehavior"] : "scroll",
                        buttonsOrientation: meta["navigation"]["buttonsOrientation"] != null && ("" + meta["navigation"]["buttonsOrientation"]).trim() !== "" ? meta["navigation"]["buttonsOrientation"] : "horizontal",
                        buttonsVerticalPosition: meta["navigation"]["buttonsVerticalPosition"] != null && ("" + meta["navigation"]["buttonsVerticalPosition"]).trim() !== "" ? meta["navigation"]["buttonsVerticalPosition"] : "left",
    				postPublishAnonymous: meta["navigation"]["appliedConfiguration"] != null ? meta["navigation"]["appliedConfiguration"] : null,
    			};
    		} else {
    			meta["navigation"] = {
    			    positionTab: null,
    			    enabledTab: null,
    			    enabledButtons: null,
    			    positionButtons: null,
    				included: null,
    				checkMandatoryInCurrentPage: null,
    				buttonsBehavior: "scroll",
                    buttonsOrientation: "horizontal",
                    buttonsVerticalPosition: "left",
    			};
    		}
    		meta["globalNavigationEnabled"] = meta["globalNavigationEnabled"] != undefined ? meta["globalNavigationEnabled"] : true; 
            meta["pages"] = meta["pages"] != null ? meta["pages"] : [{ name: "Page 1", isNameDisplayed: true, pageTechName: "Page" + new Date().getTime(), positionTab: "bottom", enabledTab: false, included: true, enabledButtons: true, positionButtons: "tab", buttonsBehavior: "scroll", buttonsOrientation: "horizontal", buttonsVerticalPosition: "left", iconName: "sticky-note", checkMandatoryInCurrentPage: true, individualNavigation: false }];
    		meta["creationDate"] = "" + new Date().getTime();
    		meta["creator"] = authenticatedUserID;
    		meta["~c8oAcl"] = authenticatedUserID;
    		meta["flows"] = meta["flows"] != null ?  meta["flows"] : [{"id": "formulas", "elements": []}, {"id": "submit", elements: []}];
    		meta["technicalVersion"] = meta["fromImport"] == true ? meta["technicalVersion"] : "1.0.16";
    		meta["c8o_view_type_drafts_form"] = true;
    		meta["config"] = meta["config"] != null ? meta["config"] : {
    			desktop: {
    				horizontalMargin: {unit: "px", value: "100"}, width:{unit: "px", value: "1280"}, "font-size": {unit: "px", value: "16"}
    			},
    			tablet: {
    				horizontalMargin: {unit: "px", value: "60"}, width: {unit: "px", value: "940"}, "font-size": {unit: "px", value: "16"}
    			},
    			phoneL: {
    				horizontalMargin: {unit: "px", value: "10"}, width: {unit: "%", value: "100"}, "font-size": {unit: "px", value: "16"}
    			},
    			phoneP: {
    				horizontalMargin: {unit: "px", value: "10"}, width: {unit: "%", value: "100"}, "font-size": {unit: "px", value: "16"}
    			}
    		};
    		if(meta["tag"] != null && meta["tag"] != ""){
    			if(!Array.isArray(meta["tag"])){
    				meta["tag"] = [meta["tag"]];
    			}
    		}
    		else{
    			try{
    				delete meta["tag"];
    			}
    			catch(e){
    				
    			}
    		}
    	}
    	if (!new_doc) {
    		// now check if existing_doc is authorized for currentUser
    		authGranted = canEditFormDocument(existing_doc, authenticatedUserID);
    		if (!authGranted) {
    			existing_doc = null;
    		}
    		else {
    			historyDraft = existing_doc;
    		}
    	}
    }
    // we can proceed to the action
    if (new_doc || authGranted) {
    	var _up_thumbnail, _up_wallpaper, _up_delete_attach, _up_any_file = false;
    	var _up_content_type, _up_name;
    	// add timestamp to store last modification date
    	meta["lastMofification"] = "" + new Date().getTime();
    	if (file && meta["_up_thumbnail"]) {
    		_up_thumbnail = true;
    		_up_content_type = "" + meta["_up_content_type"];
    		delete meta["_up_thumbnail"];
    		delete meta["_up_content_type"];
    	}
    	else if (file && meta["_up_wallpaper"]) {
    		_up_wallpaper = true;
    		_up_content_type = "" + meta["_up_content_type"];
    		delete meta["_up_wallpaper"];
    		delete meta["_up_content_type"];
    	}
    	else if (meta["_up_delete_attach"]) {
    		_up_delete_attach = "" + meta["_up_delete_attach"];
    		delete meta["_up_delete_attach"];
    	}
    	if (meta["_up_any_file"]) {
    		_up_any_file = true;
    		_up_content_type = "" + meta["_up_content_type"];
    		_up_name = "" + meta["_up_name"];
    		delete meta["_up_any_file"];
    		delete meta["_up_content_type"];
    		delete meta["_up_name"];
    	}
    	// #1359: the first save of each minute first keeps the state the draft holds as an automatic version
    	// (attachment uploads excluded: an import uploads its media right after creating the draft).
    	if (historyDraft != null && !_up_thumbnail && !_up_wallpaper && !_up_delete_attach && !_up_any_file) {
    		if (c8oHistory.checkpointDue(historyDraft)) {
    			try {
    				c8oHistory.snapshot(historyDraft, historyDraft.historyNextOrigin == "import" ? "import" : "auto", { author: authenticatedUserID, skipIfUnchanged: true });
    			}
    			catch (e) {
    				log.warn("application history: automatic version of " + historyDraft._id + " failed: " + e);
    			}
    			// set even after a failure, so a broken history store does not slow down every save
    			meta["historyCheckpointAt"] = new Date().getTime();
    			meta["historyNextOrigin"] = null;
    		}
    		else if (historyDraft.historyCheckpointAt != null) {
    			meta["historyCheckpointAt"] = historyDraft.historyCheckpointAt;
    		}
    	}
    	// prepare merge params
    	let mergeRules = new HashMap();
    	if (meta["formulaire"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("formulaire")), new java.lang.String("override"));
    	}
    	if (meta["pages"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("pages")), new java.lang.String("override"));
    	}
    	if (meta["actions"] || meta["actions"] === null) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("actions")), new java.lang.String("override"));
    	}
    	if (meta["tag"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("tag")), new java.lang.String("override"));
    	}
    	if (meta["subTag"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("subTag")), new java.lang.String("override"));
    	}
    	if (meta["config"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("config")), new java.lang.String("override"));
    	}
    	if (meta["flows"]) {
    		mergeRules.put(java.util.Arrays.asList(new java.lang.String("flows")), new java.lang.String("override"));
    	}
    	// post doc 
    	doc = toJSON(fsclient.postDocument(java.lang.String("c8oforms_fs"), toJettison(meta), new HashMap(), formWriteGuard.enabled ? enums.CouchPostDocumentPolicy.none : enums.CouchPostDocumentPolicy.merge, mergeRules, false));
    
    	// A revision conflict must not upload or delete any attachment.
    	if (!doc.error) {
    	if (_up_thumbnail) {
    		attfile = new java.io.File(file);
    		fsclient.putDocumentAttachment("c8oforms_fs", meta._id, "thumbnail", new HashMap(), attfile, _up_content_type);
    	}
    	else if (_up_wallpaper) {
    		attfile = new java.io.File(file);
    		fsclient.putDocumentAttachment("c8oforms_fs", meta._id, "wallpaper", new HashMap(), attfile, _up_content_type);
    	}
    	else if (_up_delete_attach) {
    		try {
    			// catch can be normal cause previous attach not always exists
    			fsclient.deleteDocumentAttachment("c8oforms_fs", meta._id, _up_delete_attach, new HashMap());
    		}
    		catch (e) {
    			console.log("deleteDocumentAttachment", e, loglevel);
    		}
    
    	}
    	if (_up_any_file) {
    		attfile = new java.io.File(file);
    		fsclient.putDocumentAttachment("c8oforms_fs", meta._id, _up_name, new HashMap(), attfile, _up_content_type);
    	}
    	}
    }
    }
    '
↓object2 [steps.JsonToXmlStep-1696261644250]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: doc
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: res
↓meta [variables.RequestableVariable-1696262383951]: 
  comment: JSON metadata payload associated with the operation.
↓file [variables.RequestableVariable-1697451140315]: 
  comment: Binary file content processed by the sequence.
  isFileUpload: true